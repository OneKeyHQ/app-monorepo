interface:
  display_name: "OneKey Trade Swap Market"
  short_description: "App-side Swap, Stock/Market, Wallet Home/Home Token/Send/Earn handoff, native/mobile host, provider channel, cold-start, history/status, and order-flow guidance"
  default_prompt: "Use $1k-trade-swap-market for OneKey Swap, Swap Pro, Stock/Market speed-swap, Wallet Home or Home Token entries, Send/Earn/Market/Buy handoffs into Swap, native/mobile host differences, Bridge, Limit, K-line/chart, token selectors, cold-start frame-by-frame validation, quote/build/send flows, provider channels, PrivateSend-like channels, stock/order channels, state-machine/hook fixes, history/status, local writeback, replay/repair, fees, slippage, ETA, or funding handoffs into Swap. For Jira/Slack/review/ledger-derived tasks, verify the current evidence packet before coding; identify the closest valid repo pattern before adding hooks, atoms, adapters, or abstractions; split large hooks by stable business responsibility rather than line count."
