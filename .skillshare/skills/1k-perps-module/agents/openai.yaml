interface:
  display_name: "OneKey Perps / Hyperliquid"
  short_description: "Perps code navigation and domain contracts"
  default_prompt: "Use $1k-perps-module to work on OneKey Perps orders, market data, accounts, deposits, signing, charts, or layouts, reading the references relevant to the task."
