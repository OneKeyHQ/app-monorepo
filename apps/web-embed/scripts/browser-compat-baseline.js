const CHROMIUM_BASELINE_VERSION = 80;
const CHROMIUM_BASELINE_TARGET = String(CHROMIUM_BASELINE_VERSION);
const CHROMIUM_BASELINE_LABEL = `Chromium ${CHROMIUM_BASELINE_VERSION}`;

module.exports = {
  CHROMIUM_BASELINE_LABEL,
  CHROMIUM_BASELINE_TARGET,
  CHROMIUM_BASELINE_VERSION,
};
