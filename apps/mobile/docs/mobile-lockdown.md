# Mobile LavaMoat lockdown

The mobile integration provides intrinsic integrity for the iOS and Android Hermes runtimes. It does not implement LavaMoat package compartments or per-package permissions. Dependencies retain access to ambient application capabilities such as networking and native modules. The official [React Native adapter](https://github.com/LavaMoat/LavaMoat/tree/main/packages/react-native-lockdown) supplies intrinsic repair and hardening; this is a narrower boundary than the [package runtime](https://lavamoat.github.io/about/runtime-environment/).

## Build and rollback

The current candidate enables lockdown when `ONEKEY_MOBILE_LOCKDOWN` is omitted or `true`, consistently across Metro, Babel, and development vendor generation. Set `ONEKEY_MOBILE_LOCKDOWN=false` explicitly to disable both SES injection and final hardening. Other values are rejected to catch deployment mistakes. Default activation is still being validated. The Debug results below predate the native Promise tracker correction and must be repeated with its stronger Promise checks; neither replaces native Release acceptance.

Restart Metro after changing the option. Its transform cache is separated by mode. Development vendor fingerprints also include the mode, the adapter configuration, and the finalization source; rebuild the vendor common bundle and rerun the official development-shell launcher to refresh its session resources when changing modes. An enabled delta must never run against a disabled vendor common bundle.

Lockdown cannot be undone in a running JavaScript realm. Rollback means distributing a build with the option disabled and restarting **both** runtimes. A hardening exception deliberately aborts startup; the app does not silently continue with partial protection.

The adapter is pinned to `@lavamoat/react-native-lockdown@1.0.0`, which pins `ses@1.15.0`. Version 2.0.0 changes only the Node.js engine requirement. The adapter's own dependency tree resolves `ses/hermes`; the separate web SES version must not be loaded into the same mobile realm.

Babel ignores the security preludes. In enabled builds, the existing mobile transformer returns a parse-only AST for exactly the adapter's two resolved real paths. This prevents Expo's ignored-file fallback from reloading application Babel transforms without a filename; unrelated dependencies still use their normal transformer. The developer vendor serializer composes its module selection with the configured SES guard.

The shared SES diagnostic code uses a platform-specific loader. Its native variant refuses late loading instead of pulling the vanilla web SES bundle into a lazy native segment. The vanilla bundle contains syntax that Hermes cannot compile when correctly excluded from Babel, and loading it would create a second SES instance.

## Initialization order and runtime ownership

Both production runtimes live in the same native process but have independent JavaScript heaps. The common bundle is evaluated separately in `main` and `background`; a successful lockdown in one is not proof of lockdown in the other. Each runtime independently follows this order:

1. Metro loads the untransformed Hermes SES shim and the official React Native repair prelude.
2. Metro polyfills and React Native `InitializeCore` install their vetted shims.
3. The entry sets its runtime identity and synchronously loads OneKey's shared polyfills.
4. `prepareNativePromiseRejectionTracker` installs the native Promise rejection hooks while the vetted shim window is open.
5. `finishMobileLockdown` hardens the intrinsics, checks the result, and publishes an immutable diagnostic state before wallet bootstrap.

Code executed during the vetted-shim window is trusted bootstrap code. Freezing cannot undo malicious changes made before finalization, so those dependencies remain part of the trusted computing base.

The official `lockdownSerializer` hardcodes entry module ID `0`. OneKey's persisted module registry assigns positive IDs, and union, vendor, and segment serialization use multiple entries. The integration therefore reuses the official SES/repair sources through `getPolyfills`, preserves all existing serializer hooks, and finalizes explicitly in both entry modules. No entry ID or load-order assumption links the two realms.

Union builds place the SES prelude in common and finalization in each runtime's startup dependency graph. Segments register modules into an already hardened realm. Development vendor builds similarly carry repair in common while the workspace entry finalizes after loading its shared polyfills. HMR does not rerun hardening; changing runtime identity or build mode within an existing realm is rejected.

Native resources such as native storage and module singletons may be process-shared. Lockdown does not freeze those native objects or alter their ownership. Each runtime owns its own SES functions, intrinsic objects, and diagnostic state; native initialization remains independent and must preserve the existing storage launch gates.

These two business runtimes are not an exhaustive list of contexts that native dependencies can create. The installed React Native Worklets implementation constructs separate Hermes runtimes for UI/worklet execution and initializes them through its own native decorators and worklet loaders. Those contexts do not inherit main/background intrinsic state, and this integration has not injected or validated lockdown there. Native modules, worklet initialization, and their bridges remain trusted capabilities; success in main and background must not be described as protection of every native-created JavaScript context.

The official repair uses `evalTaming: 'unsafe-eval'` because Hermes does not provide the direct-eval semantics needed for SES safe evaluation. This integration must not be presented as `L2` safe-eval confinement. Its diagnostic state is separate from the existing web/extension SES hardening page:

```js
globalThis.__ONEKEY_MOBILE_LOCKDOWN_STATE__;
// { enabled: true, runtime: 'main', lockdownApplied: true, evalTaming: 'unsafe-eval' }
```

Read this value in **each** runtime. An enabled build must also report frozen `Object.prototype`, `Array.prototype`, and `Function.prototype` and a callable `harden`. Absence of the repair prelude causes an error before wallet bootstrap.

## Automated verification

From the repository root, using a supported Node.js version:

```sh
yarn jest --runInBand --runTestsByPath apps/mobile/plugins/__tests__/mobileLockdown.test.js --coverage=false
env -u ONEKEY_MOBILE_LOCKDOWN NODE_ENV=production node apps/mobile/scripts/check-mobile-lockdown.js
ONEKEY_MOBILE_LOCKDOWN=true NODE_ENV=production node apps/mobile/scripts/check-mobile-lockdown.js
ONEKEY_MOBILE_LOCKDOWN=false NODE_ENV=production node apps/mobile/scripts/check-mobile-lockdown.js
```

The Jest suite executes the real SES/repair sources in separate Node VM realms. It checks intrinsic tamper resistance, independent main/background state, exactly-once initialization, missing-prelude failure, default and rollback behavior, serializer preservation, vendor fingerprints, and entry ordering. A configuration fixture also verifies that the real manifest validator rejects a rollback vendor before an enabled delta can run.

The compiler check runs the actual mobile Babel configuration and the Expo script worker in debug and release modes, verifies that SES and repair are ignored by application transforms and gain no Metro dependencies, executes the resulting main/background fixtures in Node VM realms, and compiles them using this repository's pinned Hermes compiler. It writes artifacts to a temporary directory and prints their paths. **Compilation and Node VM tests do not prove native Hermes runtime compatibility.** The compiler package does not contain an executable Hermes VM.

### Repeatable native Debug acceptance

Use the official Mobile DevSession launcher with an explicitly selected empty simulator/emulator, a dedicated Metro port, and `--metro-bind-host 127.0.0.1`. The listener setting is separate from the device-visible `--metro-url`; physical-device workflows retain their existing default binding. Wait for its running receipt. Bind the app's inspector to the same Metro using React Native's supported per-app debug preferences; never attach to another worktree's default inspector port.

Run the following against the receipt inside this worktree's session directory:

```sh
node apps/mobile/scripts/check-mobile-lockdown-runtime.js \
  --report node_modules/.cache/onekey-mobile-dev/sessions/<session>/run-result.json \
  --device <explicit-device-id> --target-title '<exact-inspector-title>' \
  --isolated-profile --output <evidence-directory>
```

The checker validates the receipt, current vendor manifest and artifacts, loopback listener, and two targets from the selected native device. It compares each heap's live bundle URL against the private DevSession, platform, runtime identity, and fingerprint before attempting tamper/async probes or reading LogBox. It waits ten seconds for deferred startup work, then requires immutable state, frozen intrinsics, tamper rejection, and Promise/timer/`process.nextTick` delivery in both heaps. Disabled LogBox observation or any retained error, fatal, or syntax entry fails acceptance. Logs are neither cleared nor ignored. The checker writes a private `runtime-report.json` on success or runtime failure; transport tests use a real isolated localhost WebSocket server.

This is a bounded startup/runtime check. Verify actual onboarding and relevant interactions separately through `agent-device`, retaining screenshots and logs. Release builds have no Debug inspector; their native loader and application flows need separate device evidence.

### Repeatable native Release acceptance

Build the real native Release application with `UNION_BUILD=true`, native background threading and segmented bundles enabled, and a fresh `ONEKEY_MOBILE_LOCKDOWN_E2E` run ID containing exactly 32 lowercase hexadecimal characters. This option requires lockdown to be enabled. It is an explicit test-build option: when omitted, a dedicated Babel gate removes its entry branches before Metro collects dependencies, including in development. The compiler check verifies that neither Release diagnostic helper enters a default main/background entry. The option and diagnostic sources also participate in development transform/vendor fingerprints. Guarded helper modules retain committed production registry IDs allocated by the normal registry tool; a regression checks them through the strict production resolver, without disabling registry enforcement or changing existing IDs.

After the normal production segment loader is installed, each heap independently checks its immutable lockdown state, frozen intrinsics, same-value tamper rejection, Promise/timer/`process.nextTick` delivery, and built-in native bundle context. It then dynamically imports an initially unloaded marker segment through the real loader and checks that the segment executed inside the hardened heap. The loader retains its full SHA-256 verification. Diagnostic records contain only the public run/build/runtime identity and security results; a short segment digest prefix correlates records with host artifacts but does not replace full integrity verification.

Main and background write separate `[MobileLockdownE2E]` records using `NativeLogger`. On Android, this explicit test build also sends the same fixed fields through React Native's native logging hook so they can be collected from the isolated app's PID without root access or a Release inspector. On iOS, read the task simulator's own app log. Keep the run's surrounding native fatal/JavaScript error observations alongside the records; a probe success alone is not application acceptance.

After background's existing profile launch acknowledgement and API bootstrap, a second guarded probe calls the existing WebEmbed test bridge through background's normal reverse RPC to main. Main first waits for the actual UI provider's event subscription before requesting a mount. The checker requires the matching run ID, that provider-ready request, the platform's expected source (`bundled-https` for Android and `bundled-scheme` for iOS candidates, both with `fileBridge: false`), and frozen WebEmbed intrinsics. It logs no file path or bridge error payload. This must run against the actual protected WebEmbed assets packaged into the native application; an ordinary unprotected Rspack resource fails. The WebEmbed state is an intrinsic check, while package policy, raw SES preservation, SRI, and exact packaged asset identity require their separate build checks.

The same background probe then calls the existing `chainKaspa` API for two sequential `buildCommitTxInfo` → unsigned `createKRC20RevealTxJSON` → `deserializeFromSafeJSON` operations. It uses only a fixed public address and synthetic unfunded UTXO, with no account lookup, signing, or chain request. Original SafeJSON must contain an explicit empty signature and the expected outpoint; only then may the normalized signature be empty or omitted, matching the pinned SDK's getter behavior. Raw and normalized transaction fields must agree, and the two complete operations must preserve the same validated unsigned result. The shared 60-second deadline covers bridge readiness and both operations; late replies cannot start further requests. Reports contain only fixed status fields and require `intrinsics: true`, `kaspaUnsigned: true`, and `kaspaRuns: 2`. A transaction failure retains the independent intrinsic result and a fixed failure stage, without logging transaction or error payloads.

For a modern-engine candidate that cannot yet pass the existing Chromium 67 baseline, set all three `ONEKEY_MOBILE_LOCKDOWN_E2E_WEB_EMBED_ARTIFACT`, `ONEKEY_MOBILE_LOCKDOWN_E2E_WEB_EMBED_MANIFEST`, and `ONEKEY_MOBILE_LOCKDOWN_E2E_WEB_EMBED_MANIFEST_SHA256` inputs explicitly. The manifest is a reviewed map of every relative asset path to its complete SHA-256; its own digest must be pinned separately. With `NODE_ENV=production` and the valid E2E run ID, run `node apps/mobile/scripts/prepare-lockdown-web-embed.js` before native compilation, so iOS resource-copy phases receive the same verified bytes. The bundle builder repeats the verification and preparation. Normal builds do not execute this branch.

The preparer rejects missing or additional files, path traversal, symbolic links, changed bytes, and source/output overlap. It validates the protected HTML, raw SES, SRI, and wrappers, then verifies all files again in each generated native asset directory. It executes the existing browser-compatibility checker unchanged and records its actual outcome in a private candidate receipt. A failed Chromium 67 gate remains a failed gate; this explicitly marked E2E candidate is never declared release eligible. Device execution of such a candidate cannot establish compatibility with the currently supported minimum engine or authorize changing that baseline.

The following command requires exactly one successful record from each heap, matching build identity, real Hermes artifacts containing the run ID, a corresponding marker artifact, the protected packaged WebEmbed result, and no recognized native fatal/JavaScript errors in the supplied isolated run log:

```sh
node apps/mobile/scripts/check-mobile-lockdown-release.js \
  --artifacts apps/mobile/out-dir-bundle/<platform>/dist \
  --platform <ios-or-android> --run-id <run-id> \
  --native-log <isolated-run-log> --output <private-report.json>
```

Run the same artifact checker without `--run-id` or `--native-log` on a normal Release output to require that no test probe appears in its Hermes bytecode. Verify that the native APK/app contains these exact host-verified artifacts, then retain actual onboarding and flow evidence separately. The tests for these diagnostics use controlled fixtures and do not constitute native Release acceptance.

## Historical validation record (2026-09-08, opt-in builds)

Validation used React Native 0.86.2, Expo 57.0.14, the repository's `hermes-compiler@250829098.0.16`, and Node.js 24. Artifact checks and native execution are recorded separately below. Both main and background own their own JavaScript heaps; these checks do not establish shared native storage or native module behavior.

| Check                                                   | Result                                                                                                                                |
| ------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| Actual mobile Babel configuration, enabled and disabled | Main and background fixtures executed in separate Node VM realms and compiled to Hermes bytecode; SES and repair were not transformed |
| iOS production union and segment build, enabled         | Common, main, background, and segments generated; all generated bundles compiled to Hermes bytecode                                   |
| Android production union and segment build, enabled     | Same complete bundle and Hermes compilation checks passed                                                                             |
| Split-bundle integrity, both platforms                  | Scanned 2,611 main, 6 background, and 286 background-shared segments; zero main or background violations                              |
| Native SES resolution                                   | Shared native loader selected; the serializer rejects any second/vanilla SES instance before emitting a bundle                        |

The iOS compiler emitted 1,111 host-global warning lines and Android emitted 1,110, such as references to native-installed globals. Both union builds also reported existing forbidden-common-startup graph entries, including `ServiceCloudBackup`. These warnings require separate startup-graph work; this validation does not claim a warning-free build. Android native runtime verification was not part of this historical compiler run; the later default-on Android device run is recorded separately below.

### iOS Debug runtime smoke

An isolated, empty iPhone 17 simulator running iOS 26.5 started with `ONEKEY_MOBILE_LOCKDOWN=true` through the official development-shell launcher. The final dependency lock changed the exact native shell input key, despite an unchanged native contract. The official tooling rebuilt the Debug shell incrementally with CocoaPods 1.17.0 in deployment mode, then signed, packaged, and verified it. Remote WebEmbed, shell, and vendor lookups returned HTTP 404, so the launcher built those resources locally. The subsequent disabled control and restored-enabled run reused the verified shell and WebEmbed caches and regenerated the vendor for each mode.

The enabled vendor fingerprint was `53c37832325e2736b8a930361d1f5b166a61f36270775388461e191466e8dcc6` (9,087 common modules). The signed shell archive SHA-256 was `e68a4ed13c28b52455f098fbc7397b54040f490c9a5b5bdc58f0a3b741e7f63c`, with exact shell input key `6fbd4089ea5964e0cd395f50e2fbed5b503e8dee5f0689591f4899e68a2d27ab`. Both live runtimes in the enabled runs reported the matching vendor fingerprint.

| Enabled native Debug check                                                | main                                                        | background                                                        |
| ------------------------------------------------------------------------- | ----------------------------------------------------------- | ----------------------------------------------------------------- |
| Independent immutable diagnostic state                                    | `enabled: true`, `lockdownApplied: true`, `runtime: 'main'` | `enabled: true`, `lockdownApplied: true`, `runtime: 'background'` |
| `Object.prototype`, `Array.prototype`, `Function.prototype`               | All frozen                                                  | All frozen                                                        |
| Callable `harden`; strict same-value assignment to `Array.prototype.push` | Function present; assignment rejected                       | Function present; assignment rejected                             |
| Promise, timer, and `process.nextTick` delivery                           | All delivered                                               | All delivered                                                     |

The following official run receipts and inspector responses were retained separately. Each receipt reported `status: running`, `shell.signing: verified-archive`, and `userNoticeRequired: true` for its automatic local fallback. The notices were reported, and no cache or signature checks were bypassed.

| Mode             | Receipt `launchedAt` (UTC) | main LogBox observations                            |
| ---------------- | -------------------------- | --------------------------------------------------- |
| Enabled          | `2026-09-08T16:49:46.078Z` | Three RevenueCat/Prime logout error entries         |
| Disabled control | `2026-09-08T17:07:13.014Z` | No entries, including a follow-up two minutes later |
| Enabled restored | `2026-09-08T17:12:59.058Z` | No entries in the final snapshot                    |

Background had one warning placeholder, `Open debugger to view warnings.`, in all three runs. LogBox remained enabled and its entries were not cleared. There were no recorded lockdown, read-only-property, fatal, or syntax exceptions; the first enabled run was not free of application errors.

The first main error reported an unconfigured RevenueCat singleton; the following SDK and wrapper errors reported logout of an anonymous user. Read-only SDK checks later returned `configured: true` and `anonymous: true`. Source review found that `PrimeGlobalEffect` immediately reconciles logged-out state through `logoutPurchasesSdk`, while `usePrimePaymentMethods.native` awaits configuration lookup and defers SDK configuration to `requestIdleCallback`. The logout wrapper logs failures; the effect retries when logout has not completed. These paths match the PR's base branch. The calls run in main against RevenueCat's process-wide native singleton; the main/background JavaScript heaps and lockdown states remain independent.

Neither the disabled control nor the restored-enabled snapshot reproduced those errors. This bounded comparison does not establish their complete cause or rule out an effect of hardening on initialization timing. SDK lifecycle and application-flow acceptance remain pending; account and authentication logic were not changed.

The disabled vendor fingerprint was `56a70d897f8adc654d6ba970140478f7dc5fe1d16202f623378d60c0462ff91b`. Both disabled heaps reported `enabled: false`, `lockdownApplied: false`, unfrozen Object/Array/Function prototypes, and no `harden` function. The launcher restarted the native process between runs. This checks disabled development startup, not production OTA rollback.

The wallet onboarding screen rendered in all three runs without creating/importing a wallet, unlocking, or signing. Screenshots and inspector diagnostics were captured from the isolated simulator. The task's Metro and UI sessions were stopped and the simulator was shut down afterward.

### Final patch input verification

After the final Web tooling patches, the official enabled launcher regenerated the iOS vendor with fingerprint `f11354bdb674c77a566ebf4646a4f4e045bfef9b388911489a9a4ffa6e0cc2c7` (9,087 common modules). The vendor configuration fingerprints the entire `patches` directory, including the JavaScript-only `lavamoat-core` receiver fix. That tooling module is not a mobile runtime dependency, and the common module source digest remained unchanged. The native build digest, contract, exact shell input key, and signed archive SHA-256 also remained unchanged; the verified shell was reused without native compilation.

The vendor-build run launched at `2026-09-08T17:40:14.012Z`. Its remote vendor lookup returned HTTP 404 and its reported local-build notice was retained. Onboarding rendered, but that run alone was not accepted as per-heap verification: the file-based native bundle initially registered its inspector on the default Metro port. A subsequent official launch used React Native's supported `SIMCTL_CHILD_RCT_METRO_PORT` environment setting to route the app's inspector to the task's own Metro. The diagnostic client supplied the localhost Origin required by React Native's debugger connection check. No server, authentication, cache, or signature checks were disabled.

The final receipt at `2026-09-08T17:46:48.583Z` reported `status: running`, `shell.signing: verified-archive`, all three resources from `local-cache`, and `userNoticeRequired: false`. Main and background each reported the final fingerprint and passed every enabled native check in the table above, including immutable state, intrinsic tamper rejection, and Promise/timer/`process.nextTick` delivery. Onboarding rendered again without wallet operations. These are independent checks of the two Hermes heaps; native resources remain process-shared where applicable.

LogBox remained enabled and was not cleared. The final main snapshot contained seven error entries: the three RevenueCat/Prime errors from the first enabled run, two missing developer gallery routes (`component-Form` and `component-Navigation`), a React missing-key diagnostic naming `TermsAndPrivacy`, and a 2,205 ms Dialog animation diagnostic. Background retained one warning placeholder. This run was not free of application errors and reproduced the earlier RevenueCat observations despite their absence in the intermediate disabled and restored-enabled snapshots.

Bounded source review found that `routeUtils` checks every gallery enum only in `platformEnv.isDev`, including the two entries absent from the gallery registration list. React's development renderer emitted the key diagnostic for the onboarding rich-text children; Dialog's native development check logs measured animations exceeding 550 ms. The relevant route, gallery, onboarding, and Dialog sources match base `d7fc238`; the captured LogBox stacks were empty. This identifies the diagnostic sources, not a complete causal explanation or proof that hardening has no timing impact. Application-flow and performance acceptance remain pending. The earlier disabled control remains evidence for its recorded vendor fingerprint and was not repeated for this final patch input. Final receipts, screenshots, and per-heap responses were retained, then the task's sessions were stopped and its isolated simulator was shut down.

These checks confirm the listed Debug startup and JavaScript properties in both heaps. They do not verify the native release segment loader, production OTA rollback, hardware or storage concurrency, wallet operations, startup timing, or heap overhead. The same native process can share resources even though the two diagnostic states and their intrinsics are separate JavaScript copies.

## Default-on candidate: Android Debug device record

An empty, task-specific Google APIs ARM64 emulator running Android API 36 launched the application with `ONEKEY_MOBILE_LOCKDOWN` unset. The official DevSession built the canonical WebEmbed, signed Android Debug shell, and development vendor after their remote cache lookups returned HTTP 404. All three local-build notices were reported. Cache, signature, native-contract, and session checks remained enabled; no user wallet was created, imported, unlocked, or used for signing.

The receipt's launch time was `2026-09-08T19:15:21.731Z`. Its native shell input key was `591b761be04b1ee2abd0f62645c71faca8b47b52356717fc0bd267b6515b5ac4`, native contract was `6f47b0469d763b15293be6060ea8cbb052b2fb8e2c5a2685310e869b1a98d486`, and APK SHA-256 was `e06095dc4fedc5c429948ad908907174c137c1240865014fc5123ebe648551f5`. The vendor fingerprint was `8960fd5df5e0ca1ad3b7524258a499a36a4747d0a8a32296b02b5fc8367a5125`, containing 9,101 common modules. The canonical WebEmbed input key was `fbeeffc19f8c5ab7d87e88962afb9220ca28864fbeaa6f5b47aaf6c0bafa11ee`; that resource used the existing Rspack build and does not establish acceptance of the separate protected WebEmbed build.

The app was relaunched using React Native's supported per-app debug preferences to select the task's dedicated Metro. Both inspector targets belonged to that same emulator. Before any mutation probe, each heap's live bundle URL, private session identity, platform, runtime identity, and vendor fingerprint matched the official receipt and current manifest. At `2026-09-08T19:19:26.389Z`, main and background independently passed immutable state/binding, frozen Object/Array/Function prototypes, callable `harden`, strict same-value intrinsic assignment rejection, and Promise/timer/`process.nextTick` delivery. These are two independent JavaScript results; process-wide native resources were not duplicated or tested for concurrency by the diagnostic.

LogBox remained enabled and was not cleared. Main had no retained entries; background had the React Native warning placeholder `Open debugger to view warnings.`. React Native routes warning details through its debugger console when full Fusebox console support is present; this placeholder is not evidence of warning-free execution. Its source condition applies to warning entries, while errors and exceptions still enter LogBox. No error, fatal, or syntax entries were observed in either heap's snapshot. The onboarding screen rendered its create, add-existing, and hardware-wallet choices, with a screenshot retained separately. These bounded observations do not replace wallet-flow or performance acceptance.

This first Android run preceded the explicit Metro listener option. It used a dedicated port with the launcher's original all-interface binding. The subsequent `--metro-bind-host 127.0.0.1` change preserves physical-device defaults but changes the vendor fingerprint; the final checker now requires the receipt to attest a loopback-only listener. A fresh official run is required before accepting that final configuration. The recorded Android result remains evidence for the exact artifact above, rather than evidence for an artifact generated after the launcher change.

### Final Android default-on and loopback verification

The current default-on configuration completed a fresh official DevSession at `2026-09-08T20:45:52.350Z`, with `ONEKEY_MOBILE_LOCKDOWN` unset and the explicit loopback listener. Its canonical normal WebEmbed, exact native shell, and vendor each required a reported HTTP 404 local fallback. Native Debug compilation passed in 54 seconds, using 157 executed and 1,803 up-to-date tasks. The new APK SHA-256 was `b9b424ade61a6c1ee5f2e9e690999f541e2658696a61314de9f029646e7f5c5a`, exact shell input key `e034a15b22e1054ab0ca6b45627fa0a03598de9c443bf9bd218dd4ed68bcba61`, and native build digest `48a7577d45389d4cdceb32a605419055c8aa92ddea1f75b8733cbe92e4e4fe43`. The native contract remained unchanged from the first Android record.

At `2026-09-08T20:46:40.639Z`, the strict runtime checker passed against vendor fingerprint `a75b8773256905fc95ccb2bf3ecf4577477088ed64458a528ca82e48695db51a` with 9,101 common modules. Each heap independently matched the current receipt, live development bundle URL, platform, runtime, and fingerprint before passing immutable state/binding, frozen intrinsics, `harden`, tamper rejection, and Promise/timer/`process.nextTick` delivery. In both heaps LogBox remained enabled, tracing was inactive, and logs were retained. Main contained no entries; background contained only its warning placeholder. No retained error, fatal, or syntax entries were observed. The actual onboarding screen and its three wallet choices were captured and visually inspected without wallet operations.

This run replaces the first Android record for the current loopback/default-on acceptance. It used canonical WebEmbed input `b05d9d959e62f00d6aefe7cf0ef9040351ae7768d0c18fc2ed17c1630d89a175`, which passed the unchanged Chromium 67 check with 39 external and three inline scripts; that normal Rspack resource is separate from the protected modern-engine candidate. The task's Android app and DevSession were stopped after retaining the receipt, exact artifact hashes, per-heap report, and screenshot. Native shared-resource concurrency and Release behavior remain separate checks.

### Current iOS default-on and loopback verification

The isolated iOS simulator completed a fresh official default-on launch at `2026-09-08T21:00:54.497Z`. Canonical WebEmbed was reused; exact shell and vendor remote lookups returned HTTP 404, and both local-build notices were reported. CocoaPods 1.17.0 completed deployment installation with 217 pods, and the official Debug build, signing, archive verification, and installation passed. The signed archive SHA-256 was `1bc8d7d0889f7fdcea3bd25deb983a159dd132a23a54050d9197e90d3b00f63b`, exact shell input key `1839be331e19fd682669b5d58db83c4d8f137be7cf77148727daa444faee64fe`, and build digest `84d347b0305047fa1710fe5fed7ab333e3c024f0048ca981c093e678d612a709`.

Both heaps matched current vendor fingerprint `0f8a0314b64565aa377cd232c65d6d68fc064c1fcb13609e333d8010408918da` with 9,087 common modules. The strict checker passed at `2026-09-08T21:02:28.164Z` and again at `2026-09-08T21:04:50.801Z`: independent immutable state/binding, frozen intrinsics, `harden`, tamper rejection, and all three async checks passed. Both live development bundle URLs matched the private receipt and each heap's public `__DEV__` value was `true`. LogBox remained enabled with tracing inactive; main had no retained entries and background retained only its warning placeholder in both snapshots. The actual onboarding screen was captured and visually inspected without wallet operations.

The seven historical iOS diagnostics did not recur in this bounded current-input run. Their original evidence remains above; this result does not establish their complete cause or exclude timing-dependent application issues. Main/background are still independent JavaScript heaps sharing one native process, and native-resource concurrency was not exercised. The task's app/UI session, launcher, and Metro were stopped and its simulator shut down after preserving current receipts, hashes, per-heap reports, development flags, and screenshot.

## Native Promise tracking compatibility

The Release test detected that `nativePromiseRejectionTracker.native.ts` replaced global `Promise` after hardening when application services subscribed to errors. A fixture using the actual RN polyfill, npm Promise, and SES sources reproduced the changed constructor identity and mutable replacement prototype. A second experiment with the exact APK's Hermes V1 engine found that simply registering its native rejection tracker after hardening also fails: that API installs private properties on the Promise constructor. Its native API property is itself non-writable and non-configurable.

Each `main` and `background` entry now prepares one native tracker after synchronous polyfills and before hardening. The original Hermes API and Promise remain unchanged. Application listeners and SDK observers update separate closures after hardening; callback replacement does not reinstall native hooks or reset pending-rejection identifiers. Only the `allRejections: true` contract used by the installed RN and Sentry versions is supported; unsupported filter options fail explicitly. Sentry's normal synchronous/native error integration remains active. A small custom integration forwards unhandled rejections through `captureException` with its original `onunhandledrejection` mechanism and retains handled notifications. Explicit rollback preserves the previous npm Promise and SDK configuration. The default reporter uses the installed formatter API and a fixed fallback when formatting fails; a `finally` block preserves application delivery even when console reporting throws. BigInt and circular rejections retain their original identity for both observers.

The standalone experiment uses the same public engine libraries extracted from the first Release APK, not an application inspector or wallet process. Both a fresh runtime and one with a previously registered RN-style tracker passed on Hermes V1 `250829098.0.16`: preserved constructor identity, frozen constructor/prototype, unchanged native API, a real non-Error rejection value, SDK callback replacement, one app delivery, handled notification, and native microtask delivery. This supports the engine compatibility fix; it does not replace new Debug or Release application runs on either platform.

The reproducible sources are `scripts/create-hermes-promise-fixture.js` and `scripts/fixtures/hermes-promise-runner.cpp`. Generate the fixture with:

```sh
node apps/mobile/scripts/create-hermes-promise-fixture.js --output "$EVIDENCE_DIR/promise-fixture.js"
node apps/mobile/scripts/create-hermes-promise-fixture.js --pre-registered --output "$EVIDENCE_DIR/promise-pre-registered.js"
```

Compile the C++ runner with the installed Android NDK, the matching Hermes Prefab headers and RN `ReactCommon/jsi` headers, and `-lhermesvm -ljsi`. Link and deploy `libhermesvm.so`, `libjsi.so`, `libfbjni.so`, and `libc++_shared.so` from the exact tested APK to a task-owned temporary directory on an explicitly selected emulator. Run the executable with that directory as `LD_LIBRARY_PATH` and the generated fixture as its only argument. The runner enables the engine microtask queue, evaluates the fixture, drains real microtasks, and fails unless all fixed-field assertions pass. Do not run it inside an existing app heap. The recorded engine SHA-256 is `9c6400211e31275e8ab4e53a4b7bc2048120bd32697eadd1d8f882ff7eb699b8`; the JSI SHA-256 is `5acb80d7eb93b2114a419c9e42b44476969299e806ad7eafe1b293e221b9a682`.

## First actual Android Release candidate: failed acceptance

The first completed non-debuggable `ProdRelease` APK used run ID `d27626cab0055876d388d6fa804e284f`. Its SHA-256 was `7f473df6e657b8fb59dded6f5d53da65cf4fc47993cd70eee53ff0c84e7c51e8`. The host verified all 2,628 packaged Hermes files and all 43 exact protected WebEmbed files against their source SHA-256 values, and APK signature verification passed. The isolated API 36 emulator used Android WebView `133.0.6943.137`; onboarding rendered normally.

Main's intrinsic/tamper/async and native-segment checks passed. Background reported a segment-check failure; native logs prove that its 688-byte shared marker segment was evaluated in the background heap, so the shared segment directory was not the failure. The Promise replacement described above was independently reproduced and corrected in the subsequent APK. The WebEmbed call timed out before a successful bridge reply; source review found that its one-shot mount request could precede the UI provider's delayed subscription. The test now requires an actual provider listener before main requests mounting, and still requires the real background RPC response. Native profile acknowledgement alone is not UI-provider readiness.

This run also lacked explicit production `BUILD_NUMBER` and `BUNDLE_VERSION` inputs. Its JS version was `6.6.0`, while the ordinary local native template reported `1.0.0`/`1`. The E2E build preflight now rejects incomplete production identity before Metro compilation. Future local runs must supply standard version/build/bundle inputs consistent with the native template; no runtime identity fallback is added.

Real RevenueCat errors were retained: an unconfigured singleton logout, an anonymous-user logout, and billing unavailable on the emulator. These are application-flow findings, not a clean-startup result. The emulator's billing limitation does not establish the cause of every account timing error. This first APK remains failed acceptance despite its valid packaging and visible onboarding; the corrected APK has a separate record below.

The Android APK HTTPS asset-loader path is currently limited to an explicit modern-engine E2E candidate. It serves the exact APK WebEmbed through `https://appassets.androidplatform.net/web-embed/index.html` while preserving raw SES and SRI. The mode remains `releaseEligible: false`; the existing Chromium 67 gate is still unresolved. Its private bridge accepts only the exact packaged document's main frame, fails closed without `WEB_MESSAGE_LISTENER`, and does not create child popup WebViews. Ordinary DApp WebViews and OTA file paths remain outside this candidate mode. iOS was not integrated in that first APK experiment; its subsequent candidate scheme is described below. Actual Android WebView content, MIME handling, and adversarial bridge/SRI checks remain separate from successful startup and its security-state reply.

### Corrected Android Release: runtime passed, interactive onboarding blocked

The next normal `assembleProdRelease` completed with lockdown unset (default enabled), native background threading, union bundles, and real lazy segments. Run ID `c061404cd967f1a07bcac9604985e151` produced non-debuggable APK SHA-256 `6e1d71379b67e6fb7b1a4816c815c7d897c15fb5bce658179b3d8fbe7133974e`. Signature verification passed, and the installed APK matched the host hash. All 2,628 Hermes artifacts and all 43 protected WebEmbed files matched their complete source hashes. Its explicit local test identity was native/JS version `1.0.0`, build `1`, bundle `1788907215`; this was not a published app release.

The isolated Android API 36 emulator cold-launched this APK at `2026-09-08T23:24:37Z`. Main and background independently passed immutable state/binding, frozen Object/Array/Function/Promise prototypes, intrinsic tamper rejection, and Promise/timer/`process.nextTick` delivery. Each heap loaded its initially unloaded marker through the actual native segment loader. Main observed the UI provider subscription before requesting WebEmbed; background then received a successful real RPC reply from the exact packaged HTTPS document with frozen WebEmbed intrinsics. The strict host checker required `bridgeSource: 'bundled-https'`, not an interchangeable file/OTA source. These are separate JavaScript heaps sharing one native process; the result does not establish native-singleton concurrency safety.

The retained own-process log contained 38 error-level and 2,722 warning-level lines, including native forwarding of some JavaScript messages. It contained no recorded fatal exception, fatal signal, `[JSError]`, or `JavascriptException`. Main still logged an anonymous RevenueCat logout, and the native billing service was unavailable; the previous unconfigured logout was not observed, which does not explain its absence. Background's inscription-protection response guard raised an invalid-response error that `ServiceSetting` caught and logged. Eleven native Yoga diagnostics reported `Could not parse yoga::Display: block` when the update Dialog appeared. The guard and Dialog sources match the base branch; neither was changed or suppressed. Source attribution does not prove hardening has no timing impact, and purchase/backend/UI acceptance remains incomplete.

The visible foreground was the app's own `MainActivity`. Onboarding existed beneath a featured forced-update Dialog with no close action because the local `1.0.0` identity triggered the existing version policy. The download button was not clicked and the version check was not bypassed. Interactive onboarding and wallet operations therefore remain blocked for this APK. Screenshots, the UI tree, all retained diagnostics, full artifact records, and a separate acceptance summary preserve that distinction.

Four real Android WebView `133.0.6943.137` instrumentation tests subsequently passed against this same Release APK. The test APK contained no replacement native libraries; the app's actual Release class loader and mapped Hermes/React Native libraries were verified. The installed main APK hash was unchanged before and after testing. The scoped parent frame delivered two messages, while actual same-origin and opaque-origin iframe attempts received their DOM acknowledgements but delivered no native messages. HTML/base-URL spoofing and navigation attempts were rejected; an ordinary WebView still delivered its normal bridge message without receiving APK asset mappings. Real browser responses matched the APK's JavaScript and 11.5 MB binary bytes, MIME types, and SHA-256 values. With the original HTML and SRI attributes intact, a mutated runtime response was rejected by the actual browser and its sentinel did not execute. Lack of `WEB_MESSAGE_LISTENER` remains a unit-tested failure branch because this installed provider supports it. These checks establish the tested native WebView boundaries, not wallet-flow or Chromium 67 compatibility acceptance.

The following Android run and planned iOS run use the repository's `6.6.0` release identity through the same native version fields as the pinned CI actions, with build `1` from the manual native workflow and bundle version measured from the CI's 2026 UTC epoch. Native files are saved by original bytes/hash and restored only if their contents still match the task's temporary write. No runtime version fallback or app update-policy change is introduced. The protected WebEmbed remains a modern-engine candidate with the existing Chromium 67 gate failing and `releaseEligible: false`.

### Android Release with consistent 6.6.0 identity: creation and unlock passed

The subsequent normal `assembleProdRelease` used the repository's `6.6.0` version, manual build `1`, and CI-epoch bundle version `21685552` consistently in native and JavaScript inputs. Lockdown remained unset, exercising the default-enabled candidate. The guarded native version edit was restored to its original bytes after compilation. Run ID `49c7b6cc369794bde77dc572a5b680ff` produced non-debuggable APK SHA-256 `be9323643a628cc9f6edaf7851af30ea54daeb3b8243c6f23a6d59fdcb20ec12`; signature checks, the installed APK hash, all 2,628 Hermes artifacts, and all 43 protected WebEmbed hashes passed.

On the isolated API 36 emulator with WebView `133.0.6943.137`, main and background separately passed the complete intrinsic/immutable binding/tamper/async/native-segment checks, including frozen Promise. Background's actual WebEmbed RPC returned the expected `bundled-https` source and frozen WebEmbed intrinsics after main's provider-ready mount request. All four real native WebView boundary/SRI instrumentation tests were repeated successfully against this exact APK, with target class-loader/native-library provenance and unchanged installed APK hashes. These remain three distinct JavaScript contexts: main Hermes, background Hermes, and the WebView; the two Hermes heaps share a native process and may use shared native singletons.

The normal onboarding screen was interactive, with no forced-update blocker. Through the standard UI, the test created a new unfunded wallet with on-device entropy, set and confirmed a temporary random passcode, and entered `Account #1` with `$0.00`. The normal `Lock now` action displayed `Welcome back` and the secure passcode field. Correct passcode authentication returned to the same wallet home. Screenshots and UI records confirm both transitions. No recovery words or private keys were opened, exported, or recorded; the passcode existed only in a private process's memory. After collecting evidence, that process exited, the UI session closed, and only this task-created app profile was cleared on its explicitly selected emulator.

This was not an error-free application run. The initial cold-start record retained 27 error-level lines. The separate wallet-flow interval retained 87 error-level and 5,548 warning-level lines, including native forwarding duplicates, and no recognized fatal exception, fatal signal, `[JSError]`, `JavascriptException`, or unhandled-rejection signature. Main again logged an unconfigured RevenueCat logout followed by anonymous-user logout errors; native billing and offerings remained unavailable. Background logged two caught invalid inscription-protection responses. Ten Yoga `Could not parse yoga::Display: block` diagnostics occurred during this **6.6.0 wallet-flow interval**, and native rendering logged 45 SurfaceTexture/EGL errors plus one buffer-release diagnostic during onboarding transitions. These records are retained separately from the successful security and interaction assertions. The account timing, response guard, and Dialog sources remain unchanged; that source comparison does not prove hardening has no timing effect or resolve the diagnostics.

The private acceptance sidecar is `android-release-current-acceptance.json`, alongside separate runtime, full packaged-artifact, instrumentation, wallet-flow log, screenshot, and cleanup records. The run does not cover wallet import, signing, DApp/WalletConnect, hardware transport, purchases, notification delivery, OTA rollback, or formal performance. This exact APK predates the subsequent iOS-only scheme wiring, native patch, and Kaspa candidate correction described below. It did not execute the new two-operation Kaspa check and cannot satisfy its strengthened report contract. Its historical evidence remains valid for its recorded bytes; it is not a fresh build or acceptance record for the new candidate. Its modern-engine WebEmbed candidate still fails the Chromium 67 baseline and remains `releaseEligible: false`.

### iOS packaged WebEmbed candidate: scheme preflight and integration boundary

A separate task-owned WKWebView application on iOS 26.5 loaded the same 43 protected files with the original SRI attributes and default cross-origin settings. Both the existing bundled `file:` configuration and a control granting read access only to the exact WebEmbed directory rejected all three initial scripts. Native WebKit logs identified access-control failures; the page remained empty and intrinsics were not frozen. Directory read access alone did not solve this failure.

The independent preflight then used only the public WebKit scheme-handler API with fixed `onekey-web-embed://bundle/index.html`, no access-control response header, and no file/universal cross-origin flags. The original assets rendered the real WebEmbed page, loaded its lazy API dispatcher, and executed the actual Kaspa WASM public-data fixture. The context reported secure-context status, working native random values and SHA-256, and frozen intrinsics. A separate 11.5 MB binary response matched its exact asset hash and MIME type. With original HTML/SRI unchanged, deliberately corrupting only the runtime response prevented execution and hardening; restoring that response restored the passing state. The independent test application's handshake stubs are not evidence of OneKey's complete native bridge or iOS Release acceptance.

The candidate-only first-party wiring now selects this fixed iOS document with the existing scoped `oneKeyWebEmbedAssets` prop. The native patch must authenticate the actual main frame, WebKit security origin, committed top-level navigation, and document URL again immediately before asynchronous message dispatch. JS additionally requires both the configured source URI and native event URL to match the original canonical document before its hash route; missing URLs, HTML/base-URL sources, credentials, ports, queries, encoded paths, traversal, and remote/file fallbacks cannot receive the packaged origin. JS then supplies the fixed origin explicitly instead of accepting a generic URL parser's opaque `null` result. Background adds only that exact origin on iOS candidate builds. Ordinary WebViews, normal builds, and existing OTA file handling retain their previous behavior.

The strict Release checker now requires `bridgeSource: 'bundled-scheme'` and `fileBridge: false` for the iOS candidate, while Android continues to require `bundled-https`. The expanded release/readiness suites passed 100 tests, including wrong-source rejection, platform-scoped permission negatives, and the two-operation Kaspa contract below. The signed iOS Release observations below cover the finished native patch, exact assets, both Hermes heaps, and the actual background-to-main-to-WebView bridge for source snapshot `ba2d98c65ce2`. Its first cold Kaspa operation failed. The current Chromium 67 baseline is unchanged and the candidate remains ineligible for release.

### Updated Kaspa candidate: browser and test evidence

The new 43-file protected WebEmbed candidate has manifest SHA-256 `bd1e47515692024f66a6f5b5e18d5442b68fa2622822b08eeea1a82a7376872b` and index SHA-256 prefix `3ce1aaa0db47`. All 43 file hashes, raw SES preservation, and script SRI were verified. Real Chrome checks passed over both HTTP and the existing file/OTA test configuration, with two routes per transport and two consecutive public commit/reveal/deserialize operations per route: eight complete unsigned reveal cycles. These operations exercise SDK paths that empty-transaction deserialization alone did not cover.

The controlled mobile Release diagnostics passed 100 unit tests. Their actual TypeScript validators also accepted raw and normalized public results from the unmodified pinned SDK, including its omitted normalized empty-signature field, and rejected signed or missing raw signatures and mismatched normalized data. The tests cover a stalled second reveal, the shared deadline, late replies, and independent reporting of intrinsic and transaction results. They do not replace execution through a native WebView.

This remains a modern-engine E2E candidate with `releaseEligible: false`; the unchanged Chromium 67 gate still reports 27 failures. Fresh iOS and Android Release builds must receive this exact candidate through the formal preparer, with new run IDs, consistent native/JS identities, and new receipts. Each must verify both independent Hermes heaps and the real background-to-main-to-WebView path, including `intrinsics: true`, `kaspaUnsigned: true`, and `kaspaRuns: 2`. The two Hermes heaps share one native process and may share native resources; the WebView is a separate JavaScript context. Neither these browser results nor the historical Android `be9323643a62` APK establish new native acceptance. The bounded iOS observations below do not establish repeatable cold-start acceptance.

### iOS Release snapshot ba2d98c65ce2: cold failure and subsequent two-operation pass

A standard signed Release union build used native and JS version `6.6.0`, build `1`, bundle version `21694949`, and run ID `2257be822564981fb2682de97803a277`. The first package was rejected before installation because the generated ServiceExtension plist still reported marketing version `1.0`. Repeating the normal incremental Xcode build with `MARKETING_VERSION=6.6.0 CURRENT_PROJECT_VERSION=1` corrected both app and extension identity; no packaged file was edited or re-signed manually. Guarded native input edits were restored to their original bytes.

The final executable SHA-256 was `7424be1b9fb20ecf69cafef68fbff81b705855f50bae48c7ac12b265699d8140`. Strict code-signature verification, the 2,627 expected Hermes artifact hashes, all 43 exact WebEmbed files, native/JS identity, and exclusion of development-shell artifacts passed before installation and again on the installed package. This comparison checked the expected files but did not reject additional bytecode files. This evidence is bound to source commit `ba2d98c65ce28169f33441ddae5d897b00cc4c9e` and candidate manifest `bd1e47515692`; it predates the subsequent base-branch merge and SDK 109 dependency update.

A later complete bytecode-magic inventory found 5,251 HBC files in that archived app: the 2,627 expected files plus 2,624 identical copies under nested `segments/segments` and `segments-background/segments-background` directories. Repeated `cp -R` operations into an existing destination caused the extra trees. The union build phase now replaces both product segment trees before copying, with a regression that executes the actual phase repeatedly and checks removal when the next build has no segments. The historical package does not pass the stronger complete-inventory requirement; the duplicate files have not been established as the cause of the cold Kaspa failure.

The first launch used a freshly installed application container on the isolated iOS 26.5 simulator. Main and background independently passed immutable state/bindings, frozen intrinsics and Promise, tamper rejection, async delivery, and the real native lazy-segment check. Main observed the actual provider listener before requesting mounting. Background then received the real WK bridge reply with `bridgeSource: 'bundled-scheme'`, `fileBridge: false`, and `intrinsics: true`. However, its first cold Kaspa call failed with `kaspaRuns: 0` and stage `kaspa-commit`; strict acceptance failed. That historical stage included both awaiting the commit RPC and validating its result. The approximately 31-second interval is consistent with the existing 30-second background-to-main RPC timeout, but the fixed report did not expose enough information to establish that cause. The first-install report and native log remain unchanged.

A later standard restart of the exact same installed package passed the archived strict checker, including all 2,627 host artifact hashes, both independent Hermes results, and the real WK bridge with `kaspaUnsigned: true` and `kaspaRuns: 2`. This restart retained the application container and may have reused WebKit compilation or application caches. It is a separate observation, not a replacement for the cold failure. A private console filter retained only fixed error categories and known asset names; no concrete Kaspa error was captured. System log access was unavailable, and application network/update warnings were retained, so neither run is reported as error-free. The normal onboarding screen rendered. Secure input automation could not establish its required accessible-field/focus guarantees; no passcode was generated or entered, and wallet creation and lock/unlock remain pending on iOS.

The E2E-only helper now separates request waits from result-validation stages. Failed reports include only a fixed failure category and local `elapsedMs`/`rpcElapsedMs` values bounded to 0–60,000 milliseconds. Only an exact own data-property match for the existing reverse-RPC timeout receives `rpc-timeout`; arbitrary error text, accessors, transaction contents, and native paths are never reported. Regression tests distinguish that timeout from the unchanged 60-second total deadline, result rejection, late completion, hostile error inspection, and clock changes. The release/readiness suites pass 113 tests, while the success report, two sequential unsigned operations, and first-call cold order remain unchanged. These improved diagnostics have not yet run in a new native package.

The E2E-only bridge trace additionally records fixed send/receive/result stages across background, main, and the WK page, with bounded elapsed times and per-session bridge/view identity comparisons. It recognizes only the public test fixture; callback IDs remain internal and arbitrary payloads are not logged. Instance wrappers preserve call and Promise identity, stop observing a replaced view, and clean up only their own listeners and methods. Ordinary Babel builds remove these paths entirely. The release/readiness/trace suites pass 147 tests; execution in a new native package remains pending.

Main and background remain separate Hermes heaps sharing one native process; the WK context and its caches are separate. These checks do not establish native-singleton concurrency behavior. The new source/dependency snapshot requires a new reviewed WebEmbed candidate, new native build receipts/run IDs, and fresh-container iOS and Android acceptance. Chromium 67 still fails 27 checks, and this modern-engine candidate remains `releaseEligible: false`.

## Native acceptance matrix

Complete this matrix before enabling the option by default or using it in a release. Use both enabled and disabled builds and record the exact native/JS versions.

| Platform | Build                      | Runtime coverage    | Required evidence                                                                                                  |
| -------- | -------------------------- | ------------------- | ------------------------------------------------------------------------------------------------------------------ |
| iOS      | Debug with vendor common   | main and background | Prior default-on loopback checks passed; rerun after the Promise tracker fix; historical disabled control recorded |
| Android  | Debug with vendor common   | main and background | Prior default-on loopback checks passed; rerun after the Promise tracker fix with explicit Promise freezing        |
| iOS      | Release union and segments | main and background | Snapshot ba2d98c65ce2 passed both heaps and the WK scheme bridge; first cold Kaspa call failed, same-container restart passed two operations; new source/dependencies and repeatable cold acceptance pending |
| Android  | Release union and segments | main and background | Historical 6.6.0 APK passed both heaps, HTTPS bridge, four WebView boundary tests, creation and lock/unlock; fresh Kaspa candidate and two-operation acceptance pending |
| Both     | OTA enabled to disabled    | main and background | Both runtimes restart; disabled bundle has no SES prelude and mutable intrinsics                                   |

For each enabled runtime, verify a strict-mode attempt to replace `Array.prototype.push` throws. Then exercise cold startup, unlock, account derivation, signing with test-only credentials, hardware-wallet transport in background, WalletConnect, and lazy chain/feature segment loading. Verify Promise/timer delivery and error reporting remain functional. Capture startup duration and JavaScript heap changes separately for each runtime; do not confuse one shared native allocation with two JavaScript copies.

The current default-on candidate is not yet accepted for rollout. Prior Android and iOS Debug loopback checks passed, but the Promise tracker correction requires fresh stronger Debug checks. The historical 6.6.0 Android Release passed intrinsic, async, segment, packaged HTTPS bridge, wallet creation and normal lock/unlock checks; retained application errors and untested flows remain separate limitations. Both platforms still need fresh Release execution for the merged source/dependencies and precise failure diagnostics. The archived iOS Release passed both Hermes heaps and the real WK bridge, but its first cold Kaspa failure is unresolved despite a later same-container two-operation pass; iOS creation/lock/unlock and the remaining application flows are pending. Native WebEmbed also requires both-platform update-path verification with raw SES and SRI preserved; the separate scheme preflight does not establish full OneKey bridge acceptance. Compiler fixtures, bytecode builds, and browser smoke do not imply those device checks passed.

## References

- [LavaMoat React Native integration and Babel requirements](https://github.com/LavaMoat/LavaMoat/tree/react-native-lockdown-v1.0.0/packages/react-native-lockdown)
- [Upstream serializer and its entry ID assumption](https://github.com/LavaMoat/LavaMoat/blob/react-native-lockdown-v1.0.0/packages/react-native-lockdown/src/index.js)
- [Official React Native repair options and Hermes Promise repair](https://github.com/LavaMoat/LavaMoat/blob/react-native-lockdown-v1.0.0/packages/react-native-lockdown/src/repair.js)
- [Endo vetted-shim model](https://github.com/endojs/endo/blob/master/packages/ses/docs/guide.md#using-hardenedjs-with-vetted-shims)
