{
  "name": "@onekeyhq/cli",
  "version": "0.1.0",
  "description": "OneKey wallet CLI for developers and AI agents",
  "bin": {
    "onekey": "./bin/onekey"
  },
  "main": "dist/cli.js",
  "files": [
    "dist/",
    "bin/"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "npx jest",
    "typecheck": "npx tsc --noEmit"
  },
  "dependencies": {
    "axios": "^1.7.0",
    "chalk": "^4.1.2",
    "commander": "^12.0.0",
    "yaml": "^2.8.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "tsup": "^8.0.0"
  }
}
