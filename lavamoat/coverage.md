# Runtime coverage and acceptance

This matrix describes the expanded integration while acceptance is in progress. It does not assert that every protected artifact has passed end-to-end tests or that existing release commands already select it. The target registry is [targets.cjs](../development/lavamoat/targets.cjs); mobile intrinsic lockdown is a separate integration without package policies.

Evidence below belongs to the identified artifacts. Earlier results do not validate the subsequent base-branch merge, SDK 109 update or later runtime changes. Current and historical artifacts are identified separately below.

| Target                          | Policy directory under `lavamoat/`                                             | Protected root build command                                                                                           |
| ------------------------------- | ------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------- |
| Web                             | `webpack/web`                                                                  | `yarn lavamoat:build:web`                                                                                              |
| Desktop renderer                | `webpack/desktop-renderer`                                                     | `yarn lavamoat:build:desktop-renderer`                                                                                 |
| Electron main                   | `webpack/desktop-main`                                                         | `yarn lavamoat:build:desktop-main`                                                                                     |
| Electron preload                | `webpack/desktop-preload`                                                      | `yarn lavamoat:build:desktop-preload`                                                                                  |
| Electron services               | `webpack/desktop-services/{index,enum,windowsHello,checkBiometricAuthChanged}` | `yarn lavamoat:build:desktop-services-<entry>`                                                                         |
| Chrome/Edge MV3 pages           | `webpack/ext/mv3/pages`                                                        | `yarn lavamoat:build:ext-pages`                                                                                        |
| Chrome/Edge MV3 background      | `webpack/ext/mv3/background`                                                   | `yarn lavamoat:build:ext-background`                                                                                   |
| Chrome/Edge MV3 content script  | `webpack/ext/mv3/content-script`                                               | `yarn lavamoat:build:ext-content-script`                                                                               |
| Embedded WebView                | `webpack/web-embed`                                                            | `yarn lavamoat:build:web-embed`                                                                                        |
| CLI                             | `webpack/cli`                                                                  | `yarn lavamoat:build:cli`                                                                                              |
| CLI esbuild source process      | `node/build-tools`                                                             | `yarn lavamoat:build:node-build`                                                                                       |
| iOS/Android main and background | None: official RN intrinsic lockdown                                           | Normal mobile builds select `ONEKEY_MOBILE_LOCKDOWN`; changing it requires rebuilding matching common/entry artifacts. |

Each corresponding `lavamoat:policy:<target>` command discovers permissions. Discovery does not approve capabilities. Overrides are manually reviewed inputs and cannot be written by the generated-policy update workflow. The four Electron service entries use separate policies so generating one cannot replace another's permissions. A first-party-only enum entry can legitimately produce an empty policy.

The three extension outputs must be combined with `yarn workspace @onekeyhq/ext lavamoat:finalize`. It rejects conflicting staged files and runs the existing extension output checks. The aggregate protected build already includes this step. The page compiler covers popup, expand-tab, side-panel, standalone-window, content-script iframe, passkey, and offscreen documents. Its entry and lazy scripts use SRI against the final bytes, including the untouched SES prelude and existing remote-code removals. Background and content-script entries embed one raw SES runtime in their fixed manifest filenames.

## Isolation boundaries

Web and the Desktop application UI/background execute in one JS runtime. Electron main, each preload world, and service processes are separate runtimes; a renderer result cannot prove their state. Native addons and declared external SDKs are policy-controlled host capabilities, not recursively sandboxed package graphs.

The blockie-avatar Web Worker and the Web service worker are separate heaps outside the application package policies. The [service worker](../apps/web/src/service-worker.js) and its Workbox dependencies retain authority over app caches and controlled requests; its existing asset-integrity and update checks remain required. Protected page startup does not prove that either worker is hardened.

Mobile main/background execute independently in separate Hermes heaps. The same native process can share MMKV, DB/file handles, and module singletons. Each heap needs its own hardening and asynchronous-delivery checks, and neither runtime's readiness proves the other's. Native-vs-JS compatibility remains independent of version-locked main/background bundles.

React Native libraries can create additional worklet Hermes runtimes outside those two entry points. This integration does not install lockdown in those runtimes. Worklet runtimes and native modules remain trusted dependencies; main/background results do not establish their protection.

MV3 extension pages, background, offscreen, and content-script worlds do not share JS heaps. LavaMoat supplies a virtual `window` alias even in a worker compartment, so shared platform detection checks for a DOM Document before classifying a browser page. A real MV3 regression verifies worker, page, and isolated-content roles with the actual detection module. Content-script protection must not freeze the DApp's MAIN-world intrinsics. The existing injected provider is copied unchanged into the host page and remains a separately reviewed SDK boundary; it is not included in the extension compartments. Existing CSP and host permissions remain enforced during tests.

The official RN adapter uses `unsafe-eval` for Hermes compatibility and provides intrinsic integrity, not per-package capability isolation. The official Node runner covers the original CLI esbuild entry and dependencies discovered from that source. Its esbuild native child process intentionally retains build authority. It does not protect arbitrary repository scripts, Jest, signing infrastructure, or every release task.

## Validation levels

| Validation                        | Checks                                                                                                                                                              | Limits                                                                                                                                 |
| --------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| Policy tooling and negative tests | Exact generated paths, manual override preservation, denial of unapproved imports/globals/builtins, raw SES integrity, hashes/SRI and runtime ordering              | Small fixtures do not execute the full wallet.                                                                                         |
| Web artifact smoke                | Fresh profiles, root/deep-link navigation, real controls, health request, frozen intrinsics and asynchronous delivery                                               | The CI health response is explicit; a separate live-health mode exists. No signing or existing user profile.                           |
| WebView artifact smoke            | Real route/dispatcher and lazy image/WASM operations with artifact integrity checks                                                                                 | Native handshake responses are an explicit fixture. Actual device transport and the minimum supported engine need separate acceptance. |
| Preload artifact smoke            | Exact preload bytes in sandbox/context-isolated Electron, frozen intrinsics, real contextBridge and IPC transport                                                   | Main-process IPC responses are explicit fixtures; full Desktop startup is separate.                                                    |
| Desktop application smoke         | Production main/preload paths, renderer startup, visible Preferences, frozen intrinsics, original navigation timing and captured failures                           | Separate diagnostic and normal startup modes use fresh profiles. Native authentication and hardware remain separate acceptance.        |
| CLI artifact smoke                | Production CLI entry, offline schema/commands, frozen intrinsics and absence of keyring loads                                                                       | Does not establish signed standalone installers, keyring operations or signing.                                                        |
| MV3 integration fixtures          | Actual extension installation/CSP, independent worlds, real bridge SDK and denied capabilities                                                                      | Full production pages/background and the copied provider must also be checked together.                                                |
| MV3 artifact smoke                | Unchanged package budgets, exact artifact snapshot, startup observation before lockdown, real page/offscreen lifecycle and readonly DApp transport                  | Passkey idle readiness is not WebAuthn authentication. No wallet signing or hardware transport.                                        |
| Mobile runtime harness            | Receipt/worktree/session/entry/fingerprint identity, both Hermes heaps, immutable state, prototype tamper rejection, Promise/timer/nextTick, retained LogBox errors | Debug observation does not establish Release segment loading, rollback, signing, or hardware transport.                                |

Runtime reports must record the exact artifact hash or native/session/vendor identity, collected errors, and any controlled host responses. A test fails on uncaught errors, unhandled rejections, relevant asset failures, or missing runtime identity. Do not clear logs, disable CSP, or downgrade SES to obtain acceptance.

## Recorded Web evidence

The SDK 109 normal `yarn workspace @onekeyhq/web build` command, with the root binary search path supplied by the usual root/CI entry, produced the protected artifact with index SHA-256 `8dbcad33e2fbbf78d77b703f50fef04572d4a71818969230a6e865c99f882b87`. It includes the protected Web translation optimization, verified existing-SES loader, corrected numeric package identifiers and initial-only crypto/network vendor groups. Its complete 2,442-file, 466,499,833-byte archive matches the output, all 26 recorded build inputs remain unchanged, and all archived hashes still match after smoke testing. Normalized raw policy and manual override bytes are identical to the preceding Web candidate; no permissions were added by the identifier or vendor changes.

The actual runtime maps 28,711 module entries to 5,310 distinct package owners without `undefined` owners or cross-package collisions. Its translated policy exactly matches the approved policy. All 1,051 source maps contain no compiled SES module, and the artifact contains exactly one untouched SES prelude. Final HTML SRI and identical `404.html` bytes were verified. Fresh Chromium contexts passed both `/` and `/wallet/` in live-health mode. The market and wallet-connect controls rendered, both actual HTTPS health requests returned 200, frozen intrinsics rejected mutation, and Promise/timer delivery worked. No captured exceptions, unhandled rejections, console errors, local asset failures or ignored external network failures occurred. These startup checks do not establish chart readiness or every wallet operation.

The compiler retained 112 warnings and two Tamagui temporary-file rename errors before its configuration build recovered. Normal finalizers completed without a Sentry upload plugin or mutation of emitted scripts; this local acceptance did not publish an artifact. An earlier workspace invocation without the root binary search path failed before compilation because `cross-env` was unavailable. No configured public market seed was available locally, so cold network timing is not comparable to the CI seed fixture.

The unchanged startup graph budget now fails one check: 7,357,050 initial script bytes against 6,291,456. The other checks pass: 2,876 initial modules against 3,300, 9,739,360 source bytes against 13,172,736, 1,712,335 gzip bytes against 2,097,152, 1,357,759 Brotli bytes against 1,638,400, and a 687,449-byte generated runtime module against 819,200. All nine initial scripts have source maps and no forbidden source is present. This reduces the preceding candidate's five failures to one. The cold-timing run remains blocked by its original graph prerequisite; passing browser startup does not replace this performance gate.

## Recorded WebView evidence

The current protected artifact has index SHA-256 `3ce1aaa0db4708f0299d6f8a53dc88069271307f5b4e4c99856d2e0b947fbc26` and a 43-file manifest with SHA-256 `bd1e47515692024f66a6f5b5e18d5442b68fa2622822b08eeea1a82a7376872b`. It passed fresh HTTP and local OTA-file browser checks for both root navigation and direct API routing. Each route completed two consecutive public, unsigned Kaspa commit/reveal/deserialization operations with identical results, in addition to image conversion and runtime integrity checks. The assertions require explicit empty signatures in the original safe JSON before accepting the SDK's normalized empty representation. No page errors, console errors, unhandled rejections or asset failures were observed, and all served file hashes still matched after testing. The native handshake was a fixture; device acceptance is separate.

The local-file test uses the existing OTA `allowFileAccessFromFileURLs` behavior. It does not prove loading from bundled Android/iOS assets, which have a different file-access configuration. The same artifact failed the Chromium 67 syntax gate with 27 violations in the unchanged upstream runtime, so this browser result is not release acceptance for the existing engine baseline.

The browser checks were repeated with public commit construction as the first Kaspa call in each fresh context, before empty-transaction deserialization could warm the SDK. Both HTTP and file transports again passed two unsigned rounds on both routes. The smoke runner accepts `--artifact <directory>` to verify an archived artifact independently of a changing build directory. These browser checks still use a native handshake fixture.

An earlier Android Release candidate, embedding index `89cbda297e20300bdc83d305c6e4391f51a304313c37e62cfd88f958ac894ab3`, passed the actual bundled transport on an isolated API 36 emulator with WebView 133. Its non-debuggable APK SHA-256 is `be9323643a628cc9f6edaf7851af30ea54daeb3b8243c6f23a6d59fdcb20ec12`, with native/JS version `6.6.0`, build `1`, and bundle `21685552`; all 2,628 Hermes files and 43 WebEmbed files matched the inspected build inputs. Main and background independently passed frozen Promise/intrinsics, immutable state, tamper rejection, asynchronous delivery and native lazy-segment checks. The background made the real WebEmbed test and runtime-security RPCs through the fixed APK HTTPS AssetLoader origin, with file bridging disabled. This loader is enabled only for the explicitly verified modern-engine test candidate. It does not change the default WebView rollout or establish iOS acceptance.

Four Android instrumentation tests passed against that unchanged Release APK. Real WebView observations verified parent-frame delivery, rejected both same-origin and opaque-origin iframe messages, denied legacy bridge spoofing and source/navigation escapes, and preserved an ordinary WebView's bridge without exposing APK resources. A response-corruption test retained the original HTML and SRI hash: Chromium rejected the modified script, its sentinel did not execute, and a clean control initialized lockdown. Actual JavaScript and binary fetches matched APK hashes and returned HTTP 200 with the expected MIME types. The test APK contained no native libraries; process mappings confirmed Hermes and React Native came from the Release target. Providers lacking `WEB_MESSAGE_LISTENER` remain covered only by unit tests on this modern emulator. The instrumentation's dispatch observer tests native guards; the application's actual RPC result is the separate check above.

That Android candidate passed normal onboarding, created a disposable wallet, and returned to the visible `Account #1` home screen with a `$0.00` balance after normal lock and PIN unlock. Its recovery phrase was not displayed or exported; the task profile was cleared after evidence collection. Retained logs include RevenueCat/Billing errors, caught inscription-protection configuration validation failures, ten native Yoga `display: block` diagnostics during the wallet flow, and SurfaceTexture/EGL errors during onboarding. No recognized fatal exception or unhandled rejection was observed in the captured intervals. An earlier `1.0.0` candidate was blocked by the forced-update dialog; the current version resolved that test identity mismatch without bypassing update checks. Import, signing, DApp/hardware transport, notifications and OTA rollback remain unverified. This exact APK precedes the later iOS scheme and Kaspa compatibility changes. The updated Release checker requires two real unsigned Kaspa operations; its 100 unit tests and actual-SDK result validation passed, but this older APK cannot satisfy the new acceptance record. These results establish the listed runtime properties and bounded UI flow, not error-free startup, final-source Android acceptance or release eligibility.

An isolated iOS 26.5 WKWebView preflight failed to execute the original anonymous-SRI scripts through bundled `file:` loading, both with index-only and exact-directory read access. Navigation completion did not establish application startup. A public `WKURLSchemeHandler` probe using `onekey-web-embed://bundle/index.html` subsequently loaded the unchanged artifact without file/universal-access flags or additional CORS headers. It rendered the index page, exercised the real lazy API and Kaspa deserialization, and verified secure-context WebCrypto and the original 11,524,998-byte binary's SHA-256. Corrupting only the runtime response caused a real SRI rejection and prevented lockdown initialization; clean controls passed before and after. The probe used a small explicit native handshake fixture. Production RNCWebView source/frame guards and full iOS Release acceptance are still in progress.

The installed production Apple helper and extracted RNCWebView state methods then passed 123 native assertions, including real main/iframe provenance, navigation rejection, SRI corruption, symlink rejection, canceled-task callbacks and Fabric reuse. Independent review found and fixed stale mode state after reuse and URL-parser acceptance of encoded hostnames or empty ports. The Impl, Fabric and Manager files compiled with the actual CocoaPods/Fabric flags. The [native runner](../apps/mobile/scripts/native-tests/web-embed-scheme/README.md) uses a disposable app; these results do not replace complete OneKey Release and bridge acceptance.

A complete iOS 26.5 Release application from commit `ba2d98c65ce28169f33441ddae5d897b00cc4c9e` was subsequently installed with the 43-file artifact above. Main and extension identities were `6.6.0`/build `1`; the signed arm64 executable SHA-256 was `7424be1b9fb20ecf69cafef68fbff81b705855f50bae48c7ac12b265699d8140`. The 2,627 expected Hermes files and all WebEmbed files matched the inspected inputs. A later full bytecode inventory found 2,624 additional identical files nested by repeated segment copies; this historical package does not pass the stronger complete-inventory requirement. The union copy phase now replaces its two product segment trees, and the next native package must be checked for unexpected files as well as expected hashes. Main and background independently passed frozen intrinsics, immutable state, tamper rejection, asynchronous delivery and real lazy-segment loading. The actual WKWebView bridge returned the bundled scheme origin and frozen-intrinsic state. The strict runtime check then failed at the first Kaspa commit stage with zero completed unsigned rounds, approximately 31 seconds after the runtime probes. This is a failed application acceptance result; browser and native-helper passes do not replace it. The original report did not distinguish an RPC failure from result validation, so the cause remains under investigation. Normal onboarding reached passcode creation, but reliable private input was unavailable and wallet creation/lock/unlock were not counted as passed.

A new SDK 109 iOS Release candidate from commit `62a75c430200b5565c8648cfed9798051bc27a76` now passes complete package inventory: all 2,627 expected Hermes files match, with no missing, extra or changed bytecode, and all 43 modern-engine WebEmbed assets match. The signed arm64 executable SHA-256 is `2e5937f64f0c2bdcbe4352f8af9c027dd3a73b047424d2b8bfa7f69a620f3e58`. App and extension identities are `6.6.0`/build `1`, bundle version `21705601`; both the archived and freshly installed package pass strict signature checks. The new application container still fails the first Kaspa operation with zero completed rounds. Main/background integrity and asynchronous delivery, and the real WK bridge/intrinsic checks, pass. Passive traces confirm that the WebView receives the commit request and discovers both lazy scripts, but only the smaller loader reports completion before the unchanged 30-second RPC timeout. The failure is classified as `rpc-timeout`; script delivery, completion and native lifecycle remain under investigation. This modern-engine candidate remains ineligible for release under the existing minimum-engine gate.

A subsequent diagnostic Release package, bundle `21711631`, has executable SHA-256 `df5842944bcfa03147eb128ec3b1e4a8374d805b38098d3f23be787e92cb1a34`. Its exact 2,627-file Hermes and 43-file WebEmbed inventories and signatures pass. One fresh-container run passed both Hermes runtimes, the actual WK bridge and two cold-first unsigned Kaspa rounds. Native diagnostics recorded an early Fabric WebView destruction followed by a replacement handler that fully delivered both scripts before the successful calls. A fresh installation of the archived untraced `21705601` package then failed again at the first RPC after 30,032 ms, with zero rounds. Mobile runtime sources and WebEmbed assets were identical between these candidates; native tracing and build identities differed. The successful traced run does not establish a fix or prove the cause of the untraced failure. Full iOS acceptance remains failed, and wallet creation and lock/unlock remain unverified. The bounded [native diagnostic harness](../apps/mobile/scripts/native-tests/web-embed-trace/README.md) passed 93 checks and nine disabled-state equivalence checks; its instrumentation is compiled out by default.

The revised diagnostic recorder defers formatting and system-log submission to one utility task scheduled at 65 seconds. Its fixed 256-slot publication buffer preserves the original resource callbacks and 90-second capture limit. The portable candidate passed 105 reader/identity checks, nine disabled-source equivalence checks, buffer/concurrency checks, ThreadSanitizer and 25 parser checks. Both real scheduling fixtures passed, including full-budget system-log submission completing at 68,249 ms. These are native command-line fixtures, not an application cold-start result. The dump samples a reservation prefix over an interval; it does not include later reservations or prove log persistence, application acceptance or a fix for the timeout. A fresh diagnostic Release and actual collector validation remain required.

## Recorded extension evidence

The SDK 109 normal `build:v3` pipeline, including the inherited Worker Fetch repair, passed unchanged final output checks: 160,490,676 total bytes against 166,000,000, 891 JavaScript files against 1,000, and a 35,898,028-byte background against 40,894,464. Its complete 1,124-file archive matches the output and all 11,220 recorded inputs remain unchanged. The background SHA-256 is `33ef345e348b42f4f226e0d80d9fb907102da2616ab3a6ecb46038d5bda8aefd`. Actual popup wallet-empty and expanded-page onboarding controls rendered; frozen intrinsics and no-eval checks passed. The earlier missing Worker resource, bridge-origin and native Fetch receiver failures no longer occurred. Passkey failed with `Not implemented, please call overrideMethods`, so the complete smoke failed before Offscreen and DApp acceptance. Compilation retained 16 warnings across the three compilers.

This archive predates the numeric-owner correction: its unpatched compiler combined permission-free package owners into an `undefined` compartment. Host hardening and visible UI do not establish package isolation. The corrected compiler, Passkey/Offscreen helper initialization and HTTP(S)-only endpoint trust checks pass targeted regressions. A native Offscreen fixture with actual unready atoms confirms that packaged fetch completes while HTTP trust checks still wait for configuration. These changes require a new production artifact build and complete smoke before replacing this failed acceptance record.

## Recorded Desktop evidence

The normal renderer and six main/preload/service build outputs passed separate production artifact checks. The renderer index SHA-256 is `da9ed8704de2789b2994c3c3b43db00d0d16945fd3acea9ad50c68e3fed21e34`; main is `5c3c86bcb1cd0b269a7dfa4b95275375c34cc69e217d23ee8a532294e693fe95` and preload is `1f6e89f7a2181de495fce117eb2f0cc699f5f2d865deab5fce099b17fef00cf2`. Fresh Electron profiles passed both diagnostic and normal startup timing, visible onboarding and Preferences, main/preload/renderer hardening, asynchronous delivery, final SRI and production security settings. No captured exceptions, unhandled rejections, console errors or asset failures occurred. Health requests reached Chromium with an explicit successful fixture response. Separate exact-preload IPC and utility-process checks passed; native authentication responses in the latter remain synthetic.

Renderer compilation retained 11 warnings and removed all source maps through the normal finalizers without Sentry upload. These artifact checks precede the Kaspa transaction-construction compatibility work below; they establish startup and the tested routes, not every chain operation.

## Outstanding rollout constraints

The protected web-embed runtime contains untouched upstream SES 1.15.0. Its optional chaining and nullish syntax cannot parse on the existing Chromium 67 minimum. The application compatibility gate still rejects it. The [upstream webpack security guidance](https://lavamoat.github.io/guides/webpack/#security) requires loading SES without bundling or transforming it. Transpiling SES or excluding its bytes from the compatibility gate would invalidate the intended acceptance; the supported-engine decision remains unresolved.

Expanded real-WASM tests exposed Kaspa transaction-construction failures that earlier deserialization checks did not exercise. The SDK dynamically reads `crypto.getRandomValues`, creates three fixed function bodies through `Function`, and dynamically reads `location.protocol` to recognize an extension realm. Missing the last permission makes a second offscreen transaction wait indefinitely for an animation frame.

The integration now grants only the SDK browser RNG method in four browser policies and the read-only protocol property in extension pages. A loader verifies the pinned package, generated source, embedded and binary WASM hashes before replacing the three exact non-crypto function bodies with static functions inside the existing package compartment. Unknown bodies and receivers fail closed; the ordinary-browser scheduling path requires an existing callable animation-frame API and does not install a global fallback. Extension pages additionally load the same verified binary from a private packaged URL, retaining CSP and asset-access restrictions. Real MV3 fixtures passed two consecutive unsigned transactions for both byte-loading paths in actual offscreen documents. Final application artifacts and native bridge regressions are still being validated; these fixtures and earlier startup checks do not establish every chain operation.

Full application and native Release acceptance is still being completed. The CLI normal build now emits its protected artifact at the existing `dist/cli.js` publishing path; `build:unprotected` is the explicit rollback command. Its five offline artifact checks passed with frozen intrinsics and no keyring loads. Web and Desktop production scripts now select protected builds in this branch. The current Web and Desktop artifacts passed the bounded startup checks above; artifacts incorporating the later Kaspa compatibility changes still need validation. Desktop OTA updates only the renderer; the six Node/Electron entries require a complete binary release. Extension production scripts select protected staging and finalization, and combined package budgets pass; full application acceptance remains pending. WebView default rollout remains unresolved. Final rollout must be based on exact protected artifacts, with explicit rollback commands and per-target evidence recorded here or in the linked mobile record.
