# @onekey/native-debug-bridge

AI-accessible debug bridge for OneKey iOS/Android.

See `docs/plans/2026-05-19-native-debug-bridge-design.md` for design,
`docs/plans/2026-05-19-native-debug-bridge-mvp.md` for MVP scope.

## Status

This is the **scaffold commit** — only `tsconfig`, `vitest.config`, and an empty
`src/index.ts` placeholder exist. The daemon (`odbd`), CLI (`odb`), and MCP
server (`odb-mcp`) bins + their scripts arrive in later commits per the MVP
plan (`docs/plans/2026-05-19-native-debug-bridge-mvp.md`).
