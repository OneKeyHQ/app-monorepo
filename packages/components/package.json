{
  "name": "@onekeyhq/components",
  "version": "0.0.1",
  "private": true,
  "main": "src/index.tsx",
  "dependencies": {
    "native-base": "^3.2.2",
    "react-native-jazzicon": "^0.1.2",
    "react-native-svg": "^12.1.1",
    "styled-components": "^5.3.3"
  },
  "devDependencies": {
    "@svgr/cli": "^5.5.0",
    "lodash": "^4.17.21",
    "prettier": "^2.4.1"
  },
  "scripts": {
    "build:icons": "rm -rf src/Icon/react && npx @svgr/cli --config-file .svgrrc.json -d src/Icon/react svg && node src/Icon/script.js",
    "lint": "tsc --noEmit && eslint src --ext .ts,.tsx",
    "clean": "rimraf node_modules"
  }
}
