// Shared animateOnly constants to avoid creating new arrays on every render.
export const ANIMATE_ONLY_OPACITY = ['opacity'] as string[];
export const ANIMATE_ONLY_TRANSFORM = ['transform'] as string[];
export const ANIMATE_ONLY_OPACITY_TRANSFORM = [
  'opacity',
  'transform',
] as string[];
export const ANIMATE_ONLY_WIDTH = ['width'] as string[];
export const ANIMATE_ONLY_BORDER_COLOR = ['borderColor'] as string[];
export const ANIMATE_ONLY_BACKGROUND_COLOR = ['backgroundColor'] as string[];
export const ANIMATE_ONLY_BG_BORDER_COLOR = [
  'backgroundColor',
  'borderColor',
] as string[];
