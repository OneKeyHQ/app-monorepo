import type { INativeSheetPresentationProps } from './types';

export const NATIVE_SHEET_PRESENTATION_SUPPORTED = false;

export function NativeSheetPresentation(_props: INativeSheetPresentationProps) {
  return null;
}
