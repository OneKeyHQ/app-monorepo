import type { ComponentProps, FC } from 'react';

import Box from '../Box';

export const DesktopDragZoneAbsoluteBar: FC<ComponentProps<typeof Box>> = (
  props,
) => <Box {...props} />;

export default DesktopDragZoneAbsoluteBar;
