import type { IBaseMenuOptions } from '../BaseMenu';

/**
 * useUpdateItem component is only needed for desktop client.
 */
const useUpdateItem: () => IBaseMenuOptions | null = () => null;
export default useUpdateItem;
