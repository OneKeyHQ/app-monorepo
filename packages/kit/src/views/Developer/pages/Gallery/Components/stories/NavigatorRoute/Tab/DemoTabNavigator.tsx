import { Stack, SizableText } from '@onekeyhq/components';

// Simple placeholder component to replace the deleted Tab demo
const DemoTabNavigator = () => (
  <Stack flex={1} justifyContent="center" alignItems="center">
    <SizableText>Tab Demo Removed - Use Tabs.Container Instead</SizableText>
  </Stack>
);

export default DemoTabNavigator;