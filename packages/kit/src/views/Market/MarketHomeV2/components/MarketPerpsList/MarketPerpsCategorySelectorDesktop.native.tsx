import type { IMarketPerpsCategorySelectorProps } from './MarketPerpsCategorySelector.types';

export function MarketPerpsCategorySelectorDesktop(
  _props: IMarketPerpsCategorySelectorProps,
) {
  // Native uses the mobile selector branch. This stub prevents Metro from
  // resolving desktop-only scroll/gradient dependencies for the native bundle.
  return null;
}
