// Shared by the normal-variant header and item rows so the token column can
// never drift out of alignment between the two. Sized for the 32px token icon
// plus its 10px gap with room for a longer ticker.
export const PORTFOLIO_TOKEN_COLUMN_WIDTH = 140;
