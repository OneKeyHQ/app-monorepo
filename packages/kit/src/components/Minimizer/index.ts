import { NativeModules } from 'react-native';

const { Minimizer } = NativeModules;

// export default {
//   exit: () => null,
//   goBack: () => null,
//   minimize: () => null,
// };

export default Minimizer;
