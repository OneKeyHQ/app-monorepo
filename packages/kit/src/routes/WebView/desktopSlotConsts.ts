// String value MUST stay in sync with the literal hard-coded inside
// packages/components/src/layouts/Navigation/Navigator/TabStackNavigator.tsx
// (kept duplicated because @onekeyhq/components cannot import from @onekeyhq/kit
// per CLAUDE.md import hierarchy).
export const WEB_VIEW_DESKTOP_SLOT_ID = 'web-view-desktop-slot';
