export enum EKeylessWalletEnableScene {
  Onboarding = 'onboarding',
  GetMnemonic = 'getMnemonic',
}
export enum EKeylessFinalizeAction {
  Create = 'create',
  Restore = 'restore',
  ResetPin = 'resetPin',
}
