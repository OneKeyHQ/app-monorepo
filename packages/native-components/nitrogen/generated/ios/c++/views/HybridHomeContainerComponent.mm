///
/// HybridHomeContainerComponent.mm
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2026 Marc Rousavy @ Margelo
///

#import "HybridHomeContainerComponent.hpp"
#import <memory>
#import <react/renderer/componentregistry/ComponentDescriptorProvider.h>
#import <React/RCTViewComponentView.h>
#import <React/RCTComponentViewFactory.h>
#import <React/UIView+ComponentViewProtocol.h>
#import <NitroModules/NitroDefines.hpp>
#import <UIKit/UIKit.h>

#import "HybridHomeContainerSpecSwift.hpp"
#import "OneKeyNativeComponents-Swift-Cxx-Umbrella.hpp"

using namespace facebook;
using namespace margelo::nitro::onekeynativecomponents;
using namespace margelo::nitro::onekeynativecomponents::views;

/**
 * Represents the React Native View holder for the Nitro "HomeContainer" HybridView.
 */
@interface HybridHomeContainerComponent: RCTViewComponentView
@end

@implementation HybridHomeContainerComponent {
  std::shared_ptr<HybridHomeContainerSpecSwift> _hybridView;
}

+ (void) load {
  [super load];
  [RCTComponentViewFactory.currentComponentViewFactory registerComponentViewClass:[HybridHomeContainerComponent class]];
}

+ (react::ComponentDescriptorProvider) componentDescriptorProvider {
  return react::concreteComponentDescriptorProvider<HybridHomeContainerComponentDescriptor>();
}

- (instancetype) init {
  if (self = [super init]) {
    std::shared_ptr<HybridHomeContainerSpec> hybridView = OneKeyNativeComponents::OneKeyNativeComponentsAutolinking::createHomeContainer();
    _hybridView = std::dynamic_pointer_cast<HybridHomeContainerSpecSwift>(hybridView);
    [self updateView];
  }
  return self;
}

- (void) updateView {
  // 1. Get Swift part
  OneKeyNativeComponents::HybridHomeContainerSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Get UIView*
  void* viewUnsafe = swiftPart.getView();
  UIView* view = (__bridge_transfer UIView*) viewUnsafe;

  // 3. Update RCTViewComponentView's [contentView]
  [self setContentView:view];
}

- (void) updateProps:(const std::shared_ptr<const react::Props>&)props
            oldProps:(const std::shared_ptr<const react::Props>&)oldProps {
  // 1. Downcast props
  const auto& newViewPropsConst = *std::static_pointer_cast<HybridHomeContainerProps const>(props);
  auto& newViewProps = const_cast<HybridHomeContainerProps&>(newViewPropsConst);
  OneKeyNativeComponents::HybridHomeContainerSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Update each prop individually
  swiftPart.beforeUpdate();

  // initialSnapshotJson: string
  if (newViewProps.initialSnapshotJson.isDirty) {
    swiftPart.setInitialSnapshotJson(newViewProps.initialSnapshotJson.value);
    newViewProps.initialSnapshotJson.isDirty = false;
  }
  // backgroundColor: string
  if (newViewProps.backgroundColor.isDirty) {
    swiftPart.setBackgroundColor(newViewProps.backgroundColor.value);
    newViewProps.backgroundColor.isDirty = false;
  }
  // debugOverlayEnabled: boolean
  if (newViewProps.debugOverlayEnabled.isDirty) {
    swiftPart.setDebugOverlayEnabled(newViewProps.debugOverlayEnabled.value);
    newViewProps.debugOverlayEnabled.isDirty = false;
  }
  // onAction: optional
  if (newViewProps.onAction.isDirty) {
    swiftPart.setOnAction(newViewProps.onAction.value);
    newViewProps.onAction.isDirty = false;
  }
  // onRefresh: optional
  if (newViewProps.onRefresh.isDirty) {
    swiftPart.setOnRefresh(newViewProps.onRefresh.value);
    newViewProps.onRefresh.isDirty = false;
  }
  // onVisibleTabChange: optional
  if (newViewProps.onVisibleTabChange.isDirty) {
    swiftPart.setOnVisibleTabChange(newViewProps.onVisibleTabChange.value);
    newViewProps.onVisibleTabChange.isDirty = false;
  }
  // onRenderError: optional
  if (newViewProps.onRenderError.isDirty) {
    swiftPart.setOnRenderError(newViewProps.onRenderError.value);
    newViewProps.onRenderError.isDirty = false;
  }
  // onIntent: optional
  if (newViewProps.onIntent.isDirty) {
    swiftPart.setOnIntent(newViewProps.onIntent.value);
    newViewProps.onIntent.isDirty = false;
  }
  // onTransportResult: optional
  if (newViewProps.onTransportResult.isDirty) {
    swiftPart.setOnTransportResult(newViewProps.onTransportResult.value);
    newViewProps.onTransportResult.isDirty = false;
  }

  swiftPart.afterUpdate();

  // 3. Update hybridRef if it changed
  if (newViewProps.hybridRef.isDirty) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = newViewProps.hybridRef.value;
    if (maybeFunc.has_value()) {
      maybeFunc.value()(_hybridView);
    }
    newViewProps.hybridRef.isDirty = false;
  }

  // 4. Continue in base class
  [super updateProps:props oldProps:oldProps];
}

@end
