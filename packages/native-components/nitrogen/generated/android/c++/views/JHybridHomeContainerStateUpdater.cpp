///
/// JHybridHomeContainerStateUpdater.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2026 Marc Rousavy @ Margelo
///

#include "JHybridHomeContainerStateUpdater.hpp"
#include "views/HybridHomeContainerComponent.hpp"
#include <NitroModules/NitroDefines.hpp>

namespace margelo::nitro::onekeynativecomponents::views {

using namespace facebook;
using ConcreteStateData = react::ConcreteState<HybridHomeContainerState>;

void JHybridHomeContainerStateUpdater::updateViewProps(jni::alias_ref<jni::JClass> /* class */,
                                           jni::alias_ref<JHybridHomeContainerSpec::javaobject> javaView,
                                           jni::alias_ref<JStateWrapper::javaobject> stateWrapperInterface) {
  JHybridHomeContainerSpec* view = javaView->cthis();

  // Get concrete StateWrapperImpl from passed StateWrapper interface object
  jobject rawStateWrapper = stateWrapperInterface.get();
  if (!stateWrapperInterface->isInstanceOf(react::StateWrapperImpl::javaClassStatic())) {
      throw std::runtime_error("StateWrapper is not a StateWrapperImpl");
  }
  auto stateWrapper = jni::alias_ref<react::StateWrapperImpl::javaobject>{
            static_cast<react::StateWrapperImpl::javaobject>(rawStateWrapper)};

  std::shared_ptr<const react::State> state = stateWrapper->cthis()->getState();
  auto concreteState = std::dynamic_pointer_cast<const ConcreteStateData>(state);
  const HybridHomeContainerState& data = concreteState->getData();
  const std::optional<HybridHomeContainerProps>& maybeProps = data.getProps();
  if (!maybeProps.has_value()) {
    // Props aren't set yet!
    throw std::runtime_error("HybridHomeContainerState's data doesn't contain any props!");
  }
  const HybridHomeContainerProps& props = maybeProps.value();
  if (props.initialSnapshotJson.isDirty) {
    view->setInitialSnapshotJson(props.initialSnapshotJson.value);
    // TODO: Set isDirty = false
  }
  if (props.backgroundColor.isDirty) {
    view->setBackgroundColor(props.backgroundColor.value);
    // TODO: Set isDirty = false
  }
  if (props.debugOverlayEnabled.isDirty) {
    view->setDebugOverlayEnabled(props.debugOverlayEnabled.value);
    // TODO: Set isDirty = false
  }
  if (props.onAction.isDirty) {
    view->setOnAction(props.onAction.value);
    // TODO: Set isDirty = false
  }
  if (props.onRefresh.isDirty) {
    view->setOnRefresh(props.onRefresh.value);
    // TODO: Set isDirty = false
  }
  if (props.onVisibleTabChange.isDirty) {
    view->setOnVisibleTabChange(props.onVisibleTabChange.value);
    // TODO: Set isDirty = false
  }
  if (props.onRenderError.isDirty) {
    view->setOnRenderError(props.onRenderError.value);
    // TODO: Set isDirty = false
  }

  // Update hybridRef if it changed
  if (props.hybridRef.isDirty) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = props.hybridRef.value;
    if (maybeFunc.has_value()) {
      std::shared_ptr<JHybridHomeContainerSpec> shared = javaView->cthis()->shared_cast<JHybridHomeContainerSpec>();
      maybeFunc.value()(shared);
    }
    // TODO: Set isDirty = false
  }
}

} // namespace margelo::nitro::onekeynativecomponents::views
