{
  "mcpServers": {
    "jira": {
      "command": "npx",
      "args": ["-y", "@mcp-devtools/jira"],
      "env": {
        "JIRA_URL": "${JIRA_URL}",
        "JIRA_API_MAIL": "${JIRA_API_MAIL}",
        "JIRA_API_KEY": "${JIRA_API_KEY}"
      }
    }
  }
}
