name: 03-react-native-unittest

on:
  schedule:
    - cron: '0 22 * * *'
  workflow_dispatch:

permissions:
  contents: read
  issues: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  YARN_ENABLE_GLOBAL_CACHE: true

jobs:
  harness-android:
    name: Harness Tests (Android)
    runs-on: ubuntu-24.04
    timeout-minutes: 90

    strategy:
      matrix:
        node-version: [24.x]

    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'yarn'
          registry-url: 'https://npm.pkg.github.com'
          check-latest: true
          scope: '@onekeyhq'

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Enable KVM
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm

      - name: Install Android SDK components
        run: |
          sdkmanager --install "platform-tools" "platforms;android-35" "system-images;android-35;google_apis;x86_64" "emulator"

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-${{ hashFiles('apps/mobile/android/**/*.gradle*', 'apps/mobile/android/gradle-wrapper.properties') }}
          restore-keys: |
            gradle-

      - name: Create AVD and detect path
        run: |
          echo "no" | avdmanager create avd -n test_avd -k "system-images;android-35;google_apis;x86_64" --force
          AVD_INI=$(find /home/runner -name "test_avd.ini" 2>/dev/null | head -1)
          if [ -z "$AVD_INI" ]; then
            echo "ERROR: test_avd.ini not found anywhere under /home/runner"
            exit 1
          fi
          AVD_DIR=$(dirname "$AVD_INI")
          echo "Found AVD ini at: $AVD_INI"
          echo "Setting ANDROID_AVD_HOME=$AVD_DIR"
          echo "ANDROID_AVD_HOME=$AVD_DIR" >> $GITHUB_ENV

      - name: Install Dependencies
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NODE_OPTIONS: '--max_old_space_size=4096'
        run: yarn install --immutable

      - name: Restore cached APK
        id: apk-cache
        uses: actions/cache/restore@v4
        with:
          path: |
            apps/mobile/android/app/build/outputs/apk/prod/debug/app-prod-debug.apk
            .apk-build-commit
          key: android-debug-apk-${{ github.sha }}
          restore-keys: |
            android-debug-apk-

      - name: Check if native rebuild needed
        id: native-check
        run: |
          if [ -f .apk-build-commit ] && [ -f apps/mobile/android/app/build/outputs/apk/prod/debug/app-prod-debug.apk ]; then
            CACHED_COMMIT=$(cat .apk-build-commit)
            echo "Cached APK built from commit: $CACHED_COMMIT"
            # Fetch the cached commit so git diff can compare
            git fetch --depth=1 origin "$CACHED_COMMIT" 2>/dev/null || true
            if ./development/scripts/check-native-changes.sh "$CACHED_COMMIT"; then
              echo "No native changes since $CACHED_COMMIT — reusing cached APK"
              echo "rebuild=false" >> "$GITHUB_OUTPUT"
            else
              echo "Native changes detected — rebuild required"
              echo "rebuild=true" >> "$GITHUB_OUTPUT"
            fi
          else
            echo "No cached APK found — build required"
            echo "rebuild=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Build Android Debug APK
        if: steps.native-check.outputs.rebuild == 'true'
        timeout-minutes: 80
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NODE_OPTIONS: '--max_old_space_size=4096'
          SENTRY_DISABLE_AUTO_UPLOAD: 'true'
        working-directory: apps/mobile/android
        run: ./gradlew assembleProdDebug -PreactNativeArchitectures=x86_64

      - name: Stop Gradle daemon to free memory
        if: steps.native-check.outputs.rebuild == 'true'
        working-directory: apps/mobile/android
        run: ./gradlew --stop

      - name: Save APK cache
        if: steps.native-check.outputs.rebuild == 'true'
        run: echo "${{ github.sha }}" > .apk-build-commit
      - name: Upload APK cache
        if: steps.native-check.outputs.rebuild == 'true'
        uses: actions/cache/save@v4
        with:
          path: |
            apps/mobile/android/app/build/outputs/apk/prod/debug/app-prod-debug.apk
            .apk-build-commit
          key: android-debug-apk-${{ github.sha }}

      - name: Configure swap memory
        run: |
          # The 16 GiB runner is overcommitted at peak: 6 GiB emulator RAM
          # (qemu RSS ~7-8 GiB) + Node/Metro --max-old-space-size=8192
          # (RSS up to ~9 GiB) + adb/logcat/runner agent + OS easily
          # exceeds physical memory. Without swap the kernel OOM-killer
          # kills the runner agent mid-test, surfacing as
          # "The hosted runner lost communication with the server".
          # Allocate extra swap on /mnt (the runner SSD has ~50 GiB free).
          sudo fallocate -l 10G /mnt/extra-swapfile
          sudo chmod 600 /mnt/extra-swapfile
          sudo mkswap /mnt/extra-swapfile
          sudo swapon /mnt/extra-swapfile
          echo "Memory after swap configuration:"
          free -h
          swapon --show

      - name: Start Android Emulator
        run: |
          $ANDROID_HOME/emulator/emulator -avd test_avd -no-window -no-audio -no-boot-anim -gpu swiftshader_indirect -no-snapshot-save -memory 6144 &
          adb wait-for-device
          SECONDS=0
          TIMEOUT=300
          boot_completed=""
          while [ "$boot_completed" != "1" ]; do
            if [ "$SECONDS" -ge "$TIMEOUT" ]; then
              echo "ERROR: Emulator boot timed out after ${TIMEOUT}s"
              exit 1
            fi
            sleep 5
            boot_completed=$(adb shell getprop sys.boot_completed 2>/dev/null | tr -d '\r')
            echo "Waiting for emulator to boot... (${SECONDS}s elapsed, sys.boot_completed=$boot_completed)"
          done
          echo "Emulator is ready"
          adb devices

      - name: Install APK on Emulator
        run: adb install apps/mobile/android/app/build/outputs/apk/prod/debug/app-prod-debug.apk

      - name: Start logcat capture
        run: adb logcat -v time > /tmp/logcat.txt 2>&1 &

      # Harness tests are split into 6 shards run in separate Node processes.
      # The harness keeps Metro in-process, and each test bundle creates a
      # delta-calculator with full module graph. Heavy UI/jotai tests can add
      # ~340 MB heap each; the 8 GiB V8 heap is exhausted after ~24 such
      # tests (3-shard split was insufficient because heavy tests cluster
      # alphabetically in shard 1/3). 6 shards keeps the worst-case shard at
      # ~12 heavy tests, peaking around 4 GiB — well under the heap limit.
      # The patched @react-native-harness/cli accepts jest's --shard=k/n.
      #
      # `continue-on-error` lets every shard run even if an earlier one fails,
      # so a single CI run surfaces failures from all shards at once instead
      # of one-per-iteration. The summary step at the end fails the job if
      # any shard's outcome is failure, preserving the red/green signal.
      - name: Run Harness Tests (Android, shard 1/6)
        id: shard1
        timeout-minutes: 20
        continue-on-error: true
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          HARNESS_ANDROID_AVD: test_avd
        working-directory: apps/mobile
        run: yarn harness:test:android --shard=1/6

      - name: Run Harness Tests (Android, shard 2/6)
        id: shard2
        timeout-minutes: 20
        continue-on-error: true
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          HARNESS_ANDROID_AVD: test_avd
        working-directory: apps/mobile
        run: yarn harness:test:android --shard=2/6

      - name: Run Harness Tests (Android, shard 3/6)
        id: shard3
        timeout-minutes: 20
        continue-on-error: true
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          HARNESS_ANDROID_AVD: test_avd
        working-directory: apps/mobile
        run: yarn harness:test:android --shard=3/6

      - name: Run Harness Tests (Android, shard 4/6)
        id: shard4
        timeout-minutes: 20
        continue-on-error: true
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          HARNESS_ANDROID_AVD: test_avd
        working-directory: apps/mobile
        run: yarn harness:test:android --shard=4/6

      - name: Run Harness Tests (Android, shard 5/6)
        id: shard5
        timeout-minutes: 20
        continue-on-error: true
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          HARNESS_ANDROID_AVD: test_avd
        working-directory: apps/mobile
        run: yarn harness:test:android --shard=5/6

      - name: Run Harness Tests (Android, shard 6/6)
        id: shard6
        timeout-minutes: 20
        continue-on-error: true
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          HARNESS_ANDROID_AVD: test_avd
        working-directory: apps/mobile
        run: yarn harness:test:android --shard=6/6

      - name: Aggregate shard outcomes
        run: |
          echo "Shard outcomes:"
          echo "  1/6: ${{ steps.shard1.outcome }}"
          echo "  2/6: ${{ steps.shard2.outcome }}"
          echo "  3/6: ${{ steps.shard3.outcome }}"
          echo "  4/6: ${{ steps.shard4.outcome }}"
          echo "  5/6: ${{ steps.shard5.outcome }}"
          echo "  6/6: ${{ steps.shard6.outcome }}"
          if [ "${{ steps.shard1.outcome }}" != "success" ] || \
             [ "${{ steps.shard2.outcome }}" != "success" ] || \
             [ "${{ steps.shard3.outcome }}" != "success" ] || \
             [ "${{ steps.shard4.outcome }}" != "success" ] || \
             [ "${{ steps.shard5.outcome }}" != "success" ] || \
             [ "${{ steps.shard6.outcome }}" != "success" ]; then
            echo "::error::One or more harness shards failed"
            exit 1
          fi
          echo "All shards passed"

      - name: Dump logcat on failure
        if: failure()
        run: |
          echo "=== Last 200 lines of logcat ==="
          tail -200 /tmp/logcat.txt || true
          echo ""
          echo "=== App-specific logs ==="
          grep -i -E "(onekey|ReactNative|ReactNativeJS|AndroidRuntime|FATAL|crash|WebSocket)" /tmp/logcat.txt | tail -100 || true

  notify-failure:
    name: Notify on Failure
    runs-on: ubuntu-24.04
    needs: harness-android
    if: failure() && github.event_name == 'schedule'
    steps:
      - uses: actions/checkout@v4

      - name: Create failure issue
        uses: actions/github-script@v7
        with:
          script: |
            const title = `Nightly RN unit tests failed on ${new Date().toISOString().split('T')[0]}`;
            const { data: issues } = await github.rest.issues.listForRepo({
              owner: context.repo.owner,
              repo: context.repo.repo,
              labels: 'rn-unittest-failure',
              state: 'open',
            });
            if (issues.length > 0) {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issues[0].number,
                body: `Still failing: [Run #${context.runNumber}](${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId})`,
              });
            } else {
              await github.rest.issues.create({
                owner: context.repo.owner,
                repo: context.repo.repo,
                title,
                body: `The nightly RN unit test run failed.\n\n[View workflow run](${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId})`,
                labels: ['rn-unittest-failure'],
              });
            }
