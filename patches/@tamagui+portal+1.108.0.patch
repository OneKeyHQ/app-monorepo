diff --git a/node_modules/@tamagui/portal/dist/cjs/Portal.js b/node_modules/@tamagui/portal/dist/cjs/Portal.js
index 0000000..1111111 100644
--- a/node_modules/@tamagui/portal/dist/cjs/Portal.js
+++ b/node_modules/@tamagui/portal/dist/cjs/Portal.js
@@ -26,11 +26,18 @@
 });
 module.exports = __toCommonJS(Portal_exports);
 var import_polyfill_dev = require("@tamagui/polyfill-dev"), import_constants = require("@tamagui/constants"), import_stacks = require("@tamagui/stacks"), React = __toESM(require("react")), import_react_dom = require("react-dom"), import_use_did_finish_ssr = require("@tamagui/use-did-finish-ssr"), import_jsx_runtime = require("react/jsx-runtime");
+const isIOSWebKit = typeof navigator !== "undefined" && (/iP(ad|hone|od)/.test(navigator.userAgent) || navigator.platform === "MacIntel" && navigator.maxTouchPoints > 1);
 const Portal = React.memo(
   ({ host = globalThis.document?.body, ...props }) => {
     if (import_constants.isServer)
       return null;
-    const didFinishSSR = (0, import_use_did_finish_ssr.useDidFinishSSR)(), contents = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
+    const didFinishSSR = (0, import_use_did_finish_ssr.useDidFinishSSR)();
+    if (isIOSWebKit) {
+      const hasContent = React.Children.toArray(props.children).some((c) => c !== null && c !== !1 && c !== void 0);
+      if (!hasContent)
+        return null;
+    }
+    const contents = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
       import_stacks.YStack,
       {
         contain: "strict",
diff --git a/node_modules/@tamagui/portal/dist/esm/Portal.js b/node_modules/@tamagui/portal/dist/esm/Portal.js
index 0000000..2222222 100644
--- a/node_modules/@tamagui/portal/dist/esm/Portal.js
+++ b/node_modules/@tamagui/portal/dist/esm/Portal.js
@@ -5,11 +5,18 @@
 import { createPortal } from "react-dom";
 import { useDidFinishSSR } from "@tamagui/use-did-finish-ssr";
 import { jsx } from "react/jsx-runtime";
+const isIOSWebKit = typeof navigator !== "undefined" && (/iP(ad|hone|od)/.test(navigator.userAgent) || navigator.platform === "MacIntel" && navigator.maxTouchPoints > 1);
 const Portal = React.memo(
   ({ host = globalThis.document?.body, ...props }) => {
     if (isServer)
       return null;
-    const didFinishSSR = useDidFinishSSR(), contents = /* @__PURE__ */ jsx(
+    const didFinishSSR = useDidFinishSSR();
+    if (isIOSWebKit) {
+      const hasContent = React.Children.toArray(props.children).some((c) => c !== null && c !== !1 && c !== void 0);
+      if (!hasContent)
+        return null;
+    }
+    const contents = /* @__PURE__ */ jsx(
       YStack,
       {
         contain: "strict",
diff --git a/node_modules/@tamagui/portal/src/Portal.tsx b/node_modules/@tamagui/portal/src/Portal.tsx
index 0000000..3333333 100644
--- a/node_modules/@tamagui/portal/src/Portal.tsx
+++ b/node_modules/@tamagui/portal/src/Portal.tsx
@@ -10,6 +10,19 @@
 
 // web only version
 
+// iOS 26+ Safari scans the visible viewport edges for `position: fixed`
+// full-viewport elements. If it finds any (even transparent, pointerEvents:none
+// portal hosts kept around by Dialog/Sheet/Popover/Toast components), Safari
+// switches its top/bottom chrome from translucent ("cover") into opaque
+// ("modal") mode, and stays there. Skip rendering the YStack host on iOS
+// WebKit when no children are present so Safari does not latch into modal
+// chrome after the first time a Sheet/Dialog/etc. is opened.
+const isIOSWebKit =
+  typeof navigator !== 'undefined' &&
+  (/iP(ad|hone|od)/.test(navigator.userAgent) ||
+    (navigator.platform === 'MacIntel' &&
+      (navigator as any).maxTouchPoints > 1))
+
 export const Portal = React.memo(
   ({ host = globalThis.document?.body, ...props }: PortalProps) => {
     if (isServer) {
@@ -18,6 +31,15 @@
 
     const didFinishSSR = useDidFinishSSR()
 
+    if (isIOSWebKit) {
+      const hasContent = React.Children.toArray(
+        (props as any).children,
+      ).some((c) => c !== null && c !== false && c !== undefined)
+      if (!hasContent) {
+        return null
+      }
+    }
+
     const contents = (
       <YStack
         contain="strict"
