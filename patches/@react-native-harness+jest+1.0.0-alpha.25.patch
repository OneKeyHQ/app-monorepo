diff --git a/node_modules/@react-native-harness/jest/dist/index.js b/node_modules/@react-native-harness/jest/dist/index.js
--- a/node_modules/@react-native-harness/jest/dist/index.js
+++ b/node_modules/@react-native-harness/jest/dist/index.js
@@ -6,12 +6,55 @@
 import { getErrorMessage } from './logs.js';
 import { DeviceNotRespondingError } from '@react-native-harness/bridge/server';
 import { NativeCrashError } from './errors.js';
+const DEFAULT_TEST_FILE_TIMEOUT_MS = 180000;
 class CancelRun extends Error {
     constructor(message) {
         super(message);
         this.name = 'CancelRun';
     }
 }
+class TestFileTimeoutError extends Error {
+    testPath;
+    timeoutMs;
+    constructor(testPath, timeoutMs) {
+        super(`Harness test file timed out after ${timeoutMs}ms: ${testPath}`);
+        this.testPath = testPath;
+        this.timeoutMs = timeoutMs;
+        this.name = 'TestFileTimeoutError';
+    }
+}
+class RestartTimeoutError extends Error {
+    testPath;
+    timeoutMs;
+    constructor(testPath, timeoutMs) {
+        super(`Harness app restart timed out after ${timeoutMs}ms before test file: ${testPath}`);
+        this.testPath = testPath;
+        this.timeoutMs = timeoutMs;
+        this.name = 'RestartTimeoutError';
+    }
+}
+const getTestFileTimeoutMs = () => {
+    const value = Number(process.env.HARNESS_TEST_FILE_TIMEOUT_MS);
+    return Number.isFinite(value) && value > 0
+        ? value
+        : DEFAULT_TEST_FILE_TIMEOUT_MS;
+};
+const withTimeout = (promise, createError) => {
+    const timeoutMs = getTestFileTimeoutMs();
+    let timeoutId;
+    const timeoutPromise = new Promise((_, reject) => {
+        timeoutId = setTimeout(() => {
+            reject(createError(timeoutMs));
+        }, timeoutMs);
+    });
+    return Promise.race([promise, timeoutPromise]).finally(() => {
+        if (timeoutId) {
+            clearTimeout(timeoutId);
+        }
+    });
+};
+const withTestFileTimeout = (promise, testPath) => withTimeout(promise, (timeoutMs) => new TestFileTimeoutError(testPath, timeoutMs));
+const withRestartTimeout = (promise, testPath) => withTimeout(promise, (timeoutMs) => new RestartTimeoutError(testPath, timeoutMs));
 export default class JestHarness {
     isSerial = true;
     #globalConfig;
@@ -51,22 +94,24 @@
             }
             if (harnessConfig.resetEnvironmentBetweenTestFiles &&
                 !isFirstTest) {
-                await harness.restart();
+                console.log(`[harness-jest] Restarting app before ${test.path}`);
+                await withRestartTimeout(harness.restart(), test.path);
             }
             isFirstTest = false;
             return onStart(test).then(async () => {
+                console.log(`[harness-jest] Running ${test.path}`);
                 if (!harnessConfig.detectNativeCrashes) {
-                    return runHarnessTestFile({
+                    return withTestFileTimeout(runHarnessTestFile({
                         testPath: test.path,
                         harness,
                         globalConfig: this.#globalConfig,
                         projectConfig: test.context.config,
-                    });
+                    }), test.path);
                 }
                 // Start crash monitoring
                 const crashPromise = harness.crashMonitor.startMonitoring(test.path);
                 try {
-                    const result = await Promise.race([
+                    const result = await withTestFileTimeout(Promise.race([
                         runHarnessTestFile({
                             testPath: test.path,
                             harness,
@@ -74,7 +119,7 @@
                             projectConfig: test.context.config,
                         }),
                         crashPromise,
-                    ]);
+                    ]), test.path);
                     return result;
                 }
                 finally {
@@ -94,6 +139,26 @@
                 return;
             }
             if (err instanceof DeviceNotRespondingError) {
+                onFailure(test, {
+                    message: err.message,
+                    stack: '',
+                });
+                return;
+            }
+            if (err instanceof TestFileTimeoutError) {
+                onFailure(test, {
+                    message: err.message,
+                    stack: '',
+                });
+                try {
+                    await withRestartTimeout(harness.restart(), test.path);
+                }
+                catch (restartError) {
+                    console.warn('[harness-jest] App restart after test timeout failed:', restartError);
+                }
+                return;
+            }
+            if (err instanceof RestartTimeoutError) {
                 onFailure(test, {
                     message: err.message,
                     stack: '',
diff --git a/node_modules/@react-native-harness/jest/dist/run.js b/node_modules/@react-native-harness/jest/dist/run.js
--- a/node_modules/@react-native-harness/jest/dist/run.js
+++ b/node_modules/@react-native-harness/jest/dist/run.js
@@ -1,4 +1,5 @@
 import path from 'node:path';
+import fs from 'node:fs';
 import { formatResultsErrors } from 'jest-message-util';
 import { toTestResult } from './toTestResult.js';
 // Helper function to flatten nested test suites into a flat array of tests with hierarchy
@@ -42,6 +43,111 @@
     }
     return { passes, failures, pending, todo };
 };
+// ---- Snapshot comparison helpers ----
+// Resolve the .snap.web file path for a given test file, matching
+// jest-expo's createPlatformResolver('web') logic.
+const resolveSnapshotPath = (testPath) => {
+    return testPath.replace('__tests__', '__tests__/__snapshots__') + '.snap.web';
+};
+// Load and parse a .snap.web file. Returns an object whose keys are
+// snapshot names and values are the serialized expected strings.
+const loadSnapshotFile = (snapshotPath) => {
+    const data = Object.create(null);
+    if (!fs.existsSync(snapshotPath)) return data;
+    try {
+        const contents = fs.readFileSync(snapshotPath, 'utf8');
+        const populate = new Function('exports', contents);
+        populate(data);
+    } catch (e) {
+        console.warn(`[harness-snapshot] Failed to load ${snapshotPath}:`, e);
+    }
+    return data;
+};
+// Serialize a value the same way Jest does (via pretty-format).
+// We lazily load jest-snapshot/build/utils to avoid import issues.
+let _serialize = null;
+const serializeValue = (value) => {
+    if (!_serialize) {
+        try {
+            const utils = require('jest-snapshot/build/utils');
+            _serialize = utils.serialize;
+        } catch {
+            // Fallback: quote strings, JSON.stringify others
+            _serialize = (v) => typeof v === 'string' ? `"${v}"` : JSON.stringify(v, null, 2);
+        }
+    }
+    return _serialize(value);
+};
+// Remove extra line breaks that addExtraLineBreaks() adds to multiline snapshots.
+const removeExtraLineBreaks = (s) =>
+    s.length > 2 && s.startsWith('\n') && s.endsWith('\n') ? s.slice(1, -1) : s;
+// Deep-sort object keys recursively for stable comparison.
+const deepSortKeys = (val) => {
+    if (Array.isArray(val)) return val.map(deepSortKeys);
+    if (val && typeof val === 'object') {
+        const sorted = {};
+        for (const k of Object.keys(val).sort()) {
+            sorted[k] = deepSortKeys(val[k]);
+        }
+        return sorted;
+    }
+    return val;
+};
+// Normalize a pretty-format/JSON string for comparison by sorting object keys
+// and removing trailing commas. This handles two sources of divergence:
+// 1. JSON bridge may reorder object keys (Hermes vs V8 property order)
+// 2. pretty-format adds trailing commas; JSON round-trip loses them
+const normalizeSnapshot = (s) => {
+    // Strip trailing commas before } or ] (with optional whitespace/newlines)
+    const noTrailing = s.replace(/,(\s*[}\]])/g, '$1');
+    try {
+        const parsed = JSON.parse(noTrailing);
+        return JSON.stringify(deepSortKeys(parsed), null, 2);
+    } catch {
+        return noTrailing;
+    }
+};
+// Compare device-collected snapshots against the .snap.web file.
+// Returns { matched, unmatched, added, failures[] }.
+const compareSnapshots = (testPath, deviceSnapshots) => {
+    const snapshotPath = resolveSnapshotPath(testPath);
+    const existingData = loadSnapshotFile(snapshotPath);
+    let matched = 0;
+    let unmatched = 0;
+    let added = 0;
+    const failures = []; // { testName, key, expected, received }
+    for (const snap of deviceSnapshots) {
+        const { key, received } = snap;
+        // Inline snapshots already matched on device — just count them
+        if (key === '__inline_matched__') {
+            matched++;
+            continue;
+        }
+        const serialized = serializeValue(received);
+        if (key in existingData) {
+            // Stored values may have extra line breaks for multiline content
+            const expected = removeExtraLineBreaks(existingData[key]);
+            if (serialized === expected) {
+                matched++;
+            } else {
+                // Try normalized comparison for objects where key order or
+                // trailing commas differ between device JSON and snap file
+                const normExpected = normalizeSnapshot(expected);
+                const normReceived = normalizeSnapshot(serialized);
+                if (normExpected === normReceived) {
+                    matched++;
+                } else {
+                    unmatched++;
+                    const testName = key.replace(/ \d+$/, '');
+                    failures.push({ testName, key, expected, received: serialized });
+                }
+            }
+        } else {
+            added++;
+        }
+    }
+    return { matched, unmatched, added, failures };
+};
 export const runHarnessTestFile = async ({ testPath, globalConfig, projectConfig, harness, }) => {
     const start = Date.now();
     const relativeTestPath = path.relative(globalConfig.rootDir, testPath);
@@ -57,6 +163,39 @@
     const end = Date.now();
     const allTests = flattenTests(results);
     const stats = calculateStats(allTests);
+    // ---- Host-side snapshot comparison ----
+    let snapshotStats = { matched: 0, unmatched: 0, added: 0 };
+    const deviceSnapshots = results.__snapshots__ || [];
+    if (deviceSnapshots.length > 0) {
+        const snapResult = compareSnapshots(testPath, deviceSnapshots);
+        snapshotStats = snapResult;
+        // Inject snapshot failures into the corresponding test results
+        if (snapResult.failures.length > 0) {
+            const failuresByTest = new Map();
+            for (const f of snapResult.failures) {
+                if (!failuresByTest.has(f.testName)) {
+                    failuresByTest.set(f.testName, []);
+                }
+                failuresByTest.get(f.testName).push(
+                    `Snapshot "${f.key}" mismatch:\n\n` +
+                    `Expected:\n${f.expected}\n\n` +
+                    `Received:\n${f.received}`
+                );
+            }
+            for (const t of allTests) {
+                const fullName = [...(t.ancestorTitles || []), t.name].join(' ');
+                if (failuresByTest.has(fullName)) {
+                    const messages = failuresByTest.get(fullName);
+                    if (t.status === 'passed') {
+                        t.status = 'failed';
+                        t.error = { message: messages.join('\n\n---\n\n') };
+                        stats.passes--;
+                        stats.failures++;
+                    }
+                }
+            }
+        }
+    }
     // Convert TestResult[] to the format expected by toTestResult
     const tests = allTests.map((test) => {
         const errorMessage = test.error?.message;
@@ -92,6 +231,7 @@
         tests,
         jestTestPath: testPath,
         coverage: results.coverage,
+        snapshotStats,
     });
     totalResults.failureMessage = formatResultsErrors(totalResults.testResults, projectConfig, globalConfig, testPath);
     return totalResults;
diff --git a/node_modules/@react-native-harness/jest/dist/toTestResult.js b/node_modules/@react-native-harness/jest/dist/toTestResult.js
--- a/node_modules/@react-native-harness/jest/dist/toTestResult.js
+++ b/node_modules/@react-native-harness/jest/dist/toTestResult.js
@@ -17,14 +17,14 @@
         setupFilesEnd: 0,
     };
 };
-const getSnapshot = () => {
+const getSnapshot = (snapshotStats) => {
     return {
-        added: 0,
+        added: snapshotStats?.added || 0,
         fileDeleted: false,
-        matched: 0,
+        matched: snapshotStats?.matched || 0,
         unchecked: 0,
         uncheckedKeys: [],
-        unmatched: 0,
+        unmatched: snapshotStats?.unmatched || 0,
         updated: 0,
     };
 };
@@ -45,7 +45,7 @@
     });
 };
 export const toTestResult = (options) => {
-    const { stats, skipped, errorMessage, jestTestPath, coverage } = options;
+    const { stats, skipped, errorMessage, jestTestPath, coverage, snapshotStats } = options;
     return {
         failureMessage: errorMessage,
         leaks: false,
@@ -56,7 +56,7 @@
         openHandles: [],
         perfStats: getPerfStats(options),
         skipped,
-        snapshot: getSnapshot(),
+        snapshot: getSnapshot(snapshotStats),
         testFilePath: jestTestPath,
         testResults: getTestResults(options),
         coverage,
