diff --git a/node_modules/metro/src/Assets.js b/node_modules/metro/src/Assets.js
index 2cdbbb9..f1f78d7 100644
--- a/node_modules/metro/src/Assets.js
+++ b/node_modules/metro/src/Assets.js
@@ -203,6 +203,8 @@ async function getAssetFiles(assetPath, platform = null) {
   const assetData = await getAbsoluteAssetRecord(assetPath, platform);
   return assetData.files;
 }
+
+const GET_TOP_DIR_SYMBOL = "relative_dir_symbol";
 async function getAsset(
   relativePath,
   projectRoot,
@@ -211,12 +213,19 @@ async function getAsset(
   assetExts,
   fileExistsInFileMap,
 ) {
+  if (relativePath.startsWith(GET_TOP_DIR_SYMBOL)) {
+    relativePath = relativePath.replaceAll(GET_TOP_DIR_SYMBOL, "..");
+  }
   const assetData = AssetPaths.parse(
     relativePath,
     new Set(platform != null ? [platform] : []),
   );
   const absolutePath = _path.default.resolve(projectRoot, relativePath);
   if (!assetExts.includes(assetData.type)) {
+    console.error(
+      `'${relativePath}' cannot be loaded as its extension is not registered in assetExts`,
+      absolutePath,
+    );
     throw new Error(
       `'${relativePath}' cannot be loaded as its extension is not registered in assetExts`,
     );
@@ -225,6 +234,10 @@ async function getAsset(
     fileExistsInFileMap == null &&
     !pathBelongsToRoots(absolutePath, [projectRoot, ...watchFolders])
   ) {
+    console.error(
+      `'${relativePath}' could not be found, because it cannot be found in the project root or any watch folder`,
+      absolutePath,
+    );
     throw new Error(
       `'${relativePath}' could not be found, because it cannot be found in the project root or any watch folder`,
     );
diff --git a/node_modules/metro/src/Server.js b/node_modules/metro/src/Server.js
index f8eb36c..e1066b1 100644
--- a/node_modules/metro/src/Server.js
+++ b/node_modules/metro/src/Server.js
@@ -939,6 +939,13 @@ class Server {
       );
       bundlePerfLogger.point("serializingBundle_end");
       const bundleCode = typeof bundle === "string" ? bundle : bundle.code;
+      // Release the dependency graph for modulesOnly bundles (e.g. harness
+      // test files) immediately after serialization.  These one-shot bundles
+      // are never requested again, so keeping their graph in memory only
+      // causes linear memory growth until OOM.
+      if (serializerOptions.modulesOnly) {
+        await this._bundler.endGraph(graphId);
+      }
       return {
         numModifiedFiles: delta.reset
           ? delta.added.size + revision.prepend.length
