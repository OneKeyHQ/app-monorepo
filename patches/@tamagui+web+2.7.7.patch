diff --git a/node_modules/@tamagui/web/dist/cjs/helpers/webPropsToSkip.native.js b/node_modules/@tamagui/web/dist/cjs/helpers/webPropsToSkip.native.js
index 421eb93..c6a975f 100644
--- a/node_modules/@tamagui/web/dist/cjs/helpers/webPropsToSkip.native.js
+++ b/node_modules/@tamagui/web/dist/cjs/helpers/webPropsToSkip.native.js
@@ -70,7 +70,8 @@ var webPropsToSkip = {
   onScroll: 1,
   onCopy: 1,
   onCut: 1,
-  onPaste: 1,
+  // OneKey patch: React Native TextInput emits this custom event on native.
+  // onPaste: 1,
   // Other web-only props
   htmlFor: 1,
   dangerouslySetInnerHTML: 1
diff --git a/node_modules/@tamagui/web/dist/esm/helpers/webPropsToSkip.native.js b/node_modules/@tamagui/web/dist/esm/helpers/webPropsToSkip.native.js
index a976632..d5f0121 100644
--- a/node_modules/@tamagui/web/dist/esm/helpers/webPropsToSkip.native.js
+++ b/node_modules/@tamagui/web/dist/esm/helpers/webPropsToSkip.native.js
@@ -41,7 +41,8 @@ var webPropsToSkip = {
   onScroll: 1,
   onCopy: 1,
   onCut: 1,
-  onPaste: 1,
+  // OneKey patch: React Native TextInput emits this custom event on native.
+  // onPaste: 1,
   // Other web-only props
   htmlFor: 1,
   dangerouslySetInnerHTML: 1
diff --git a/node_modules/@tamagui/web/src/helpers/webPropsToSkip.native.ts b/node_modules/@tamagui/web/src/helpers/webPropsToSkip.native.ts
index 08cc802..8f8c675 100644
--- a/node_modules/@tamagui/web/src/helpers/webPropsToSkip.native.ts
+++ b/node_modules/@tamagui/web/src/helpers/webPropsToSkip.native.ts
@@ -48,7 +48,8 @@ export const webPropsToSkip = {
   onScroll: 1,
   onCopy: 1,
   onCut: 1,
-  onPaste: 1,
+  // OneKey patch: React Native TextInput emits this custom event on native.
+  // onPaste: 1,
 
   // Other web-only props
   htmlFor: 1,
diff --git a/node_modules/@tamagui/web/dist/cjs/helpers/getCSSStylesAtomic.cjs b/node_modules/@tamagui/web/dist/cjs/helpers/getCSSStylesAtomic.cjs
index 2143962c26..53ed23ca80 100644
--- a/node_modules/@tamagui/web/dist/cjs/helpers/getCSSStylesAtomic.cjs
+++ b/node_modules/@tamagui/web/dist/cjs/helpers/getCSSStylesAtomic.cjs
@@ -168,7 +168,7 @@ function createAtomicRules(identifier, property, value, pseudo) {
   const cls = property in import_helpers.cssShorthandLonghands ? `.${identifier}.${identifier}` : `.${identifier}`;
   let selector = pseudo ? pseudoSelector ? `${pseudoSelector} ${cls}` : `${selectorPriority[pseudo.name]} ${cls}${pseudoIdPostfix}` : `:root ${cls}`;
   if (pseudoSelector === import_pseudoDescriptors.pseudoDescriptors.enterStyle.selector) {
-    selector = `${selector}, .${identifier}${pseudoSelector}`;
+    selector = `._dsp_contents${pseudoSelector} ${cls}, .${identifier}${pseudoSelector}`;
   }
   const important = !!pseudo;
   let rules = [];
diff --git a/node_modules/@tamagui/web/dist/esm/helpers/getCSSStylesAtomic.mjs b/node_modules/@tamagui/web/dist/esm/helpers/getCSSStylesAtomic.mjs
index 5b1d59b201..8070355c6d 100644
--- a/node_modules/@tamagui/web/dist/esm/helpers/getCSSStylesAtomic.mjs
+++ b/node_modules/@tamagui/web/dist/esm/helpers/getCSSStylesAtomic.mjs
@@ -139,7 +139,7 @@ function createAtomicRules(identifier, property, value, pseudo) {
   const cls = property in cssShorthandLonghands ? `.${identifier}.${identifier}` : `.${identifier}`;
   let selector = pseudo ? pseudoSelector ? `${pseudoSelector} ${cls}` : `${selectorPriority[pseudo.name]} ${cls}${pseudoIdPostfix}` : `:root ${cls}`;
   if (pseudoSelector === pseudoDescriptors.enterStyle.selector) {
-    selector = `${selector}, .${identifier}${pseudoSelector}`;
+    selector = `._dsp_contents${pseudoSelector} ${cls}, .${identifier}${pseudoSelector}`;
   }
   const important = !!pseudo;
   let rules = [];
diff --git a/node_modules/@tamagui/web/src/helpers/getCSSStylesAtomic.ts b/node_modules/@tamagui/web/src/helpers/getCSSStylesAtomic.ts
index eb1ff9921f..7f36832a00 100644
--- a/node_modules/@tamagui/web/src/helpers/getCSSStylesAtomic.ts
+++ b/node_modules/@tamagui/web/src/helpers/getCSSStylesAtomic.ts
@@ -190,6 +190,9 @@ function createAtomicRules(
-  // enter style on css driver needs both:
-  //   .t_unmounted .selector
-  //   .selector.t_unmounted
+  // `t_unmounted` is shared by TamaguiRoot and animated components. A broad
+  // descendant selector lets an entering ancestor such as Popover activate a
+  // nested child's enterStyle even when AnimatePresence uses initial={false}.
+  // This made the Web account panel slide from the right whenever it reopened.
+  // `_dsp_contents` identifies TamaguiRoot, preserving its hydration gate while
+  // component enter styles only match their own `t_unmounted` class.
   if (pseudoSelector === pseudoDescriptors.enterStyle.selector) {
-    selector = `${selector}, .${identifier}${pseudoSelector}`
+    selector = `._dsp_contents${pseudoSelector} ${cls}, .${identifier}${pseudoSelector}`
   }
