diff --git a/node_modules/electron-updater/out/AppUpdater.d.ts b/node_modules/electron-updater/out/AppUpdater.d.ts
index 449df36..3c8a76c 100644
--- a/node_modules/electron-updater/out/AppUpdater.d.ts
+++ b/node_modules/electron-updater/out/AppUpdater.d.ts
@@ -15,6 +15,7 @@ export type AppUpdaterEvents = {
     error: (error: Error, message?: string) => void;
     login: (info: AuthInfo, callback: LoginCallback) => void;
     "checking-for-update": () => void;
+    "update-download-fileInfo": (fileInfo: ResolvedUpdateFileInfo) => void;
     "update-not-available": (info: UpdateInfo) => void;
     "update-available": (info: UpdateInfo) => void;
     "update-downloaded": (event: UpdateDownloadedEvent) => void;
@@ -162,6 +163,7 @@ export declare abstract class AppUpdater extends AppUpdater_base {
      * @param options If you want to override configuration in the `app-update.yml`.
      */
     setFeedURL(options: PublishConfiguration | AllPublishOptions | string): void;
+    resetForRetry(): boolean;
     /**
      * Asks the server whether there is an update.
      * @returns null if the updater is disabled, otherwise info about the latest version
@@ -204,6 +206,7 @@ export declare abstract class AppUpdater extends AppUpdater_base {
     private getOrCreateDownloadHelper;
     protected executeDownload(taskOptions: DownloadExecutorTask): Promise<Array<string>>;
     protected differentialDownloadInstaller(fileInfo: ResolvedUpdateFileInfo, downloadUpdateOptions: DownloadUpdateOptions, installerPath: string, provider: Provider<any>, oldInstallerFileName: string): Promise<boolean>;
+    isInstallerPath(installerPath: string): boolean;
 }
 export interface DownloadUpdateOptions {
     readonly updateInfoAndProvider: UpdateInfoAndProvider;
diff --git a/node_modules/electron-updater/out/AppUpdater.js b/node_modules/electron-updater/out/AppUpdater.js
index d6d5deb..08e655d 100644
--- a/node_modules/electron-updater/out/AppUpdater.js
+++ b/node_modules/electron-updater/out/AppUpdater.js
@@ -246,6 +246,16 @@ class AppUpdater extends events_1.EventEmitter {
         }
         this.clientPromise = Promise.resolve(provider);
     }
+    resetForRetry() {
+        if (this.checkForUpdatesPromise != null || this.downloadPromise != null) {
+            this._logger.info("Skipping updater retry reset because an operation is in progress");
+            return false;
+        }
+        this.clientPromise = null;
+        this.stagingUserIdPromise = new lazy_val_1.Lazy(() => this.getOrCreateStagingUserId());
+        this.updateInfoAndProvider = null;
+        return true;
+    }
     /**
      * Asks the server whether there is an update.
      * @returns null if the updater is disabled, otherwise info about the latest version
@@ -268,6 +278,7 @@ class AppUpdater extends events_1.EventEmitter {
         })
             .catch((e) => {
             nullizePromise();
+            this.resetForRetry();
             this.emit("error", e, `Cannot check for updates: ${(e.stack || e).toString()}`);
             throw e;
         });
@@ -558,6 +569,8 @@ class AppUpdater extends events_1.EventEmitter {
     }
     async executeDownload(taskOptions) {
         const fileInfo = taskOptions.fileInfo;
+        // OneKey patch: expose the resolved update file metadata before download starts.
+        this.emit("update-download-fileInfo", fileInfo);
         const downloadOptions = {
             headers: taskOptions.downloadUpdateOptions.requestHeaders,
             cancellationToken: taskOptions.downloadUpdateOptions.cancellationToken,
@@ -640,6 +653,10 @@ class AppUpdater extends events_1.EventEmitter {
         log.info(`New version ${version} has been downloaded to ${updateFile}`);
         return await done(true);
     }
+    // OneKey patch: verify that the updater is bound to the package just verified by the app.
+    isInstallerPath(installerPath) {
+        return this.downloadedUpdateHelper?.file === installerPath;
+    }
     async differentialDownloadInstaller(fileInfo, downloadUpdateOptions, installerPath, provider, oldInstallerFileName) {
         try {
             if (this._testOnlyOptions != null && !this._testOnlyOptions.isUseDifferentialDownload) {
diff --git a/node_modules/electron-updater/out/BaseUpdater.js b/node_modules/electron-updater/out/BaseUpdater.js
index f7c2965..669a0f4 100644
--- a/node_modules/electron-updater/out/BaseUpdater.js
+++ b/node_modules/electron-updater/out/BaseUpdater.js
@@ -53,18 +53,12 @@ class BaseUpdater extends AppUpdater_1.AppUpdater {
         }
         // prevent calling several times
         this.quitAndInstallCalled = true;
-        try {
-            this._logger.info(`Install: isSilent: ${isSilent}, isForceRunAfter: ${isForceRunAfter}`);
-            return this.doInstall({
-                isSilent,
-                isForceRunAfter,
-                isAdminRightsRequired: downloadedFileInfo.isAdminRightsRequired,
-            });
-        }
-        catch (e) {
-            this.dispatchError(e);
-            return false;
-        }
+        this._logger.info(`Install: isSilent: ${isSilent}, isForceRunAfter: ${isForceRunAfter}`);
+        return this.doInstall({
+            isSilent,
+            isForceRunAfter,
+            isAdminRightsRequired: downloadedFileInfo.isAdminRightsRequired,
+        });
     }
     addQuitHandler() {
         if (this.quitHandlerAdded || !this.autoInstallOnAppQuit) {
diff --git a/node_modules/electron-updater/out/DownloadedUpdateHelper.d.ts b/node_modules/electron-updater/out/DownloadedUpdateHelper.d.ts
index 26ea883..d78d930 100644
--- a/node_modules/electron-updater/out/DownloadedUpdateHelper.d.ts
+++ b/node_modules/electron-updater/out/DownloadedUpdateHelper.d.ts
@@ -30,6 +30,7 @@ interface CachedUpdateInfo {
     fileName: string;
     sha512: string;
     readonly isAdminRightsRequired: boolean;
+    version: string;
 }
 export declare function createTempUpdateFile(name: string, cacheDir: string, log: Logger): Promise<string>;
 export {};
diff --git a/node_modules/electron-updater/out/DownloadedUpdateHelper.js b/node_modules/electron-updater/out/DownloadedUpdateHelper.js
index e6c33ea..20add74 100644
--- a/node_modules/electron-updater/out/DownloadedUpdateHelper.js
+++ b/node_modules/electron-updater/out/DownloadedUpdateHelper.js
@@ -46,6 +46,12 @@ class DownloadedUpdateHelper {
         if (cachedUpdateFile === null) {
             return null;
         }
+        if (this._downloadedFileInfo?.version !== updateInfo.version) {
+            logger.warn(`Cached update version doesn't match the latest available update. New update must be downloaded. Cached: ${this._downloadedFileInfo?.version ?? "none"}, expected: ${updateInfo.version}`);
+            await this.cleanCacheDirForPendingUpdate();
+            this._downloadedFileInfo = null;
+            return null;
+        }
         logger.info(`Update has already been downloaded to ${updateFile}).`);
         this._file = cachedUpdateFile;
         return cachedUpdateFile;
@@ -59,6 +65,7 @@ class DownloadedUpdateHelper {
             fileName: updateFileName,
             sha512: fileInfo.info.sha512,
             isAdminRightsRequired: fileInfo.info.isAdminRightsRequired === true,
+            version: versionInfo.version,
         };
         if (isSaveCache) {
             await (0, fs_extra_1.outputJson)(this.getUpdateInfoFile(), this._downloadedFileInfo);
@@ -93,7 +100,8 @@ class DownloadedUpdateHelper {
         }
         let cachedInfo;
         try {
-            cachedInfo = await (0, fs_extra_1.readJson)(updateInfoFilePath);
+            // OneKey patch: force text decoding because packaged Electron can return a Uint8Array.
+            cachedInfo = await (0, fs_extra_1.readJson)(updateInfoFilePath, { encoding: "utf8" });
         }
         catch (error) {
             let message = `No cached update info available`;
