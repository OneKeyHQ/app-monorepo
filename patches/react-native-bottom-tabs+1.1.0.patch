diff --git a/node_modules/react-native-bottom-tabs/android/src/main/java/com/rcttabview/RCTTabView.kt b/node_modules/react-native-bottom-tabs/android/src/main/java/com/rcttabview/RCTTabView.kt
index 23cd875..3fe0e1d 100644
--- a/node_modules/react-native-bottom-tabs/android/src/main/java/com/rcttabview/RCTTabView.kt
+++ b/node_modules/react-native-bottom-tabs/android/src/main/java/com/rcttabview/RCTTabView.kt
@@ -38,6 +38,12 @@ import com.google.android.material.navigation.NavigationBarView.LABEL_VISIBILITY
 import com.google.android.material.transition.platform.MaterialFadeThrough
 
 class ExtendedBottomNavigationView(context: Context) : BottomNavigationView(context) {
+  init {
+    // Disable automatic window insets handling to prevent extra padding
+    // when using 3-button navigation (navigation bar visible)
+    setOnApplyWindowInsetsListener(null)
+  }
+
   override fun getMaxItemCount(): Int {
     return 100
   }
@@ -227,6 +233,17 @@ class ReactBottomNavigationView(context: Context) : LinearLayout(context) {
     } else {
       bottomNavigation.visibility = VISIBLE
     }
+
+    // Force re-measure and notify JS about the new layout dimensions
+    post {
+      val dpWidth = Utils.convertPixelsToDp(context, layoutHolder.width)
+      val dpHeight = Utils.convertPixelsToDp(context, layoutHolder.height)
+      onNativeLayoutListener?.invoke(dpWidth, dpHeight)
+
+      // Update tab bar height measurement (0 when hidden)
+      val tabBarHeight = if (isHidden) 0 else Utils.convertPixelsToDp(context, bottomNavigation.height).toInt()
+      onTabBarMeasuredListener?.invoke(tabBarHeight)
+    }
   }
 
   fun updateItems(items: MutableList<TabInfo>) {
diff --git a/node_modules/react-native-bottom-tabs/ios/RepresentableView.swift b/node_modules/react-native-bottom-tabs/ios/RepresentableView.swift
index 09adaca..94e13d0 100644
--- a/node_modules/react-native-bottom-tabs/ios/RepresentableView.swift
+++ b/node_modules/react-native-bottom-tabs/ios/RepresentableView.swift
@@ -5,11 +5,12 @@ import SwiftUI
  Wraps each view with an additional wrapper to avoid directly managing React Native views.
  This solves issues where the layout would have weird artifacts..
  */
-struct RepresentableView: PlatformViewRepresentable {
-  var view: PlatformView
 
 #if os(macOS)
 
+struct RepresentableView: NSViewRepresentable {
+  var view: PlatformView
+
   func makeNSView(context: Context) -> PlatformView {
     let wrapper = NSView()
     wrapper.addSubview(view)
@@ -17,16 +18,32 @@ struct RepresentableView: PlatformViewRepresentable {
   }
 
   func updateNSView(_ nsView: PlatformView, context: Context) {}
+}
 
 #else
 
-  func makeUIView(context: Context) -> PlatformView {
-    let wrapper = UIView()
-    wrapper.addSubview(view)
-    return wrapper
+/// Uses UIViewControllerRepresentable to properly isolate the view controller hierarchy.
+/// This prevents UIViewControllerHierarchyInconsistency crashes on iOS 17.x where
+/// react-native-screens' RNSScreenStackView finds the wrong parent VC (UIKitTabBarController
+/// instead of the tab content's hosting controller).
+struct RepresentableView: UIViewControllerRepresentable {
+  var view: PlatformView
+
+  func makeUIViewController(context: Context) -> UIViewController {
+    let vc = UIViewController()
+    vc.view.backgroundColor = .clear
+    vc.view.addSubview(view)
+    view.translatesAutoresizingMaskIntoConstraints = false
+    NSLayoutConstraint.activate([
+      view.leadingAnchor.constraint(equalTo: vc.view.leadingAnchor),
+      view.trailingAnchor.constraint(equalTo: vc.view.trailingAnchor),
+      view.topAnchor.constraint(equalTo: vc.view.topAnchor),
+      view.bottomAnchor.constraint(equalTo: vc.view.bottomAnchor)
+    ])
+    return vc
   }
 
-  func updateUIView(_ uiView: PlatformView, context: Context) {}
+  func updateUIViewController(_ uiViewController: UIViewController, context: Context) {}
+}
 
 #endif
-}
diff --git a/node_modules/react-native-bottom-tabs/ios/TabView/NewTabView.swift b/node_modules/react-native-bottom-tabs/ios/TabView/NewTabView.swift
index d699315..53733de 100644
--- a/node_modules/react-native-bottom-tabs/ios/TabView/NewTabView.swift
+++ b/node_modules/react-native-bottom-tabs/ios/TabView/NewTabView.swift
@@ -32,7 +32,6 @@ struct NewTabView: AnyTabView {
               RepresentableView(view: child.view)
                 .ignoresSafeArea(.container, edges: .all)
                 .tabAppear(using: context)
-                .hideTabBar(props.tabBarHidden)
             } label: {
               TabItem(
                 title: tabData.title,
@@ -49,6 +48,7 @@ struct NewTabView: AnyTabView {
         }
       }
     }
+    .hideTabBar(props.tabBarHidden)
     .measureView { size in
       onLayout(size)
     }
diff --git a/node_modules/react-native-bottom-tabs/ios/TabViewImpl.swift b/node_modules/react-native-bottom-tabs/ios/TabViewImpl.swift
index 72938be..3a85477 100644
--- a/node_modules/react-native-bottom-tabs/ios/TabViewImpl.swift
+++ b/node_modules/react-native-bottom-tabs/ios/TabViewImpl.swift
@@ -3,9 +3,45 @@ import React
 import SwiftUI
 @_spi(Advanced) import SwiftUIIntrospect
 
+// MARK: - OneKey Native Logger Bridge (for TabViewImpl)
+private enum TabViewImplLogger {
+  static func log(_ message: String) {
+    let fullMessage = "[TabViewNative] \(message) (ts=\(CFAbsoluteTimeGetCurrent()), main=\(Thread.isMainThread))"
+    if let cls = NSClassFromString("ReactNativeNativeLogger.OneKeyLog") as? NSObject.Type {
+      cls.perform(NSSelectorFromString("info::"), with: "TabView", with: fullMessage)
+    }
+    NSLog("%@", fullMessage)
+  }
+}
+
+private func sceneSummary(_ window: UIWindow?) -> String {
+  guard let window else { return "scene=nil" }
+  if #available(iOS 13.0, *) {
+    guard let scene = window.windowScene else { return "scene=nil(windowHasNoScene)" }
+    let state: String
+    switch scene.activationState {
+    case .foregroundActive:
+      state = "foregroundActive"
+    case .foregroundInactive:
+      state = "foregroundInactive"
+    case .background:
+      state = "background"
+    case .unattached:
+      state = "unattached"
+    @unknown default:
+      state = "unknown"
+    }
+    let sceneAddress = String(describing: Unmanaged.passUnretained(scene).toOpaque())
+    return "scene=\(sceneAddress) state=\(state)"
+  }
+  return "scene=unavailable(<iOS13)"
+}
+
 /// SwiftUI implementation of TabView used to render React Native views.
 struct TabViewImpl: View {
   @ObservedObject var props: TabViewProps
+  @State private var lastTabControllerAddress: String = ""
+  @State private var lastTabBarAddress: String = ""
   #if os(macOS)
     @Weak var tabBar: NSTabView?
   #else
@@ -41,6 +77,7 @@ struct TabViewImpl: View {
   var onLongPress: (_ key: String) -> Void
   var onLayout: (_ size: CGSize) -> Void
   var onTabBarMeasured: (_ height: Int) -> Void
+  var onContainerReattached: (_ reason: String) -> Void
 
   var body: some View {
     tabContent
@@ -50,6 +87,8 @@ struct TabViewImpl: View {
           let item = props.filteredItems[safe: index]
           guard let key = item?.key else { return false }
 
+          TabViewImplLogger.log("USER TAP on tab index=\(index), key=\(key), isLongPress=\(isLongPress), currentSelected=\(props.selectedPage ?? "nil")")
+
           if isLongPress {
             onLongPress(key)
             emitHapticFeedback(longPress: true)
@@ -68,7 +107,31 @@ struct TabViewImpl: View {
         #if os(macOS)
           tabBar = tabController
         #else
+          let controllerAddress = String(describing: Unmanaged.passUnretained(tabController).toOpaque())
+          let tabBarAddress = String(describing: Unmanaged.passUnretained(tabController.tabBar).toOpaque())
+          let controllerScene = sceneSummary(tabController.view.window)
+          let tabBarScene = sceneSummary(tabController.tabBar.window)
+          TabViewImplLogger.log(
+            "introspectTabView: controller=\(controllerAddress), tabBar=\(tabBarAddress), controllerWindow=\(tabController.view.window != nil), tabBarWindow=\(tabController.tabBar.window != nil), controller\(controllerScene), tabBar\(tabBarScene), selectedPage=\(props.selectedPage ?? "nil"), tabCount=\(props.filteredItems.count)"
+          )
+          let didChangeController = !lastTabControllerAddress.isEmpty && lastTabControllerAddress != controllerAddress
+          let didChangeTabBar = !lastTabBarAddress.isEmpty && lastTabBarAddress != tabBarAddress
+          if didChangeController || didChangeTabBar {
+            TabViewImplLogger.log("introspectTabView detected recreated container: controllerChanged=\(didChangeController), tabBarChanged=\(didChangeTabBar)")
+            onContainerReattached("tab-controller-recreated")
+          }
+          if lastTabControllerAddress != controllerAddress || lastTabBarAddress != tabBarAddress {
+            DispatchQueue.main.async {
+              lastTabControllerAddress = controllerAddress
+              lastTabBarAddress = tabBarAddress
+            }
+          }
           tabBar = tabController.tabBar
+          // Immediately apply hidden state to the new tab bar instance.
+          // Critical on iOS 26 where SwiftUI may recreate the UITabBarController,
+          // making the old tabBar reference stale (window == nil).
+          tabController.tabBar.isHidden = props.tabBarHidden
+          tabController.tabBar.isUserInteractionEnabled = !props.tabBarHidden
           if !props.tabBarHidden {
             onTabBarMeasured(
               Int(tabController.tabBar.frame.size.height)
@@ -77,19 +140,24 @@ struct TabViewImpl: View {
         #endif
       }
       #if !os(macOS)
-        .configureAppearance(props: props, tabBar: tabBar)
+        .configureAppearance(
+          props: props,
+          tabBar: tabBar,
+          onContainerReattached: onContainerReattached
+        )
       #endif
       .tintColor(props.selectedActiveTintColor)
-      .getSidebarAdaptable(enabled: props.sidebarAdaptable ?? false)
+      .applyTabViewStyle(sidebarAdaptable: props.sidebarAdaptable ?? false)
       .onChange(of: props.selectedPage ?? "") { newValue in
-        #if !os(macOS)
-          if props.disablePageAnimations {
-            UIView.setAnimationsEnabled(false)
-            DispatchQueue.main.asyncAfter(deadline: .now() + 0.1) {
-              UIView.setAnimationsEnabled(true)
-            }
-          }
+        #if os(macOS)
+        TabViewImplLogger.log("SwiftUI onChange selectedPage=\(newValue), disablePageAnimations=\(props.disablePageAnimations)")
+        #else
+        TabViewImplLogger.log("SwiftUI onChange selectedPage=\(newValue), disablePageAnimations=\(props.disablePageAnimations), animationsEnabled=\(UIView.areAnimationsEnabled)")
         #endif
+        // NOTE: Animation disabling is now handled in TabViewProvider.selectedPage setter
+        // via SwiftUI Transaction instead of global UIView.setAnimationsEnabled.
+        // The old approach blocked ALL UIKit animations for 0.1s, including
+        // React Native screen push transitions triggered shortly after tab switch.
         #if os(tvOS) || os(macOS) || os(visionOS)
           onSelect(newValue)
         #endif
@@ -214,6 +282,40 @@ extension View {
     }
   }
 
+  @ViewBuilder
+  func applyTabViewStyle(sidebarAdaptable: Bool) -> some View {
+    if #available(iOS 18.0, macOS 15.0, tvOS 18.0, visionOS 2.0, *) {
+      #if compiler(>=6.0)
+      if sidebarAdaptable {
+        self.tabViewStyle(.sidebarAdaptable)
+      } else {
+        #if os(iOS)
+        // Force compact horizontal size class on iPad to keep TabBar at bottom
+        // iPhone is already compact by default, no modification needed
+        if UIDevice.current.userInterfaceIdiom == .pad {
+          self
+            .tabViewStyle(.tabBarOnly)
+            .environment(\.horizontalSizeClass, .compact)
+        } else {
+          self.tabViewStyle(.tabBarOnly)
+        }
+        #elseif os(macOS)
+        // Force compact horizontal size class on macOS to keep TabBar at bottom
+        self
+          .tabViewStyle(.tabBarOnly)
+          .environment(\.horizontalSizeClass, .compact)
+        #else
+        self.tabViewStyle(.tabBarOnly)
+        #endif
+      }
+      #else
+      self
+      #endif
+    } else {
+      self
+    }
+  }
+
   @ViewBuilder
   func tabBadge(_ data: String?) -> some View {
     if #available(iOS 15.0, macOS 15.0, visionOS 2.0, tvOS 15.0, *) {
@@ -233,7 +335,11 @@ extension View {
 
   #if !os(macOS)
     @ViewBuilder
-    func configureAppearance(props: TabViewProps, tabBar: UITabBar?) -> some View {
+    func configureAppearance(
+      props: TabViewProps,
+      tabBar: UITabBar?,
+      onContainerReattached: @escaping (_ reason: String) -> Void
+    ) -> some View {
       self
         .onChange(of: props.barTintColor) { _ in
           updateTabBarAppearance(props: props, tabBar: tabBar)
@@ -260,7 +366,35 @@ extension View {
           updateTabBarAppearance(props: props, tabBar: tabBar)
         }
         .onChange(of: props.tabBarHidden) { newValue in
+          var didUseFallbackTabBar = false
+          var fallbackCount = 0
           tabBar?.isHidden = newValue
+          tabBar?.isUserInteractionEnabled = !newValue
+          if #available(iOS 26.0, *) {
+            // On iOS 26, SwiftUI may recreate the UITabBarController making
+            // our tabBar reference stale. Find all UITabBars in the window
+            // and apply hidden state to any that our ref missed.
+            let keyWindow = UIApplication.shared.connectedScenes
+              .compactMap { $0 as? UIWindowScene }
+              .flatMap { $0.windows }
+              .first { $0.isKeyWindow }
+            if let window = keyWindow {
+              findAllTabBars(in: window) { foundTabBar in
+                if foundTabBar !== tabBar {
+                  foundTabBar.isHidden = newValue
+                  foundTabBar.isUserInteractionEnabled = !newValue
+                  didUseFallbackTabBar = true
+                  fallbackCount += 1
+                }
+              }
+            }
+          }
+          TabViewImplLogger.log(
+            "tabBarHidden changed: hidden=\(newValue), primaryTabBarWindow=\(tabBar?.window != nil), fallbackCount=\(fallbackCount), usedFallback=\(didUseFallbackTabBar), \(sceneSummary(tabBar?.window))"
+          )
+          if didUseFallbackTabBar {
+            onContainerReattached("fallback-findAllTabBars")
+          }
         }
     }
   #endif
@@ -299,14 +433,14 @@ extension View {
   @ViewBuilder
   func hideTabBar(_ flag: Bool) -> some View {
     #if !os(macOS)
-      if flag {
-        if #available(iOS 16.0, tvOS 16.0, *) {
-          self.toolbar(.hidden, for: .tabBar)
-        } else {
-          // We fallback to isHidden on UITabBar
-          self
-        }
+      if #available(iOS 16.0, tvOS 16.0, *) {
+        // Always apply .toolbar modifier with dynamic visibility to maintain stable
+        // SwiftUI view identity. Using if/else branches changes the View type,
+        // causing view tree recreation that on iOS 26 (Liquid Glass) can leave
+        // a ghost touch-intercepting layer.
+        self.toolbar(flag ? .hidden : .automatic, for: .tabBar)
       } else {
+        // iOS <16: rely on tabBar.isHidden set in configureAppearance onChange
         self
       }
     #else
@@ -326,3 +460,14 @@ extension View {
     }
   }
 }
+
+#if !os(macOS)
+private func findAllTabBars(in view: UIView, handler: (UITabBar) -> Void) {
+  if let tabBar = view as? UITabBar {
+    handler(tabBar)
+  }
+  for subview in view.subviews {
+    findAllTabBars(in: subview, handler: handler)
+  }
+}
+#endif
diff --git a/node_modules/react-native-bottom-tabs/ios/TabViewProvider.swift b/node_modules/react-native-bottom-tabs/ios/TabViewProvider.swift
index 013032f..778db67 100644
--- a/node_modules/react-native-bottom-tabs/ios/TabViewProvider.swift
+++ b/node_modules/react-native-bottom-tabs/ios/TabViewProvider.swift
@@ -2,6 +2,62 @@ import Foundation
 import React
 import SwiftUI
 
+private let kTabViewContainerDidReattachNotification =
+  Notification.Name("RNCTabViewContainerDidReattachNotification")
+
+// MARK: - OneKey Native Logger Bridge
+// Uses NSClassFromString to access @onekeyfe/react-native-native-logger without direct import
+private enum TabViewLogger {
+  static func log(_ message: String) {
+    let fullMessage = "[TabViewNative] \(message) (ts=\(CFAbsoluteTimeGetCurrent()), main=\(Thread.isMainThread))"
+    if let cls = NSClassFromString("ReactNativeNativeLogger.OneKeyLog") as? NSObject.Type {
+      cls.perform(NSSelectorFromString("info::"), with: "TabView", with: fullMessage)
+    }
+    NSLog("%@", fullMessage)
+  }
+}
+
+private func objectAddress(_ object: AnyObject?) -> String {
+  guard let object else { return "nil" }
+  return String(describing: Unmanaged.passUnretained(object).toOpaque())
+}
+
+private func sceneSummary(_ window: UIWindow?) -> String {
+  guard let window else { return "scene=nil" }
+  if #available(iOS 13.0, *) {
+    guard let scene = window.windowScene else { return "scene=nil(windowHasNoScene)" }
+    let state: String
+    switch scene.activationState {
+    case .foregroundActive:
+      state = "foregroundActive"
+    case .foregroundInactive:
+      state = "foregroundInactive"
+    case .background:
+      state = "background"
+    case .unattached:
+      state = "unattached"
+    @unknown default:
+      state = "unknown"
+    }
+    return "scene=\(objectAddress(scene)) state=\(state)"
+  }
+  return "scene=unavailable(<iOS13)"
+}
+
+#if !os(macOS)
+private final class WeakDisplayLinkProxy: NSObject {
+  weak var target: TabViewProvider?
+
+  init(target: TabViewProvider) {
+    self.target = target
+  }
+
+  @objc func onTick() {
+    target?.onReattachPumpTickFromDisplayLink()
+  }
+}
+#endif
+
 @objcMembers
 public final class TabInfo: NSObject {
   public let key: String
@@ -52,6 +108,14 @@ public final class TabInfo: NSObject {
   private var hostingController: PlatformHostingController<TabViewImpl>?
   private var coalescingKey: UInt16 = 0
   private var iconSize = CGSize(width: 27, height: 27)
+#if !os(macOS)
+  private var reattachDisplayLink: CADisplayLink?
+  private var reattachDisplayLinkProxy: WeakDisplayLinkProxy?
+  private var reattachPumpStartedAt: CFAbsoluteTime = 0
+  private var reattachPumpStartReason: String = ""
+  private var reattachPumpTickCount: Int = 0
+  private let reattachPumpTimeoutSeconds: CFAbsoluteTime = 1.5
+#endif
 
   @objc var onPageSelected: RCTDirectEventBlock?
 
@@ -85,7 +149,19 @@ public final class TabInfo: NSObject {
 
   @objc public var selectedPage: NSString? {
     didSet {
-      props.selectedPage = selectedPage as? String
+      TabViewLogger.log("selectedPage prop changed: \(oldValue ?? "nil") -> \(selectedPage ?? "nil"), disablePageAnimations=\(props.disablePageAnimations)")
+      if props.disablePageAnimations {
+        // Use SwiftUI Transaction to disable animation ONLY for this state update,
+        // instead of the global UIView.setAnimationsEnabled(false) which blocks
+        // all UIKit animations (including React Native screen push transitions).
+        var transaction = Transaction(animation: .none)
+        transaction.disablesAnimations = true
+        withTransaction(transaction) {
+          props.selectedPage = selectedPage as? String
+        }
+      } else {
+        props.selectedPage = selectedPage as? String
+      }
     }
   }
 
@@ -166,13 +242,23 @@ public final class TabInfo: NSObject {
     self.delegate = delegate
   }
 
+  deinit {
+#if !os(macOS)
+    stopReattachPump(reason: "deinit")
+#endif
+  }
+
   @objc public func setImageLoader(_ imageLoader: RCTImageLoader) {
     self.imageLoader = imageLoader
     loadIcons(icons)
   }
 
   override public func didUpdateReactSubviews() {
-    props.children = reactSubviews().map(IdentifiablePlatformView.init)
+    let reactSubviewsList = reactSubviews() ?? []
+    props.children = reactSubviewsList.map(IdentifiablePlatformView.init)
+    TabViewLogger.log(
+      "didUpdateReactSubviews: count=\(reactSubviewsList.count), providerWindow=\(window != nil), providerSuperview=\(superview != nil), \(sceneSummary(window))"
+    )
   }
 
 #if os(macOS)
@@ -181,18 +267,52 @@ public final class TabInfo: NSObject {
     setupView()
   }
 #else
+  override public func didMoveToWindow() {
+    super.didMoveToWindow()
+    TabViewLogger.log("didMoveToWindow: window=\(window != nil), hostingController=\(objectAddress(hostingController)), \(sceneSummary(window))")
+    if window != nil {
+      ensureHostingControllerAttached(reason: "didMoveToWindow")
+      stopReattachPump(reason: "didMoveToWindow-window-restored")
+    } else {
+      startReattachPumpIfNeeded(reason: "didMoveToWindow-window-nil")
+    }
+  }
+
+  override public func didMoveToSuperview() {
+    super.didMoveToSuperview()
+    TabViewLogger.log("didMoveToSuperview: superview=\(superview != nil), window=\(window != nil), hostingController=\(objectAddress(hostingController)), \(sceneSummary(window))")
+    if superview != nil {
+      ensureHostingControllerAttached(reason: "didMoveToSuperview")
+    }
+    if window == nil {
+      startReattachPumpIfNeeded(reason: "didMoveToSuperview-window-nil")
+    } else {
+      stopReattachPump(reason: "didMoveToSuperview-window-restored")
+    }
+  }
+
   override public func layoutSubviews() {
     super.layoutSubviews()
+    TabViewLogger.log("layoutSubviews: bounds=\(bounds), window=\(window != nil), hostingController=\(objectAddress(hostingController)), \(sceneSummary(window))")
     setupView()
+    ensureHostingControllerAttached(reason: "layoutSubviews")
+    if window == nil {
+      startReattachPumpIfNeeded(reason: "layoutSubviews-window-nil")
+    } else {
+      stopReattachPump(reason: "layoutSubviews-window-restored")
+    }
   }
 #endif
 
   private func setupView() {
     if self.hostingController != nil {
+      TabViewLogger.log("setupView: skipped because hostingController already exists (\(objectAddress(hostingController)))")
       return
     }
+    TabViewLogger.log("setupView: creating hostingController, providerWindow=\(window != nil), providerSuperview=\(superview != nil), \(sceneSummary(window))")
 
     self.hostingController = PlatformHostingController(rootView: TabViewImpl(props: props) { key in
+      TabViewLogger.log("onPageSelected fired: key=\(key)")
       self.delegate?.onPageSelected(key: key, reactTag: self.reactTag)
     } onLongPress: { key in
       self.delegate?.onLongPress(key: key, reactTag: self.reactTag)
@@ -200,9 +320,14 @@ public final class TabInfo: NSObject {
       self.delegate?.onLayout(size: size, reactTag: self.reactTag)
     } onTabBarMeasured: { height in
       self.delegate?.onTabBarMeasured(height: height, reactTag: self.reactTag)
+    } onContainerReattached: { reason in
+      self.notifyContainerReattached(reason: "tabViewImpl-\(reason)")
     })
 
     if let hostingController = self.hostingController, let parentViewController = reactViewController() {
+      TabViewLogger.log(
+        "setupView: attaching hostingController=\(objectAddress(hostingController)) to parentVC=\(NSStringFromClass(type(of: parentViewController))):\(objectAddress(parentViewController))"
+      )
       parentViewController.addChild(hostingController)
 #if !os(macOS)
       hostingController.view.backgroundColor = .clear
@@ -213,7 +338,128 @@ public final class TabInfo: NSObject {
 #if !os(macOS)
       hostingController.didMove(toParent: parentViewController)
 #endif
+      notifyContainerReattached(reason: "setupView-initial-attach")
+    } else {
+      TabViewLogger.log(
+        "setupView: skipped attach, hostingController=\(objectAddress(hostingController)), parentVC=\(objectAddress(reactViewController()))"
+      )
+#if !os(macOS)
+      startReattachPumpIfNeeded(reason: "setupView-parent-nil")
+#endif
+    }
+  }
+
+  private func ensureHostingControllerAttached(reason: String) {
+    guard let hostingController else { return }
+    guard let parentViewController = reactViewController() else {
+      TabViewLogger.log("ensureHostingControllerAttached(\(reason)): skipped because parentVC is nil")
+      return
+    }
+
+    var didReattach = false
+    TabViewLogger.log(
+      "ensureHostingControllerAttached(\(reason)): begin providerWindow=\(window != nil), providerSuperview=\(superview != nil), hostingController=\(objectAddress(hostingController)), hostingParent=\(objectAddress(hostingController.parent)), expectedParent=\(objectAddress(parentViewController)), hostingViewSuperview=\(objectAddress(hostingController.view.superview)), provider=\(objectAddress(self))"
+        + ", \(sceneSummary(window))"
+    )
+    if hostingController.parent !== parentViewController {
+      if hostingController.parent != nil {
+        hostingController.willMove(toParent: nil)
+        hostingController.removeFromParent()
+      }
+      parentViewController.addChild(hostingController)
+      TabViewLogger.log("ensureHostingControllerAttached(\(reason)): rebound parent VC")
+      didReattach = true
+    }
+
+    if hostingController.view.superview !== self {
+      if hostingController.view.superview != nil {
+        hostingController.view.removeFromSuperview()
+      }
+      addSubview(hostingController.view)
+      hostingController.view.translatesAutoresizingMaskIntoConstraints = false
+      hostingController.view.pinEdges(to: self)
+      TabViewLogger.log("ensureHostingControllerAttached(\(reason)): rebound hosting view to provider")
+      didReattach = true
+    }
+
+#if !os(macOS)
+    if didReattach {
+      hostingController.didMove(toParent: parentViewController)
+      notifyContainerReattached(reason: "ensureHostingControllerAttached-\(reason)")
+    } else {
+      TabViewLogger.log("ensureHostingControllerAttached(\(reason)): no-op, hierarchy already consistent")
+    }
+#endif
+  }
+
+#if !os(macOS)
+  private func startReattachPumpIfNeeded(reason: String) {
+    guard window == nil else {
+      stopReattachPump(reason: "startReattachPumpIfNeeded-window-restored")
+      return
+    }
+    guard reattachDisplayLink == nil else {
+      TabViewLogger.log(
+        "reattachPump already running: reason=\(reason), startReason=\(reattachPumpStartReason), tickCount=\(reattachPumpTickCount), providerWindow=\(window != nil), providerSuperview=\(superview != nil), \(sceneSummary(window))"
+      )
+      return
+    }
+    reattachPumpStartedAt = CFAbsoluteTimeGetCurrent()
+    reattachPumpStartReason = reason
+    reattachPumpTickCount = 0
+    let proxy = WeakDisplayLinkProxy(target: self)
+    let displayLink = CADisplayLink(target: proxy, selector: #selector(WeakDisplayLinkProxy.onTick))
+    displayLink.add(to: .main, forMode: .common)
+    reattachDisplayLinkProxy = proxy
+    reattachDisplayLink = displayLink
+    TabViewLogger.log(
+      "reattachPump start: reason=\(reason), timeout=\(reattachPumpTimeoutSeconds)s, provider=\(objectAddress(self)), hostingController=\(objectAddress(hostingController)), providerWindow=\(window != nil), providerSuperview=\(superview != nil), \(sceneSummary(window))"
+    )
+  }
+
+  private func stopReattachPump(reason: String) {
+    guard let reattachDisplayLink else { return }
+    reattachDisplayLink.invalidate()
+    self.reattachDisplayLink = nil
+    reattachDisplayLinkProxy = nil
+    let elapsed = CFAbsoluteTimeGetCurrent() - reattachPumpStartedAt
+    TabViewLogger.log(
+      "reattachPump stop: reason=\(reason), elapsed=\(String(format: "%.3f", elapsed))s, tickCount=\(reattachPumpTickCount), startReason=\(reattachPumpStartReason), provider=\(objectAddress(self)), hostingController=\(objectAddress(hostingController)), providerWindow=\(window != nil), providerSuperview=\(superview != nil), \(sceneSummary(window))"
+    )
+  }
+
+  @objc fileprivate func onReattachPumpTickFromDisplayLink() {
+    onReattachPumpTick()
+  }
+
+  private func onReattachPumpTick() {
+    guard reattachDisplayLink != nil else { return }
+    reattachPumpTickCount += 1
+    setupView()
+    ensureHostingControllerAttached(reason: "reattachPumpTick")
+    if window != nil {
+      stopReattachPump(reason: "tick-window-restored")
+      return
     }
+    let elapsed = CFAbsoluteTimeGetCurrent() - reattachPumpStartedAt
+    if elapsed >= reattachPumpTimeoutSeconds {
+      stopReattachPump(reason: "tick-timeout")
+    }
+  }
+#endif
+
+  private func notifyContainerReattached(reason: String) {
+    let parentVC = reactViewController()
+    let parentVCClass = parentVC.map { NSStringFromClass(type(of: $0)) } ?? "nil"
+    TabViewLogger.log(
+      "container reattached: reason=\(reason), providerWindow=\(window != nil), providerSuperview=\(superview != nil), parentVC=\(parentVCClass):\(objectAddress(parentVC))"
+        + ", \(sceneSummary(window))"
+    )
+    NotificationCenter.default.post(
+      name: kTabViewContainerDidReattachNotification,
+      object: self,
+      userInfo: ["reason": reason]
+    )
   }
 
   @objc(insertChild:atIndex:)
diff --git a/node_modules/react-native-bottom-tabs/lib/module/TabView.js b/node_modules/react-native-bottom-tabs/lib/module/TabView.js
index 3ab92c7..94a4f80 100644
--- a/node_modules/react-native-bottom-tabs/lib/module/TabView.js
+++ b/node_modules/react-native-bottom-tabs/lib/module/TabView.js
@@ -10,6 +10,7 @@ import NativeTabView from './TabViewNativeComponent';
 import useLatestCallback from 'use-latest-callback';
 import DelayedFreeze from "./DelayedFreeze.js";
 import { BottomAccessoryView } from "./BottomAccessoryView.js";
+import { NativeLogger } from '@onekeyfe/react-native-native-logger';
 import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
 const isAppleSymbol = icon => icon?.sfSymbol;
 const ANDROID_MAX_TABS = 100;
@@ -72,6 +73,11 @@ const TabView = ({
 }) => {
   // @ts-ignore
   const focusedKey = navigationState.routes[navigationState.index].key;
+  const prevFocusedKeyRef = React.useRef(focusedKey);
+  if (prevFocusedKeyRef.current !== focusedKey) {
+    NativeLogger.write(1, `[TabView JS] focusedKey CHANGED: ${prevFocusedKeyRef.current} -> ${focusedKey}, navIndex=${navigationState.index}, ts=${Date.now()}`);
+    prevFocusedKeyRef.current = focusedKey;
+  }
   const customTabBarWrapperRef = useRef(null);
   const [tabBarHeight, setTabBarHeight] = React.useState(0);
   const [measuredDimensions, setMeasuredDimensions] = React.useState({
@@ -160,6 +166,7 @@ const TabView = ({
       key
     }
   }) => {
+    NativeLogger.write(1, `[TabView JS] handlePageSelected from native: key=${key}, currentFocusedKey=${prevFocusedKeyRef.current}, ts=${Date.now()}`);
     jumpTo(key);
   }, [jumpTo]);
   const handleTabBarMeasured = React.useCallback(({
@@ -199,7 +206,7 @@ const TabView = ({
       ,
       icons: renderCustomTabBar ? undefined : resolvedIconAssets,
       selectedPage: focusedKey,
-      tabBarHidden: !!renderCustomTabBar,
+      tabBarHidden: props.tabBarHidden ?? !!renderCustomTabBar,
       onTabLongPress: handleTabLongPress,
       onPageSelected: handlePageSelected,
       onTabBarMeasured: handleTabBarMeasured,
diff --git a/node_modules/react-native-bottom-tabs/lib/typescript/src/TabView.d.ts b/node_modules/react-native-bottom-tabs/lib/typescript/src/TabView.d.ts
index 6695dad..d3cfd14 100644
--- a/node_modules/react-native-bottom-tabs/lib/typescript/src/TabView.d.ts
+++ b/node_modules/react-native-bottom-tabs/lib/typescript/src/TabView.d.ts
@@ -182,6 +182,10 @@ interface Props<Route extends BaseRoute> {
      * @platform ios
      */
     renderBottomAccessoryView?: BottomAccessoryViewProps['renderBottomAccessoryView'];
+    /**
+     * Whether the tab bar is hidden.
+     */
+    tabBarHidden?: boolean;
 }
 declare const TabView: <Route extends BaseRoute>({ navigationState, renderScene, onIndexChange, onTabLongPress, rippleColor, tabBarActiveTintColor: activeTintColor, tabBarInactiveTintColor: inactiveTintColor, getBadge, getBadgeBackgroundColor, getBadgeTextColor, getLazy, getLabelText, getIcon, getHidden, getActiveTintColor, getTestID, getRole, getSceneStyle, getPreventsDefault, hapticFeedbackEnabled, labeled, getFreezeOnBlur, tabBar: renderCustomTabBar, tabBarStyle, tabLabelStyle, renderBottomAccessoryView, ...props }: Props<Route>) => import("react/jsx-runtime").JSX.Element;
 export default TabView;
diff --git a/node_modules/react-native-bottom-tabs/src/TabView.tsx b/node_modules/react-native-bottom-tabs/src/TabView.tsx
index 41b9cb3..467cdb6 100644
--- a/node_modules/react-native-bottom-tabs/src/TabView.tsx
+++ b/node_modules/react-native-bottom-tabs/src/TabView.tsx
@@ -28,6 +28,7 @@ import {
   BottomAccessoryView,
   type BottomAccessoryViewProps,
 } from './BottomAccessoryView';
+import { NativeLogger } from '@onekeyfe/react-native-native-logger';
 
 const isAppleSymbol = (icon: any): icon is { sfSymbol: string } =>
   icon?.sfSymbol;
@@ -201,6 +202,10 @@ interface Props<Route extends BaseRoute> {
    * @platform ios
    */
   renderBottomAccessoryView?: BottomAccessoryViewProps['renderBottomAccessoryView'];
+  /**
+   * Whether the tab bar is hidden.
+   */
+  tabBarHidden?: boolean;
 }
 
 const ANDROID_MAX_TABS = 100;
@@ -243,6 +248,11 @@ const TabView = <Route extends BaseRoute>({
 }: Props<Route>) => {
   // @ts-ignore
   const focusedKey = navigationState.routes[navigationState.index].key;
+  const prevFocusedKeyRef = React.useRef(focusedKey);
+  if (prevFocusedKeyRef.current !== focusedKey) {
+    NativeLogger.write(1, `[TabView JS] focusedKey CHANGED: ${prevFocusedKeyRef.current} -> ${focusedKey}, navIndex=${navigationState.index}, ts=${Date.now()}`);
+    prevFocusedKeyRef.current = focusedKey;
+  }
   const customTabBarWrapperRef = useRef<View>(null);
   const [tabBarHeight, setTabBarHeight] = React.useState<number | undefined>(0);
   const [measuredDimensions, setMeasuredDimensions] = React.useState<
@@ -354,6 +364,7 @@ const TabView = <Route extends BaseRoute>({
 
   const handlePageSelected = React.useCallback(
     ({ nativeEvent: { key } }: { nativeEvent: OnPageSelectedEventData }) => {
+      NativeLogger.write(1, `[TabView JS] handlePageSelected from native: key=${key}, currentFocusedKey=${prevFocusedKeyRef.current}, ts=${Date.now()}`);
       jumpTo(key);
     },
     [jumpTo]
@@ -392,7 +403,7 @@ const TabView = <Route extends BaseRoute>({
         // When rendering a custom tab bar, icons can be React elements, which will not be properly resolved.
         icons={renderCustomTabBar ? undefined : resolvedIconAssets}
         selectedPage={focusedKey}
-        tabBarHidden={!!renderCustomTabBar}
+        tabBarHidden={props.tabBarHidden ?? !!renderCustomTabBar}
         onTabLongPress={handleTabLongPress}
         onPageSelected={handlePageSelected}
         onTabBarMeasured={handleTabBarMeasured}
