diff --git a/node_modules/@tamagui/react-native-media-driver/dist/cjs/mediaQueryList.native.js b/node_modules/@tamagui/react-native-media-driver/dist/cjs/mediaQueryList.native.js
index ea68667..d56cd5c 100644
--- a/node_modules/@tamagui/react-native-media-driver/dist/cjs/mediaQueryList.native.js
+++ b/node_modules/@tamagui/react-native-media-driver/dist/cjs/mediaQueryList.native.js
@@ -27,7 +27,10 @@ __export(mediaQueryList_exports, {
   NativeMediaQueryList: () => NativeMediaQueryList
 });
 module.exports = __toCommonJS(mediaQueryList_exports);
+var ExpoDevice = require("expo-device");
+var ScreenOrientation = require("expo-screen-orientation");
 var import_react_native = require("react-native");
+var import_react_native_device_utils = require("@onekeyfe/react-native-device-utils");
 var import_matchQuery = require("./matchQuery.native.js");
 function _class_call_check(instance, Constructor) {
   if (!(instance instanceof Constructor)) {
@@ -61,6 +64,39 @@ function _define_property(obj, key, value) {
   }
   return obj;
 }
+// OneKey patch: Full-screen iPad surfaces can refresh unclamped media queries.
+var nativeMediaControl = globalThis.$$onekeyNativeMedia ||
+  (globalThis.$$onekeyNativeMedia = { useRealWidth: false, isTabletLike: false });
+var mediaUpdaters = new Set();
+nativeMediaControl.refresh = function () {
+  mediaUpdaters.forEach(function (update) { update(); });
+};
+var MAX_OVERRIDE_WIDTH = 767;
+var isDualScreen;
+function isDualScreenDevice() {
+  if (isDualScreen === void 0) {
+    isDualScreen = import_react_native_device_utils.ReactNativeDeviceUtils.isDualScreenDevice();
+  }
+  return isDualScreen;
+}
+function getMediaDimensions() {
+  var dimensions = import_react_native.Dimensions.get("window");
+  var isTablet = ExpoDevice.deviceType === ExpoDevice.DeviceType.TABLET;
+  var isLandscape = dimensions.width > dimensions.height;
+  var isSpanning = isDualScreenDevice() && import_react_native_device_utils.ReactNativeDeviceUtils.isSpanning();
+  // OneKey patch: only tablet-like devices switch between clamped and real
+  // width at runtime, so the Tamagui useMedia patch scopes its re-render fix
+  // to them and leaves phones on upstream behavior.
+  nativeMediaControl.isTabletLike = isTablet || isDualScreenDevice();
+  if (nativeMediaControl.useRealWidth && !isSpanning) return dimensions;
+  var shouldUseHalfWidth = isSpanning || isTablet && isLandscape;
+  var shouldClampWidth = shouldUseHalfWidth || isTablet || import_react_native.Platform.OS === "android";
+  if (!shouldClampWidth) return dimensions;
+  return {
+    ...dimensions,
+    width: Math.min(shouldUseHalfWidth ? dimensions.width / 2 : dimensions.width, MAX_OVERRIDE_WIDTH)
+  };
+}
 var NativeMediaQueryList = /* @__PURE__ */function () {
   "use strict";
 
@@ -71,10 +107,21 @@ var NativeMediaQueryList = /* @__PURE__ */function () {
     _define_property(this, "listeners", void 0);
     this.query = query;
     this.listeners = [];
+    mediaUpdaters.add(function () { _this.notify(); });
     this.notify();
     import_react_native.Dimensions.addEventListener("change", function () {
       _this.notify();
     });
+    if (ExpoDevice.deviceType === ExpoDevice.DeviceType.TABLET) {
+      ScreenOrientation.addOrientationChangeListener(function () {
+        _this.notify();
+      });
+    }
+    if (isDualScreenDevice()) {
+      import_react_native_device_utils.ReactNativeDeviceUtils.addSpanningChangedListener(function () {
+        _this.notify();
+      });
+    }
   }
   _create_class(NativeMediaQueryList2, [{
     key: "orientation",
@@ -118,7 +165,7 @@ var NativeMediaQueryList = /* @__PURE__ */function () {
   }, {
     key: "matches",
     get: function get() {
-      var windowDimensions = import_react_native.Dimensions.get("window");
+      var windowDimensions = getMediaDimensions();
       var matches = (0, import_matchQuery.matchQuery)(this.query, {
         type: "screen",
         orientation: this.orientation,
diff --git a/node_modules/@tamagui/react-native-media-driver/dist/esm/mediaQueryList.native.js b/node_modules/@tamagui/react-native-media-driver/dist/esm/mediaQueryList.native.js
index 91e7740..c96b54a 100644
--- a/node_modules/@tamagui/react-native-media-driver/dist/esm/mediaQueryList.native.js
+++ b/node_modules/@tamagui/react-native-media-driver/dist/esm/mediaQueryList.native.js
@@ -1,4 +1,7 @@
-import { Dimensions } from "react-native";
+import * as ExpoDevice from "expo-device";
+import * as ScreenOrientation from "expo-screen-orientation";
+import { Dimensions, Platform } from "react-native";
+import { ReactNativeDeviceUtils } from "@onekeyfe/react-native-device-utils";
 import { matchQuery } from "./matchQuery.native.js";
 function _class_call_check(instance, Constructor) {
   if (!(instance instanceof Constructor)) {
@@ -32,6 +35,39 @@ function _define_property(obj, key, value) {
   }
   return obj;
 }
+// OneKey patch: Full-screen iPad surfaces can refresh unclamped media queries.
+var nativeMediaControl = globalThis.$$onekeyNativeMedia ||
+  (globalThis.$$onekeyNativeMedia = { useRealWidth: false, isTabletLike: false });
+var mediaUpdaters = new Set();
+nativeMediaControl.refresh = function () {
+  mediaUpdaters.forEach(function (update) { update(); });
+};
+var MAX_OVERRIDE_WIDTH = 767;
+var isDualScreen;
+function isDualScreenDevice() {
+  if (isDualScreen === void 0) {
+    isDualScreen = ReactNativeDeviceUtils.isDualScreenDevice();
+  }
+  return isDualScreen;
+}
+function getMediaDimensions() {
+  var dimensions = Dimensions.get("window");
+  var isTablet = ExpoDevice.deviceType === ExpoDevice.DeviceType.TABLET;
+  var isLandscape = dimensions.width > dimensions.height;
+  var isSpanning = isDualScreenDevice() && ReactNativeDeviceUtils.isSpanning();
+  // OneKey patch: only tablet-like devices switch between clamped and real
+  // width at runtime, so the Tamagui useMedia patch scopes its re-render fix
+  // to them and leaves phones on upstream behavior.
+  nativeMediaControl.isTabletLike = isTablet || isDualScreenDevice();
+  if (nativeMediaControl.useRealWidth && !isSpanning) return dimensions;
+  var shouldUseHalfWidth = isSpanning || isTablet && isLandscape;
+  var shouldClampWidth = shouldUseHalfWidth || isTablet || Platform.OS === "android";
+  if (!shouldClampWidth) return dimensions;
+  return {
+    ...dimensions,
+    width: Math.min(shouldUseHalfWidth ? dimensions.width / 2 : dimensions.width, MAX_OVERRIDE_WIDTH)
+  };
+}
 var NativeMediaQueryList = /* @__PURE__ */function () {
   "use strict";
 
@@ -42,10 +78,21 @@ var NativeMediaQueryList = /* @__PURE__ */function () {
     _define_property(this, "listeners", void 0);
     this.query = query;
     this.listeners = [];
+    mediaUpdaters.add(function () { _this.notify(); });
     this.notify();
     Dimensions.addEventListener("change", function () {
       _this.notify();
     });
+    if (ExpoDevice.deviceType === ExpoDevice.DeviceType.TABLET) {
+      ScreenOrientation.addOrientationChangeListener(function () {
+        _this.notify();
+      });
+    }
+    if (isDualScreenDevice()) {
+      ReactNativeDeviceUtils.addSpanningChangedListener(function () {
+        _this.notify();
+      });
+    }
   }
   _create_class(NativeMediaQueryList2, [{
     key: "orientation",
@@ -89,7 +136,7 @@ var NativeMediaQueryList = /* @__PURE__ */function () {
   }, {
     key: "matches",
     get: function get() {
-      var windowDimensions = Dimensions.get("window");
+      var windowDimensions = getMediaDimensions();
       var matches = matchQuery(this.query, {
         type: "screen",
         orientation: this.orientation,
diff --git a/node_modules/@tamagui/react-native-media-driver/src/mediaQueryList.ts b/node_modules/@tamagui/react-native-media-driver/src/mediaQueryList.ts
index 0c33f42..bb832a5 100644
--- a/node_modules/@tamagui/react-native-media-driver/src/mediaQueryList.ts
+++ b/node_modules/@tamagui/react-native-media-driver/src/mediaQueryList.ts
@@ -1,5 +1,9 @@
 import type { MediaQueryList } from '@tamagui/web'
-import { Dimensions } from 'react-native'
+import * as ExpoDevice from 'expo-device'
+import * as ScreenOrientation from 'expo-screen-orientation'
+import { Dimensions, Platform } from 'react-native'
+
+import { ReactNativeDeviceUtils } from '@onekeyfe/react-native-device-utils'
 
 import { matchQuery } from './matchQuery'
 
@@ -7,6 +11,49 @@ type Orientation = 'landscape' | 'portrait'
 
 type Listener = (orientation: Orientation) => void
 
+// OneKey patch: Normalize responsive widths for tablets and dual-screen devices.
+// OneKey patch: Full-screen iPad surfaces can refresh unclamped media queries.
+const nativeMediaControl = globalThis.$$onekeyNativeMedia ||
+  (globalThis.$$onekeyNativeMedia = { useRealWidth: false, isTabletLike: false })
+const mediaUpdaters = new Set<() => void>()
+nativeMediaControl.refresh = () => {
+  mediaUpdaters.forEach((update) => update())
+}
+const MAX_OVERRIDE_WIDTH = 767
+let isDualScreen: boolean | undefined
+
+function isDualScreenDevice() {
+  isDualScreen ??= ReactNativeDeviceUtils.isDualScreenDevice()
+  return isDualScreen
+}
+
+function getMediaDimensions() {
+  const dimensions = Dimensions.get('window')
+  const isTablet = ExpoDevice.deviceType === ExpoDevice.DeviceType.TABLET
+  const isLandscape = dimensions.width > dimensions.height
+  const isSpanning =
+    isDualScreenDevice() && ReactNativeDeviceUtils.isSpanning()
+  // OneKey patch: only tablet-like devices switch between clamped and real
+  // width at runtime, so the Tamagui useMedia patch scopes its re-render fix
+  // to them and leaves phones on upstream behavior.
+  nativeMediaControl.isTabletLike = isTablet || isDualScreenDevice()
+  if (nativeMediaControl.useRealWidth && !isSpanning) return dimensions
+  const shouldUseHalfWidth = isSpanning || (isTablet && isLandscape)
+  const shouldClampWidth = shouldUseHalfWidth || isTablet || Platform.OS === 'android'
+
+  if (!shouldClampWidth) {
+    return dimensions
+  }
+
+  return {
+    ...dimensions,
+    width: Math.min(
+      shouldUseHalfWidth ? dimensions.width / 2 : dimensions.width,
+      MAX_OVERRIDE_WIDTH
+    ),
+  }
+}
+
 export class NativeMediaQueryList implements MediaQueryList {
   private listeners: Listener[] = []
 
@@ -16,10 +63,21 @@ export class NativeMediaQueryList implements MediaQueryList {
   }
 
   constructor(private query: string) {
+    mediaUpdaters.add(() => this.notify())
     this.notify()
     Dimensions.addEventListener('change', () => {
       this.notify()
     })
+    if (ExpoDevice.deviceType === ExpoDevice.DeviceType.TABLET) {
+      ScreenOrientation.addOrientationChangeListener(() => {
+        this.notify()
+      })
+    }
+    if (isDualScreenDevice()) {
+      ReactNativeDeviceUtils.addSpanningChangedListener(() => {
+        this.notify()
+      })
+    }
   }
 
   private notify() {
@@ -47,7 +105,7 @@ export class NativeMediaQueryList implements MediaQueryList {
   }
 
   get matches(): boolean {
-    const windowDimensions = Dimensions.get('window')
+    const windowDimensions = getMediaDimensions()
     const matches = matchQuery(this.query, {
       type: 'screen',
       orientation: this.orientation,
