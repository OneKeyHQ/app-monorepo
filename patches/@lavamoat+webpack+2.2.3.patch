diff --git a/node_modules/@lavamoat/webpack/src/buildtime/policyGenerator.js b/node_modules/@lavamoat/webpack/src/buildtime/policyGenerator.js
index d6a5247..1531385 100644
--- a/node_modules/@lavamoat/webpack/src/buildtime/policyGenerator.js
+++ b/node_modules/@lavamoat/webpack/src/buildtime/policyGenerator.js
@@ -8,7 +8,9 @@ const { getPackageNameForModulePath } = require('@lavamoat/aa')
 const { writeFileSync, mkdirSync } = require('node:fs')
 const path = require('node:path')
 
-const { isExcludedUnsafe } = require('./exclude')
+// OneKey patch: generation must match enforcement's inline-loader protection.
+// const { isExcludedUnsafe } = require('./exclude')
+const { isExcluded } = require('./exclude')
 const diag = require('./diagnostics')
 
 /**
@@ -81,6 +83,35 @@ module.exports = {
       debugMode: false,
     })
 
+    // OneKey patch: loader requests are module identities, not filesystem paths.
+    // Use webpack's resolved resource for package ownership without parsing or
+    // trusting matchResource, which can name a virtual file in another package.
+    /** @param {Module} module */
+    const getModulePath = (module) =>
+      'resource' in module && typeof module.resource === 'string'
+        ? module.resource
+        : 'userRequest' in module
+          ? /** @type {NormalModule} */ (module).userRequest
+          : module.context || '<unknown>'
+
+    /** @type {Map<string, string>} */
+    const resolvedModulePaths = new Map()
+    /** @param {Module | null} module */
+    const rememberModulePath = (module) => {
+      if (module && 'userRequest' in module && typeof module.userRequest === 'string') {
+        resolvedModulePaths.set(module.userRequest, getModulePath(module))
+      }
+    }
+    // Include excluded modules and pre-optimization connections: the inspector
+    // still resolves their identities when constructing dependency permissions.
+    for (const { module, connections } of modulesToInspect) {
+      rememberModulePath(module)
+      for (const connection of connections) {
+        rememberModulePath(connection.module)
+        rememberModulePath(connection.resolvedModule)
+      }
+    }
+
     /** @type {Record<string, ReexportMetaRecord>} } */
     const meta = {}
     const REEXPORT_WARNING_KEY = 'webpack-optimization'
@@ -97,9 +128,9 @@ module.exports = {
        * @returns {string}
        */
       const getModuleSpecifierRepresentation = (m) =>
-        'userRequest' in m
-          ? /** @type {NormalModule} */ (m).userRequest
-          : m.context || '<unknown>'
+        // OneKey patch: preserve the same resolved package ownership for reexports.
+        // 'userRequest' in m ? m.userRequest : m.context || '<unknown>'
+        getModulePath(m)
 
       const a = getPackageNameForModulePath(
         canonicalNameMap,
@@ -128,13 +159,24 @@ module.exports = {
       // Specifically what happened to surface this issue: `connections` in webpack contain a module entry that's identified by the path to its index.mjs entrypoint while the index.js entrypoint is actually used in the bundle, so inspector doesn't have a cached entry for this one and without the fallback handler would return <unknown:/...>
       // There should be no security implications of this, since the package is already resolved clearly and this is only a part of policy generation, not runtime.
       moduleToPackageFallback: (specifier) =>
-        getPackageNameForModulePath(canonicalNameMap, specifier),
+        // OneKey patch: keep specifier identity while resolving loader requests.
+        // getPackageNameForModulePath(canonicalNameMap, specifier),
+        getPackageNameForModulePath(
+          canonicalNameMap,
+          resolvedModulePaths.get(specifier) || specifier
+        ),
     })
 
     for (const { module, connections } of modulesToInspect) {
-      // Skip modules the user intentionally excludes.
-      // This is policy generation so we don't need to protect ourselves from an attack where the module has a loader defined in the specifier.
-      if (isExcludedUnsafe(module)) continue
+      // OneKey patch: inline requests remain wrapped at runtime, so their
+      // generated JavaScript and dependencies must also be inspected here.
+      // if (isExcludedUnsafe(module)) continue
+      if (
+        'loaders' in module &&
+        Array.isArray(module.loaders) &&
+        typeof module.rawRequest === 'string' &&
+        isExcluded(module)
+      ) continue
 
       if (module.userRequest === undefined) {
         diag.rawDebug(1, `LavaMoatPlugin: Module ${module} has no userRequest`)
@@ -144,12 +186,21 @@ module.exports = {
 
       const packageName = getPackageNameForModulePath(
         canonicalNameMap,
-        module.userRequest
+        // OneKey patch: never treat a loader chain or matchResource as a path.
+        // module.userRequest
+        getModulePath(module)
       )
       const moduleRecord = new LavamoatModuleRecord({
         // Knowing the actual specifier is not relevant here, they're used as unique identifiers that match between here and dependencies
         specifier: module.userRequest,
-        file: module.userRequest,
+        // OneKey patch: loaders produce JavaScript even for .css/.tsx resources.
+        // Core filters by file extension; preserve module identity separately.
+        // file: module.userRequest,
+        file: module.type?.startsWith('javascript/') &&
+          path.extname(module.userRequest) &&
+          !/\.(?:[cm]?js|ts)$/.test(module.userRequest)
+          ? `${module.userRequest}.js`
+          : module.userRequest,
         type: isBuiltin(module.userRequest) ? 'builtin' : 'js',
         packageName,
         content: module.originalSource()?.source()?.toString(),
diff --git a/node_modules/@lavamoat/webpack/src/plugin.js b/node_modules/@lavamoat/webpack/src/plugin.js
index 62fe34d..d587b5c 100644
--- a/node_modules/@lavamoat/webpack/src/plugin.js
+++ b/node_modules/@lavamoat/webpack/src/plugin.js
@@ -220,7 +220,20 @@ class LavaMoatPlugin {
         includeDevDeps: true, // even the most proper projects end up including devDeps in their bundles :(
         resolve: browserResolve,
       })
-        .then((map) => {
+        // OneKey patch: load the production map before publishing canonical names.
+        // .then((map) => {
+        .then(async (map) => {
+          // OneKey patch: prefer production dependency paths for shared packages.
+          // Host-specific development tools must not rename runtime compartments.
+          // Keep the full map and its rootDir metadata for bundled dev-only modules.
+          const productionMap = await loadCanonicalNameMap({
+            rootDir: STORE.options.rootDir || compiler.context,
+            includeDevDeps: false,
+            resolve: browserResolve,
+          })
+          for (const [packageDir, canonicalName] of productionMap) {
+            map.set(packageDir, canonicalName)
+          }
           STORE.canonicalNameMap = map
           PROGRESS.report('canonicalNameMap')
           callback()
