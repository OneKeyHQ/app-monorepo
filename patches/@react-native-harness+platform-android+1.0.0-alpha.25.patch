diff --git a/node_modules/@react-native-harness/platform-android/dist/adb.js b/node_modules/@react-native-harness/platform-android/dist/adb.js
index 0b6a1d1..4f7c4bc 100644
--- a/node_modules/@react-native-harness/platform-android/dist/adb.js
+++ b/node_modules/@react-native-harness/platform-android/dist/adb.js
@@ -21,7 +21,18 @@ export const reversePort = async (adbId, port, hostPort = port) => {
     ]);
 };
 export const stopApp = async (adbId, bundleId) => {
-    await spawn('adb', ['-s', adbId, 'shell', 'am', 'force-stop', bundleId]);
+    try {
+        await spawn('adb', ['-s', adbId, 'shell', 'am', 'force-stop', bundleId]);
+    }
+    catch (error) {
+        // Some Android emulator images return exit code 1 for force-stop even when
+        // the app is already stopped. Treat stop as best-effort; a real device
+        // failure is still caught by the following start/is-running adb calls.
+        if (String(error?.message ?? error).includes('am force-stop')) {
+            return;
+        }
+        throw error;
+    }
 };
 export const startApp = async (adbId, bundleId, activityName) => {
     await spawn('adb', [
diff --git a/node_modules/@react-native-harness/platform-android/src/adb.ts b/node_modules/@react-native-harness/platform-android/src/adb.ts
index f43b1cb..9a2b61d 100644
--- a/node_modules/@react-native-harness/platform-android/src/adb.ts
+++ b/node_modules/@react-native-harness/platform-android/src/adb.ts
@@ -29,8 +29,18 @@ export const reversePort = async (
 export const stopApp = async (
   adbId: string,
   bundleId: string
 ): Promise<void> => {
-  await spawn('adb', ['-s', adbId, 'shell', 'am', 'force-stop', bundleId]);
+  try {
+    await spawn('adb', ['-s', adbId, 'shell', 'am', 'force-stop', bundleId]);
+  } catch (error) {
+    // Some Android emulator images return exit code 1 for force-stop even when
+    // the app is already stopped. Treat stop as best-effort; a real device
+    // failure is still caught by the following start/is-running adb calls.
+    if (String(error?.message ?? error).includes('am force-stop')) {
+      return;
+    }
+    throw error;
+  }
 };
 
 export const startApp = async (
