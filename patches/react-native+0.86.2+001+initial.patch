diff --git a/node_modules/react-native/React/Fabric/Mounting/ComponentViews/LegacyViewManagerInterop/RCTLegacyViewManagerInteropComponentView.mm b/node_modules/react-native/React/Fabric/Mounting/ComponentViews/LegacyViewManagerInterop/RCTLegacyViewManagerInteropComponentView.mm
index 226781f..82a9790 100644
--- a/node_modules/react-native/React/Fabric/Mounting/ComponentViews/LegacyViewManagerInterop/RCTLegacyViewManagerInteropComponentView.mm
+++ b/node_modules/react-native/React/Fabric/Mounting/ComponentViews/LegacyViewManagerInterop/RCTLegacyViewManagerInteropComponentView.mm
@@ -103,46 +103,51 @@ - (NSMethodSignature *)methodSignatureForSelector:(SEL)aSelector
 
 + (BOOL)isSupported:(NSString *)componentName
 {
-  // Step 1: check if ViewManager with specified name is supported.
-  BOOL isComponentNameSupported =
-      [[RCTLegacyViewManagerInteropComponentView supportedViewManagers] containsObject:componentName];
-  if (isComponentNameSupported) {
-    return YES;
-  }
+  // This method accesses a shared static NSMutableDictionary and may be called
+  // concurrently from multiple JS runtimes (e.g. main + background thread).
+  // @synchronized ensures thread-safe access to the mutable shared state.
+  @synchronized([RCTLegacyViewManagerInteropComponentView class]) {
+    // Step 1: check if ViewManager with specified name is supported.
+    BOOL isComponentNameSupported =
+        [[RCTLegacyViewManagerInteropComponentView supportedViewManagers] containsObject:componentName];
+    if (isComponentNameSupported) {
+      return YES;
+    }
 
-  // Step 2: check if component has supported prefix.
-  for (NSString *item in [RCTLegacyViewManagerInteropComponentView supportedViewManagersPrefixes]) {
-    if ([componentName hasPrefix:item]) {
+    // Step 2: check if component has supported prefix.
+    for (NSString *item in [RCTLegacyViewManagerInteropComponentView supportedViewManagersPrefixes]) {
+      if ([componentName hasPrefix:item]) {
+        return YES;
+      }
+    }
+
+    // Step 3: check if the module has been registered
+    // TODO(T174674274): Implement lazy loading of legacy view managers in the new architecture.
+    NSArray<Class> *registeredModules = RCTGetModuleClasses();
+    NSMutableDictionary<NSString *, Class> *supportedLegacyViewComponents =
+        [RCTLegacyViewManagerInteropComponentView _supportedLegacyViewComponents];
+    if (supportedLegacyViewComponents[componentName] != NULL) {
       return YES;
     }
-  }
 
-  // Step 3: check if the module has been registered
-  // TODO(T174674274): Implement lazy loading of legacy view managers in the new architecture.
-  NSArray<Class> *registeredModules = RCTGetModuleClasses();
-  NSMutableDictionary<NSString *, Class> *supportedLegacyViewComponents =
-      [RCTLegacyViewManagerInteropComponentView _supportedLegacyViewComponents];
-  if (supportedLegacyViewComponents[componentName] != NULL) {
-    return YES;
-  }
+    for (Class moduleClass in registeredModules) {
+      id<RCTBridgeModule> bridgeModule = (id<RCTBridgeModule>)moduleClass;
+      NSString *moduleName = [[bridgeModule moduleName] isEqualToString:@""]
+          ? [NSStringFromClass(moduleClass) stringByReplacingOccurrencesOfString:@"Manager" withString:@""]
+          : [bridgeModule moduleName];
 
-  for (Class moduleClass in registeredModules) {
-    id<RCTBridgeModule> bridgeModule = (id<RCTBridgeModule>)moduleClass;
-    NSString *moduleName = [[bridgeModule moduleName] isEqualToString:@""]
-        ? [NSStringFromClass(moduleClass) stringByReplacingOccurrencesOfString:@"Manager" withString:@""]
-        : [bridgeModule moduleName];
+      if (supportedLegacyViewComponents[moduleName] == NULL) {
+        supportedLegacyViewComponents[moduleName] = moduleClass;
+      }
 
-    if (supportedLegacyViewComponents[moduleName] == NULL) {
-      supportedLegacyViewComponents[moduleName] = moduleClass;
+      if ([moduleName isEqualToString:componentName] ||
+          [moduleName isEqualToString:[@"RCT" stringByAppendingString:componentName]]) {
+        return YES;
+      }
     }
 
-    if ([moduleName isEqualToString:componentName] ||
-        [moduleName isEqualToString:[@"RCT" stringByAppendingString:componentName]]) {
-      return YES;
-    }
+    return NO;
   }
-
-  return NO;
 }
 
 + (void)supportLegacyViewManagersWithPrefix:(NSString *)prefix
diff --git a/node_modules/react-native/React/Fabric/Mounting/ComponentViews/View/RCTViewComponentView.mm b/node_modules/react-native/React/Fabric/Mounting/ComponentViews/View/RCTViewComponentView.mm
index b033b7c..4095ea1 100644
--- a/node_modules/react-native/React/Fabric/Mounting/ComponentViews/View/RCTViewComponentView.mm
+++ b/node_modules/react-native/React/Fabric/Mounting/ComponentViews/View/RCTViewComponentView.mm
@@ -987,6 +987,19 @@ - (UIView *)currentContainerView
 }
 
 - (void)invalidateLayer
+{
+  // During modal transition, prevent backgroundColor and layer changes from being animated.
+  extern BOOL RNSModalTransitionInProgress;
+  if (RNSModalTransitionInProgress) {
+    [UIView performWithoutAnimation:^{
+      [self _invalidateLayerImpl];
+    }];
+  } else {
+    [self _invalidateLayerImpl];
+  }
+}
+
+- (void)_invalidateLayerImpl
 {
   CALayer *layer = self.effectiveContentView.layer;
 
diff --git a/node_modules/react-native/React/Fabric/Mounting/UIView+ComponentViewProtocol.mm b/node_modules/react-native/React/Fabric/Mounting/UIView+ComponentViewProtocol.mm
index 36e5f64..2dd748a 100644
--- a/node_modules/react-native/React/Fabric/Mounting/UIView+ComponentViewProtocol.mm
+++ b/node_modules/react-native/React/Fabric/Mounting/UIView+ComponentViewProtocol.mm
@@ -103,8 +103,24 @@ - (void)updateLayoutMetrics:(const LayoutMetrics &)layoutMetrics
     } else {
       // Note: Changing `frame` when `layer.transform` is not the `identity transform` is undefined behavior.
       // Therefore, we must use `center` and `bounds`.
-      self.center = CGPoint{CGRectGetMidX(frame), CGRectGetMidY(frame)};
-      self.bounds = CGRect{CGPointZero, frame.size};
+      //
+      // When a modal transition is in progress, Fabric may mount recycled views
+      // whose stale frames differ from the new layout. Without performWithoutAnimation,
+      // UIKit captures the center/bounds change as an implicit animation within the
+      // modal presentation's animation block, causing visible content displacement.
+      extern BOOL RNSModalTransitionInProgress;
+      if (RNSModalTransitionInProgress) {
+        [UIView performWithoutAnimation:^{
+          self.center = CGPoint{CGRectGetMidX(frame), CGRectGetMidY(frame)};
+          self.bounds = CGRect{CGPointZero, frame.size};
+          // Force immediate layout so that subviews (e.g. RCTParagraphTextView)
+          // also get their frames set within performWithoutAnimation scope.
+          [self layoutIfNeeded];
+        }];
+      } else {
+        self.center = CGPoint{CGRectGetMidX(frame), CGRectGetMidY(frame)};
+        self.bounds = CGRect{CGPointZero, frame.size};
+      }
     }
   }
 
diff --git a/node_modules/react-native/ReactAndroid/src/main/java/com/facebook/react/uimanager/ViewGroupDrawingOrderHelper.kt b/node_modules/react-native/ReactAndroid/src/main/java/com/facebook/react/uimanager/ViewGroupDrawingOrderHelper.kt
index 8b43194..d2e084b 100644
--- a/node_modules/react-native/ReactAndroid/src/main/java/com/facebook/react/uimanager/ViewGroupDrawingOrderHelper.kt
+++ b/node_modules/react-native/ReactAndroid/src/main/java/com/facebook/react/uimanager/ViewGroupDrawingOrderHelper.kt
@@ -72,6 +72,10 @@ public class ViewGroupDrawingOrderHelper(private val viewGroup: ViewGroup) {
           index,
       )
       update()
+      // update() set this.drawingOrderIndices to null, so we must also null the
+      // local variable; otherwise the rebuild branch below is skipped and we
+      // return a stale, out-of-bounds index (Sentry REACT-NATIVE-48W / 4AM).
+      currentDrawingOrderIndices = null
     }
 
     if (currentDrawingOrderIndices == null) {
diff --git a/node_modules/react-native/ReactAndroid/src/main/java/com/facebook/react/views/view/ReactViewGroup.kt b/node_modules/react-native/ReactAndroid/src/main/java/com/facebook/react/views/view/ReactViewGroup.kt
index 5977524..1b3f871 100644
--- a/node_modules/react-native/ReactAndroid/src/main/java/com/facebook/react/views/view/ReactViewGroup.kt
+++ b/node_modules/react-native/ReactAndroid/src/main/java/com/facebook/react/views/view/ReactViewGroup.kt
@@ -931,7 +931,13 @@ public open class ReactViewGroup public constructor(context: Context?) :
     if (_overflow != Overflow.VISIBLE || getTag(R.id.filter) != null) {
       clipToPaddingBox(this, canvas)
     }
-    super.dispatchDraw(canvas)
+    try {
+      super.dispatchDraw(canvas)
+    } catch (e: NullPointerException) {
+      // Race condition: child view removed during draw cycle by reanimated mount transaction.
+      // The view hierarchy will be re-drawn correctly on the next frame.
+      // Tracking: https://github.com/software-mansion/react-native-reanimated/issues/8422
+    }
   }
 
   override fun drawChild(canvas: Canvas, child: View, drawingTime: Long): Boolean {
diff --git a/node_modules/react-native/ReactCommon/react/featureflags/ReactNativeFeatureFlagsOverridesOSSStable.h b/node_modules/react-native/ReactCommon/react/featureflags/ReactNativeFeatureFlagsOverridesOSSStable.h
index fdabd7b..ef70011 100644
--- a/node_modules/react-native/ReactCommon/react/featureflags/ReactNativeFeatureFlagsOverridesOSSStable.h
+++ b/node_modules/react-native/ReactCommon/react/featureflags/ReactNativeFeatureFlagsOverridesOSSStable.h
@@ -21,6 +21,10 @@ class ReactNativeFeatureFlagsOverridesOSSStable : public ReactNativeFeatureFlags
   {
     return true;
   }
+  bool enableSchedulerDelegateInvalidation() override
+  {
+    return true;
+  }
   bool useTurboModules() override
   {
     return true;
diff --git a/node_modules/react-native/ReactCommon/react/nativemodule/core/platform/ios/ReactCommon/RCTTurboModule.mm b/node_modules/react-native/ReactCommon/react/nativemodule/core/platform/ios/ReactCommon/RCTTurboModule.mm
index 516ae35..cc2353c 100644
--- a/node_modules/react-native/ReactCommon/react/nativemodule/core/platform/ios/ReactCommon/RCTTurboModule.mm
+++ b/node_modules/react-native/ReactCommon/react/nativemodule/core/platform/ios/ReactCommon/RCTTurboModule.mm
@@ -91,6 +91,9 @@ static int32_t getUniqueId()
 
 jsi::Value convertObjCObjectToJSIValue(jsi::Runtime &runtime, id value)
 {
+  if (value == nil) {
+    return jsi::Value::null();
+  }
   if ([value isKindOfClass:[NSString class]]) {
     return convertNSStringToJSIString(runtime, (NSString *)value);
   } else if ([value isKindOfClass:[NSNumber class]]) {
diff --git a/node_modules/react-native/ReactCommon/react/runtime/platform/ios/ReactCommon/RCTHost.mm b/node_modules/react-native/ReactCommon/react/runtime/platform/ios/ReactCommon/RCTHost.mm
index 0d5762d..85f2779 100644
--- a/node_modules/react-native/ReactCommon/react/runtime/platform/ios/ReactCommon/RCTHost.mm
+++ b/node_modules/react-native/ReactCommon/react/runtime/platform/ios/ReactCommon/RCTHost.mm
@@ -10,6 +10,7 @@
 
 #import <React/RCTAssert.h>
 #import <React/RCTBridgeModule.h>
+#import <React/RCTConstants.h>
 #import <React/RCTConvert.h>
 #import <React/RCTDevMenu.h>
 #import <React/RCTFabricSurface.h>
@@ -260,6 +261,9 @@ bool onSetEmulatedMedia(const SetEmulatedMediaRequest &request) override
 
 @implementation RCTHost {
   RCTInstance *_instance;
+  BOOL _reloadInProgress;
+  BOOL _reloadPending;
+  BOOL _pendingShouldRestartSurfaces;
 
   __weak id<RCTHostDelegate> _hostDelegate;
   __weak id<RCTTurboModuleManagerDelegate> _turboModuleManagerDelegate;
@@ -332,6 +336,9 @@ - (instancetype)initWithBundleURLProvider:(RCTHostBundleURLProvider)provider
                      devMenuConfiguration:(RCTDevMenuConfiguration *)devMenuConfiguration
 {
   if (self = [super init]) {
+    _reloadInProgress = NO;
+    _reloadPending = NO;
+    _pendingShouldRestartSurfaces = NO;
     _hostDelegate = hostDelegate;
     _turboModuleManagerDelegate = turboModuleManagerDelegate;
     _bundleManager = [[RCTBundleManager alloc] initWithBundleConfig:bundleConfiguration];
@@ -371,6 +378,10 @@ - (instancetype)initWithBundleURLProvider:(RCTHostBundleURLProvider)provider
     RCTExecuteOnMainQueue(^{
       // Listen to reload commands
       RCTRegisterReloadCommandListener(self);
+      [[NSNotificationCenter defaultCenter] addObserver:self
+                                               selector:@selector(_javascriptDidFailToLoad:)
+                                                   name:RCTJavaScriptDidFailToLoadNotification
+                                                 object:nil];
     });
 
     _inspectorHostDelegate = std::make_unique<RCTHostHostTargetDelegate>(self);
@@ -475,6 +486,9 @@ - (void)didReceiveReloadCommand
 
 - (void)dealloc
 {
+  [[NSNotificationCenter defaultCenter] removeObserver:self
+                                                  name:RCTJavaScriptDidFailToLoadNotification
+                                                object:nil];
   [_instance invalidate];
   if (_inspectorPageId.has_value()) {
     facebook::react::jsinspector_modern::getInspectorInstance().removePage(*_inspectorPageId);
@@ -617,8 +631,53 @@ - (void)_setBundleURL:(NSURL *)bundleURL
   RCTReloadCommandSetBundleURL(_bundleURL);
 }
 
+- (void)_completeReloadForInstance:(RCTInstance *)reloadedInstance
+{
+  RCTAssertMainQueue();
+  if (!_reloadInProgress || reloadedInstance != _instance) {
+    return;
+  }
+
+  _reloadInProgress = NO;
+  const BOOL shouldRunPendingReload = _reloadPending;
+  const BOOL shouldRestartSurfaces = _pendingShouldRestartSurfaces;
+  _reloadPending = NO;
+  _pendingShouldRestartSurfaces = NO;
+
+  if (shouldRunPendingReload) {
+    __weak RCTHost *weakSelf = self;
+    dispatch_async(dispatch_get_main_queue(), ^{
+      [weakSelf _reloadWithShouldRestartSurfaces:shouldRestartSurfaces];
+    });
+  }
+}
+
+- (void)_javascriptDidFailToLoad:(NSNotification *)notification
+{
+  [self _completeReloadForInstance:notification.object];
+}
+
 - (void)_reloadWithShouldRestartSurfaces:(BOOL)shouldRestartSurfaces
 {
+  if (![NSThread isMainThread]) {
+    __weak RCTHost *weakSelf = self;
+    dispatch_async(dispatch_get_main_queue(), ^{
+      [weakSelf _reloadWithShouldRestartSurfaces:shouldRestartSurfaces];
+    });
+    return;
+  }
+
+  // A reload command is delivered to every host in the process. Queue one
+  // follow-up request per host so foreground and background runtimes cannot
+  // permanently drop a restart while a replacement runtime is initializing.
+  if (_reloadInProgress) {
+    _reloadPending = YES;
+    _pendingShouldRestartSurfaces = _pendingShouldRestartSurfaces || shouldRestartSurfaces;
+    RCTLogInfo(@"Queued reload while the replacement runtime is initializing");
+    return;
+  }
+  _reloadInProgress = YES;
+
   [_instance invalidate];
   _instance = nil;
   if (_bundleURLProvider) {
@@ -641,6 +700,36 @@ - (void)_reloadWithShouldRestartSurfaces:(BOOL)shouldRestartSurfaces
       [_instance callFunctionOnBufferedRuntimeExecutor:[surface](facebook::jsi::Runtime &_) { [surface start]; }];
     }
   }
+
+  // Keep the reload gate closed until the bundle has finished executing and
+  // all attached surfaces have been queued to restart. Runtime initialization
+  // alone is too early: a second reload can otherwise tear down a Scheduler
+  // that still has rendering updates queued from the replacement instance.
+  // A short quiet window after bundle execution also coalesces repeated Metro
+  // reload commands (for example pressing `r` twice) without affecting the
+  // cold-start path.
+  __weak RCTHost *weakSelf = self;
+  __weak RCTInstance *weakReloadedInstance = _instance;
+  [_instance callFunctionOnBufferedRuntimeExecutor:[weakSelf, weakReloadedInstance](facebook::jsi::Runtime &_) {
+    dispatch_after(dispatch_time(DISPATCH_TIME_NOW, (int64_t)(1 * NSEC_PER_SEC)), dispatch_get_main_queue(), ^{
+      RCTHost *strongSelf = weakSelf;
+      RCTInstance *strongReloadedInstance = weakReloadedInstance;
+      if (strongSelf && strongReloadedInstance) {
+        [strongSelf _completeReloadForInstance:strongReloadedInstance];
+      }
+    });
+  }];
+
+  // Bundle loading or execution can fail before the buffered executor runs.
+  // Always reopen the gate so a later reload can recover without restarting
+  // the process. Stale timers are ignored by the instance identity check.
+  dispatch_after(dispatch_time(DISPATCH_TIME_NOW, (int64_t)(10 * NSEC_PER_SEC)), dispatch_get_main_queue(), ^{
+    RCTHost *strongSelf = weakSelf;
+    RCTInstance *strongReloadedInstance = weakReloadedInstance;
+    if (strongSelf && strongReloadedInstance) {
+      [strongSelf _completeReloadForInstance:strongReloadedInstance];
+    }
+  });
 }
 
 #pragma mark - jsinspector_modern
diff --git a/node_modules/react-native/ReactCommon/react/runtime/platform/ios/ReactCommon/RCTInstance.mm b/node_modules/react-native/ReactCommon/react/runtime/platform/ios/ReactCommon/RCTInstance.mm
index d065b1b..cf02d45 100644
--- a/node_modules/react-native/ReactCommon/react/runtime/platform/ios/ReactCommon/RCTInstance.mm
+++ b/node_modules/react-native/ReactCommon/react/runtime/platform/ios/ReactCommon/RCTInstance.mm
@@ -504,7 +504,16 @@ - (void)_attachBridgelessAPIsToModule:(id<RCTTurboModule>)module
 
 - (void)callFunctionOnBufferedRuntimeExecutor:(std::function<void(facebook::jsi::Runtime &)> &&)executor
 {
-  _reactInstance->getBufferedRuntimeExecutor()([=](jsi::Runtime &runtime) {
+  RuntimeExecutor bufferedRuntimeExecutor;
+  {
+    std::lock_guard<std::mutex> lock(_invalidationMutex);
+    if (!_valid || !_reactInstance) {
+      return;
+    }
+    bufferedRuntimeExecutor = _reactInstance->getBufferedRuntimeExecutor();
+  }
+
+  bufferedRuntimeExecutor([=](jsi::Runtime &runtime) {
     if (executor) {
       executor(runtime);
     }
diff --git a/node_modules/react-native/scripts/cocoapods/new_architecture.rb b/node_modules/react-native/scripts/cocoapods/new_architecture.rb
index 3873632..6c0683f 100644
--- a/node_modules/react-native/scripts/cocoapods/new_architecture.rb
+++ b/node_modules/react-native/scripts/cocoapods/new_architecture.rb
@@ -171,8 +171,7 @@ class NewArchitectureHelper
         excluded_info_plist = ["/Pods", "Tests", "metainternal", ".bundle", "build/", "DerivedData/", ".xcframework", ".framework", "watchkitapp", "today-extention"]
         projectPaths.each do |projectPath|
             projectFolderPath = File.dirname(projectPath)
-            infoPlistFiles = `find #{projectFolderPath} -name "Info.plist"`
-            infoPlistFiles = infoPlistFiles.split("\n").map { |f| f.strip }
+            infoPlistFiles = Dir.glob(File.join(projectFolderPath, "**", "Info.plist"))
 
             infoPlistFiles.each do |infoPlistFile|
                 # If infoPlistFile contains Pods or tests, skip it
diff --git a/node_modules/react-native/Libraries/Utilities/HMRClient.js b/node_modules/react-native/Libraries/Utilities/HMRClient.js
index 5911eaa..2b3e8cf 100644
--- a/node_modules/react-native/Libraries/Utilities/HMRClient.js
+++ b/node_modules/react-native/Libraries/Utilities/HMRClient.js
@@ -51,6 +51,7 @@ export type HMRClientNativeInterface = {
     port: number | string,
     isEnabled: boolean,
     scheme?: string,
+    fullBundleUrlOverride?: ?string,
   ): void,
 };

@@ -156,6 +157,7 @@ const HMRClient: HMRClientNativeInterface = {
     port: number | string,
     isEnabled: boolean,
     scheme?: string = 'http',
+    fullBundleUrlOverride?: ?string,
   ) {
     invariant(platform, 'Missing required parameter `platform`');
     invariant(bundleEntry, 'Missing required parameter `bundleEntry`');
@@ -182,7 +184,8 @@ const HMRClient: HMRClientNativeInterface = {
       // HMRServer understands regular bundle URLs, so prefer that in case
       // there are any important URL parameters we can't reconstruct from
       // `setup()`'s arguments.
-      fullBundleUrl ??
+      fullBundleUrlOverride ??
+        fullBundleUrl ??
         `${serverScheme}://${serverHost}/hot?bundleEntry=${bundleEntry}&platform=${platform}`,
     );

@@ -322,7 +325,35 @@ function setHMRUnavailableReason(reason: string) {

 function registerBundleEntryPoints(client: MetroHMRClient) {
   if (hmrUnavailableReason != null) {
-    DevSettings.reload('Bundle Splitting – Metro disconnected');
+    if (global['__ONEKEY_RUNTIME_TARGET__'] === 'background') {
+      const generation = global['__ONEKEY_BACKGROUND_RUNTIME_GENERATION__'];
+      const TurboModuleRegistry = require('../TurboModule/TurboModuleRegistry');
+      const backgroundThread: any =
+        TurboModuleRegistry.get('BackgroundThread');
+      if (
+        typeof generation === 'number' &&
+        backgroundThread != null &&
+        typeof backgroundThread.restart === 'function'
+      ) {
+        backgroundThread
+          .restart(
+            'background',
+            `onekey-bg-hmr:${generation}:Bundle Splitting – Metro disconnected`,
+          )
+          .catch(error => {
+            console.error(
+              '[BackgroundHMR] Background-only reconnect failed.',
+              error,
+            );
+          });
+      } else {
+        console.error(
+          '[BackgroundHMR] Background-only reconnect is unavailable; the main runtime was left running.',
+        );
+      }
+    } else {
+      DevSettings.reload('Bundle Splitting – Metro disconnected');
+    }
     return;
   }

diff --git a/node_modules/react-native/ReactAndroid/src/main/java/com/facebook/react/devsupport/HMRClient.kt b/node_modules/react-native/ReactAndroid/src/main/java/com/facebook/react/devsupport/HMRClient.kt
index 286dcb6..04f3459 100644
--- a/node_modules/react-native/ReactAndroid/src/main/java/com/facebook/react/devsupport/HMRClient.kt
+++ b/node_modules/react-native/ReactAndroid/src/main/java/com/facebook/react/devsupport/HMRClient.kt
@@ -30,2 +30,3 @@ public interface HMRClient : JavaScriptModule {
    *   http).
+   * @param fullBundleUrlOverride The exact bundle request whose graph should receive HMR updates.
    */
@@ -38,2 +39,3 @@ public interface HMRClient : JavaScriptModule {
       scheme: String?,
+      fullBundleUrlOverride: String?,
   )
diff --git a/node_modules/react-native/ReactAndroid/src/main/java/com/facebook/react/devsupport/DevSupportManagerBase.kt b/node_modules/react-native/ReactAndroid/src/main/java/com/facebook/react/devsupport/DevSupportManagerBase.kt
index 0608c07..e17eff2 100644
--- a/node_modules/react-native/ReactAndroid/src/main/java/com/facebook/react/devsupport/DevSupportManagerBase.kt
+++ b/node_modules/react-native/ReactAndroid/src/main/java/com/facebook/react/devsupport/DevSupportManagerBase.kt
@@ -702,4 +702,12 @@ public abstract class DevSupportManagerBase(
       try {
-        val sourceUrl = URL(sourceUrl)
-        var path = sourceUrl.path
+        val liveSourceUrl = reactContext.sourceURL?.let(::URL)
+        val isNativeDevVendorSource =
+            (liveSourceUrl?.protocol == "http" || liveSourceUrl?.protocol == "https") &&
+                liveSourceUrl
+                    ?.query
+                    ?.split('&')
+                    ?.contains("resolver.devVendorNative=true") == true
+        val hmrSourceUrl =
+            if (isNativeDevVendorSource) checkNotNull(liveSourceUrl) else URL(sourceUrl)
+        var path = hmrSourceUrl.path
         if (path != null) {
@@ -707,8 +715,17 @@ public abstract class DevSupportManagerBase(
         }
-        val host = sourceUrl.host
-        val scheme = sourceUrl.protocol
-        val port = if (sourceUrl.port != -1) sourceUrl.port else sourceUrl.defaultPort
+        val host = hmrSourceUrl.host
+        val scheme = hmrSourceUrl.protocol
+        val port =
+            if (hmrSourceUrl.port != -1) hmrSourceUrl.port else hmrSourceUrl.defaultPort
         reactContext
             .getJSModule(HMRClient::class.java)
-            .setup("android", path, host, port, devSettings.isHotModuleReplacementEnabled, scheme)
+            .setup(
+                "android",
+                path,
+                host,
+                port,
+                devSettings.isHotModuleReplacementEnabled,
+                scheme,
+                if (isNativeDevVendorSource) hmrSourceUrl.toString() else null,
+            )
       } catch (e: MalformedURLException) {
diff --git a/node_modules/react-native/Libraries/Core/Devtools/getDevServer.js b/node_modules/react-native/Libraries/Core/Devtools/getDevServer.js
index a158d98..bd961d9 100644
--- a/node_modules/react-native/Libraries/Core/Devtools/getDevServer.js
+++ b/node_modules/react-native/Libraries/Core/Devtools/getDevServer.js
@@ -14,2 +14,19 @@ let _cachedDevServerURL: ?string;
 let _cachedFullBundleURL: ?string;
+
+function getDevVendorFullBundleURL(): ?string {
+  // OneKey patch: expose the live runtime graph after startup from local common HBC.
+  const fullBundleURL = global['__ONEKEY_DEV_VENDOR_FULL_BUNDLE_URL__'];
+  if (
+    typeof fullBundleURL !== 'string' ||
+    !/^https?:\/\/.*?\//.test(fullBundleURL) ||
+    !/[?&]resolver\.devVendorNative=true(?:&|$)/.test(fullBundleURL) ||
+    !/[?&]resolver\.runtimeTarget=(?:main|background)(?:&|$)/.test(
+      fullBundleURL,
+    )
+  ) {
+    return null;
+  }
+  return fullBundleURL;
+}
+
 const FALLBACK = 'http://localhost:8081/';
@@ -28,3 +45,11 @@ type DevServerInfo = {
 export default function getDevServer(): DevServerInfo {
-  if (_cachedDevServerURL === undefined) {
+  const devVendorFullBundleURL = getDevVendorFullBundleURL();
+  if (
+    devVendorFullBundleURL !== null &&
+    _cachedFullBundleURL !== devVendorFullBundleURL
+  ) {
+    const match = devVendorFullBundleURL.match(/^https?:\/\/.*?\//);
+    _cachedDevServerURL = match ? match[0] : null;
+    _cachedFullBundleURL = match ? devVendorFullBundleURL : null;
+  } else if (_cachedDevServerURL === undefined) {
     const scriptUrl = NativeSourceCode.getConstants().scriptURL;
diff --git a/node_modules/react-native/Libraries/Core/setUpReactRefresh.js b/node_modules/react-native/Libraries/Core/setUpReactRefresh.js
index be8844d..3e79707 100644
--- a/node_modules/react-native/Libraries/Core/setUpReactRefresh.js
+++ b/node_modules/react-native/Libraries/Core/setUpReactRefresh.js
@@ -17,12 +17,37 @@ if (__DEV__) {
     throw new Error('Could not find the reload() implementation.');
   }

+  const restartBackgroundRuntime = (reason: string) => {
+    const generation = global['__ONEKEY_BACKGROUND_RUNTIME_GENERATION__'];
+    const TurboModuleRegistry = require('../TurboModule/TurboModuleRegistry');
+    const backgroundThread: any = TurboModuleRegistry.get('BackgroundThread');
+    if (
+      typeof generation !== 'number' ||
+      backgroundThread == null ||
+      typeof backgroundThread.restart !== 'function'
+    ) {
+      console.error(
+        '[BackgroundHMR] Background-only restart is unavailable; the main runtime was left running.',
+      );
+      return;
+    }
+    backgroundThread
+      .restart('background', `onekey-bg-hmr:${generation}:${reason}`)
+      .catch(error => {
+        console.error('[BackgroundHMR] Background-only restart failed.', error);
+      });
+  };
+
   // This needs to run before the renderer initializes.
   const ReactRefreshRuntime = require('react-refresh/runtime');
   ReactRefreshRuntime.injectIntoGlobalHook(global);

   const Refresh = {
     performFullRefresh(reason: string) {
+      if (global['__ONEKEY_RUNTIME_TARGET__'] === 'background') {
+        restartBackgroundRuntime(reason);
+        return;
+      }
       DevSettings.reload(reason);
     },

diff --git a/node_modules/react-native/Libraries/Image/resolveAssetSource.js b/node_modules/react-native/Libraries/Image/resolveAssetSource.js
index e08a6c8..de078a2 100644
--- a/node_modules/react-native/Libraries/Image/resolveAssetSource.js
+++ b/node_modules/react-native/Libraries/Image/resolveAssetSource.js
@@ -14,6 +14,7 @@ import type {ResolvedAssetSource} from './AssetSourceResolver';
 import typeof AssetSourceResolverT from './AssetSourceResolver';
 import type {ImageSource} from './ImageSource';

+import getDevServer from '../Core/Devtools/getDevServer';
 import SourceCode from '../NativeModules/specs/NativeSourceCode';

 const AssetSourceResolver: AssetSourceResolverT =
@@ -40,6 +41,12 @@ function getSourceCodeScriptURL(): ?string {
 }

 function getDevServerURL(): ?string {
+  // OneKey patch: resolve live dev-vendor assets from Metro after local common HBC startup.
+  const devServer = getDevServer();
+  if (devServer.bundleLoadedFromServer) {
+    return devServer.url;
+  }
+
   if (_serverURL === undefined) {
     const sourceCodeScriptURL = getSourceCodeScriptURL();
     const match = sourceCodeScriptURL?.match(/^https?:\/\/.*?\//);
diff --git a/node_modules/react-native/sdks/hermes-engine/hermes-engine.podspec b/node_modules/react-native/sdks/hermes-engine/hermes-engine.podspec
index 50af7e3..8a88b30 100644
--- a/node_modules/react-native/sdks/hermes-engine/hermes-engine.podspec
+++ b/node_modules/react-native/sdks/hermes-engine/hermes-engine.podspec
@@ -6,0 +7 @@ require "json"
+require "pathname"
@@ -93,0 +95,2 @@ Pod::Spec.new do |spec|
+      # OneKey patch: Keep the podspec checksum independent of the checkout path.
+      hermes_compiler_path = "$(PODS_ROOT)/#{Pathname.new(hermes_compiler_path).relative_path_from(Pathname.new(Pod::Config.instance.project_pods_root))}"
