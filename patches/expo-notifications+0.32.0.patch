diff --git a/node_modules/expo-notifications/android/src/main/java/expo/modules/notifications/tokens/PushTokenModule.kt b/node_modules/expo-notifications/android/src/main/java/expo/modules/notifications/tokens/PushTokenModule.kt
--- a/node_modules/expo-notifications/android/src/main/java/expo/modules/notifications/tokens/PushTokenModule.kt
+++ b/node_modules/expo-notifications/android/src/main/java/expo/modules/notifications/tokens/PushTokenModule.kt
@@ -20,12 +20,16 @@
    * @param token New push token.
    */
   override fun onNewToken(token: String) {
-    sendEvent(
+    try {
+      sendEvent(
       NEW_TOKEN_EVENT_NAME,
-      mapOf(
-        NEW_TOKEN_EVENT_TOKEN_KEY to token
+        mapOf(
+          NEW_TOKEN_EVENT_TOKEN_KEY to token
+        )
       )
-    )
+    } catch (_: Exception) {
+      // Module registry not ready, token will be fetched on next app launch
+    }
   }

   override fun definition() = ModuleDefinition {
